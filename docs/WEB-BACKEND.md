# King of the Garbage Hill — Web Backend (API, SignalR, push, auth)

> Code-verified against the working tree of 2026-07-14 (v4.5.6). Companion docs: [ARCHITECTURE.md](ARCHITECTURE.md) (§1 process topology, §6 state→screen), [WEB-CLIENT.md](WEB-CLIENT.md) (the Vue SPA consuming this surface), [DISCORD-INTERFACE.md](DISCORD-INTERFACE.md) (the other frontend), and [DAILY-QUESTS.md](DAILY-QUESTS.md) (the quest economy). This doc is the deep catalog of the ASP.NET Core surface: every endpoint, hub method, pushed event, and visibility rule.

## 1. Startup & topology

Process topology is in ARCHITECTURE.md §1; the web half in one paragraph: `StartWebApi()` runs on a fire-and-forget background task (`Program.cs:76`) inside the same process as the Discord bot. It builds a separate ASP.NET Core DI container and **bridges the Lamar singletons into it by instance** — `Global`, `GameReaction`, `CheckIfReady`, `GameUpdateMess`, `HelperFunctions`, `CharactersPull`, `CharacterPassives`, `Config`, `HttpClient`, `StartGameLogic`, `UserAccounts`, `DiscordWidgetService` (`Program.cs:104-115`) — so both frontends mutate the same live `GamesList` (`Global.cs:17`). Web-only singletons are registered on top: `WebGameService`, `GameNotificationService`, `GameStoryService`, `BlackjackService`, `BattleshipService`, `ReplayService` (`Program.cs:118-123`).

- JSON is camelCase for both SignalR (`Program.cs:126-131`) and controllers (`Program.cs:134-139`) — the TS interfaces in `signalr.ts` mirror C# DTOs 1:1 modulo casing.
- CORS default policy (`Program.cs:142-158`): origins `http://localhost:5173` (Vite), `http://localhost:3535`, `http://localhost`, `http://kotgh.ozvmusic.com`, `https://kotgh.ozvmusic.com`; any header/method + credentials.
- Middleware order (`Program.cs:160-204`): `UseDefaultFiles` → `UseStaticFiles` (wwwroot = built Vue SPA, `Program.cs:163-164`) → static `/art` from DataBase/art (`Program.cs:167-176`) → static `/sound` from DataBase/sound (`Program.cs:182-195`) → `UseRouting` → `UseCors` (`Program.cs:197-198`) → `MapControllers` (`Program.cs:200`) → `MapHub` at /gamehub (`Program.cs:201`) → SPA fallback `MapFallbackToFile` to `index.html` (`Program.cs:204`) — this fallback is what makes the client's history-mode routes work on refresh.
- Port: `KOTGH_PORT` env var, default 80, binds 0.0.0.0 (`Program.cs:206-208`). The env var is read **only** here; Config holds `Token`, `AnthropicApiKey`, and the default-off `GameStoryEnglishEnabled` switch (`Config.cs:9-29`, loaded from DataBase/config.json at `Config.cs:16`).
- In `--sim` mode neither Discord nor Kestrel starts (`Program.cs:65-74`); anything that needs a hub context (story push, notifications) simply never runs headless. The same block disables the Haiku service and replay capture (§9).
- Global web-relevant surface: `GamesList` (`Global.cs:17`), `FinishedGamesList` (`Global.cs:35`), and three callbacks — `OnGameFinished` (`Global.cs:41`) and `OnReplaySave` (`Global.cs:46`) registered by GameNotificationService, `SimErrorSink` for the sim harness (`Global.cs:52`). Game IDs are sequential via `GetNewtGamePlayingAndId` (`Global.cs:92-96`, note the "Newt" typo — it's load-bearing API).

## 2. Identity & auth — the trust model (Доверие)

**There is no password, token, cookie, or server-side session for normal play. The client asserts a Discord ID and the server trusts it.** Impersonation is possible by design (hobby-scale); the only OAuth-verified path is the profile widget (§12).

| Channel | Mechanism | Anchor |
|---|---|---|
| SignalR | client invokes `Authenticate(string discordIdStr)`; parsed ulong stored in `Context.Items["discordId"]`, connection registered | `GameHub.cs:61-78` |
| REST | `X-Discord-Id` header parsed per request; missing/unparseable → 401 | `GameController.cs:204-210` |
| Widget | Discord OAuth Bearer token verified against users/@me | `DiscordWidgetService.cs:116-143` |

IDs travel as **strings** over SignalR because Discord snowflakes exceed JS safe-integer range (`GameHub.cs:15-16`; string reply at `GameHub.cs:76`). On disconnect the connection is unregistered and the only "session" — the live connection's `Context.Items` — is gone (`GameHub.cs:45-53`).

**Account ID ranges** (routing in `UserAccounts.GetAccount`, `UserAccounts.cs:78-100`):

| Range | Kind | Created by |
|---|---|---|
| ≤ 1 000 000 | bot | `CreateBotAccount`, PlayerType 404 (`UserAccounts.cs:160-177`) |
| normal snowflakes | Discord human | `CreateUserAccount`, PlayerType 0, prefix `"*"` (`UserAccounts.cs:131-158`) |
| ≥ 9 000 000 000 000 000 000 | web-only | `CreateWebAccount` (`UserAccounts.cs:185-202`) via hub `RegisterWebAccount` (`GameHub.cs:147-173`): validates username 1-32 chars, allocates a monotonic ID (`GenerateWebUserId`, `UserAccounts.cs:204-210`; counter resumed past max existing web ID at startup, `UserAccounts.cs:37-46`), auto-authenticates the connection and replies `WebAccountCreated` |

`PlayerType` semantics: 0 Normal, 1 Casual, 2 Admin, 404 Bot (comment at `UserAccounts.cs:138`). Admin unlocks raw logs, full fight math, unmasked opponents (§7) and `CreateTestGame` (`GameHub.cs:510-535`).

**Persistence**: accounts live in a `ConcurrentDictionary` (`UserAccounts.cs:17`) loaded at construction — except in simulation mode, where `UserAccounts.DisableDiskPersistence` starts the store empty and suppresses every write so parallel simulator processes cannot race or corrupt each other on the shared folder (ARCHITECTURE.md §10, finding M52); `IsPlaying` is force-reset for everyone at startup (`ClearPlayingStatus`, `UserAccounts.cs:61-65`). The guarded 60 s fallback flush and reward-critical writes both route through boolean `SaveAccount`, which locks the account across serialization/write and lets user-triggered transactions restore state when a write fails (`UserAccounts.cs:113-139`). Storage reports write success after replacing the canonical JSON through a unique same-directory temporary file; startup loads only `discordAccount-{numericId}.json`, ignoring temporary/backup artifacts (`UsersDataStorage.cs:28-80,83-116`).

Startup also runs the private unknown_bug account migration before normal use: legacy/current chance rows merge, every purchased hidden-weight change is refunded and the remaining internal row is reset to ×1.00; discovery is removed; statistics/mastery are merged under the new private key; history/last-played are renamed; any forced-next-character value is cleared; the two retired achievement IDs are purged from progress/notification queues. The account is immediately saved when any field changes (`UserAccounts.MigrateUnknownBugAccount`). Public stats/store/widget/auth DTOs still filter the migrated private rows.

## 3. REST endpoints (api/game, api/widget)

`GameController` (route api/game, `GameController.cs:16-19`) is the HTTP alternative to the hub — **only a subset of actions exists here**. Gordon's three player-facing actions deliberately have REST parity; most other character-specific actions, draft, quests and mini-games remain hub-only. Auth = `X-Discord-Id` header except where noted. All action endpoints return success:true or a 400 error payload.

| Verb & route | Body / query | Auth | Delegates to | Anchor |
|---|---|---|---|---|
| GET `lobby` | — | none | `GetLobbyState` | `GameController.cs:34-38` |
| GET `{gameId}` | — | header | `GetGameState` (scoped DTO) | `GameController.cs:40-48` |
| GET `{gameId}/spectate` | — | none | spectator DTO | `GameController.cs:50-55` |
| POST `{gameId}/attack` | `AttackRequest` (TargetPlace) | header | `WebGameService.Attack` | `GameController.cs:59-67` |
| POST `{gameId}/block` | — | header | `.Block` | `GameController.cs:69-77` |
| POST `{gameId}/gordon/announce-halflife3` | — | header | `.AnnounceHalfLife3` | `GameController.cs:79-86` |
| POST `{gameId}/gordon/wake` | — | header | `.WakeGordon` | `GameController.cs:88-95` |
| POST `{gameId}/gordon/halflife3-decision` | `GordonHalfLifeDecisionRequest` (Serial, Choice = `freeze`\|`postpone`\|`release`) | header | `.ResolveHalfLife3Decision` | `GameController.ResolveHalfLife3Decision` |
| POST `{gameId}/auto-move` | — | header | `.AutoMove` | `GameController.cs:108-116` |
| POST `{gameId}/change-mind` | — | header | `.ChangeMind` | `GameController.cs:118-126` |
| POST `{gameId}/confirm-skip` | — | header | `.ConfirmSkip` | `GameController.cs:128-136` |
| POST `{gameId}/confirm-predict` | — | header | `.ConfirmPredict` | `GameController.cs:138-146` |
| POST `{gameId}/level-up` | `LevelUpRequest` (StatIndex: 1=Int 2=Str 3=Speed 4=Psyche) | header | `.LevelUp` | `GameController.cs:148-156` |
| POST `{gameId}/moral-to-points` | — | header | `.MoralToPoints` | `GameController.cs:158-166` |
| POST `{gameId}/moral-to-skill` | — | header | `.MoralToSkill` | `GameController.cs:168-176` |
| POST `{gameId}/predict` | `PredictRequest` (TargetPlayerId, CharacterName) | header | `.Predict` | `GameController.cs:178-186` |
| POST `{gameId}/aram-reroll` | `AramRerollRequest` (Slot: 1-4 passives, 5 stats) | header | `.AramReroll` | `GameController.cs:188-196` |
| POST `{gameId}/aram-confirm` | — | header | `.AramConfirm` | `GameController.cs:198-206` |
| GET `replay/{hash}` | — | none | `ReplayService.LoadReplay` | `GameController.cs:210-215` |
| GET `replays` | query limit (default 20, clamped 1-50) | header | `account.ReplayHashes` → `LoadReplaysByHashes` | `GameController.cs:217-229` |

Request DTOs: `GameStateDto.cs:283-312`. `WidgetController` has the single endpoint POST api/widget/sync with body `SyncRequest` (AccessToken) → `TryVerifyAndAuthorizeAsync` (`WidgetController.cs:11-35`), see §12.

## 4. SignalR hub `/gamehub` — client-callable methods

`GameHub` (`GameHub.cs:24`). Connection life-cycle: `OnConnectedAsync` logs only (`GameHub.cs:41-45`); `OnDisconnectedAsync` unregisters the connection (`GameHub.cs:47-55`). Rooms: SignalR group `game-{gameId}` joined in `JoinGame` (`GameHub.cs:99-105`), battleship group `bs-{gameId}` (`GameHub.cs:1266-1325`). Connection→player and connection→game maps live in GameNotificationService, not the hub (`GameNotificationService.cs:32-35`).

Every game-action method starts with `GetDiscordId()` (`GameHub.cs:1776-1781`) and replies event `Error` = "Not authenticated. Call Authenticate first." if unset (`SendNotAuthenticated`, `GameHub.cs:1783-1786`). Action methods reply `ActionResult {action, success, error}` and then push fresh personal state via `PushStateToPlayer` (`GameHub.cs:1788-1796`) — on success only, **except `Attack` which pushes unconditionally** (`GameHub.cs:233-242`).

**Session / lobby / rooms:**

| Method (args) | Effect | Anchor |
|---|---|---|
| `Authenticate(discordIdStr)` | bind ID to connection; a re-authentication first removes this connection from its previous player binding; replies `Authenticated {success, discordId, playerType, lastPlayedCharacter}`; the ultra-secret identity is replaced with an empty last-played value | `GameHub.Authenticate` |
| `JoinGame(gameId)` | join room, remember `Context.Items["gameId"]`, push scoped `GameState` (spectator DTO if unauthenticated or not a player), re-send stored AI story | `GameHub.cs:99-138` |
| `LeaveGame(gameId)` | leave room, unregister | `GameHub.cs:140-144` |
| `RegisterWebAccount(username)` | create web account (§2), auto-auth after removing any previous player binding for the same connection, `WebAccountCreated` | `GameHub.RegisterWebAccount` / `BindConnectionToPlayer` |
| `CreateWebGame()` | `WebGameService.CreateGame` (1 human + 5 bots, §8), auto-join room, `GameCreated` | `GameHub.cs:179-200` |
| `JoinWebGame(gameId)` | replace a bot (§8), auto-join room, `GameJoined` + state push | `GameHub.cs:205-228` |
| `CreateTestGame(characterName)` | **admin-only**, non-admin gets `Error`; validation uses the admin test catalog: public Tier ≥ 0 plus unknown_bug as its sole private entry | `GameHub.cs` / `WebGameService.cs` `CreateTestGame`; `CharactersPull.cs` `GetAdminSelectableCharacters` |
| `GetCharacterList()` | authenticated viewer-scoped request: regular accounts receive the public Tier ≥ 0 catalog; `PlayerType == 2` receives that catalog plus the sole private test choice. This preserves public achievement filters without crossing the private entry into a regular response | `GameHub.cs` / `WebGameService.cs` `GetCharacterList`; `CharactersPull.cs` `GetAdminSelectableCharacters` |
| `FinishGame(gameId)` | leave mid-game → replaced by bot via `EndGame` (`WebGameService.cs:979-991`) | `GameHub.cs:543-550` |
| `SetPreferWeb(gameId, preferWeb)` | sets `player.PreferWeb` directly — suppresses the player's Discord DMs (see DISCORD-INTERFACE.md §4) | `GameHub.cs:558-573` |
| `RequestGameState(gameId)` | on-demand push (player-scoped, falls back to spectator) | `GameHub.cs:1021-1041` |
| `RequestLobbyState()` | replies `LobbyState` | `GameHub.cs:1043-1047` |

**Core round actions** (all delegate to WebGameService, §8): `Attack(gameId, targetPlace)` `GameHub.cs:233-242`; `Block` `GameHub.cs:244-253`; `DoAutoMove` `GameHub.cs:286-295`; `ChangeMind` `GameHub.cs:297-306`; `ConfirmSkip` `GameHub.cs:308-317`; `ConfirmPredict` `GameHub.cs:319-328`; `LevelUp(gameId, statIndex)` `GameHub.cs:330-339`; `MoralToPoints` `GameHub.cs:341-350`; `MoralToSkill` `GameHub.cs:352-361`; `DemandContractReward(gameId, demandType)` (Geralt; demandType = previous|next) `GameHub.cs:363-372`; `Predict(gameId, targetPlayerId, characterName)` `GameHub.cs:374-383`; `AramReroll(gameId, slot)` `GameHub.cs:385-394`; `AramConfirm` `GameHub.cs:396-405`; `DraftSelect(gameId, characterName)` `GameHub.cs:409-418`.

**Character-specific actions:** Gordon `AnnounceHalfLife3(gameId)`, `WakeGordon(gameId)` and `ResolveHalfLife3Decision(gameId, serial, choice)` (`GameHub`, choice `freeze`, `postpone` or success-only `release`); `DarksciChoice(gameId, isStable)`; `YoungGleb(gameId)`; DooM Guy `DoomRoll(gameId)` / `DoomChainsaw(gameId, passiveName)`; `DeathNoteWrite(gameId, targetPlayerId, characterName)` (Kira); `ShinigamiEyes(gameId)`; Salldorum's `RewriteHistory(gameId, roundNumber)` (`GameHub.cs` `RewriteHistory`). Dopa's meta has no separate hub action: his second `LevelUp` uses indices 1–4 for Стомп/Фарм/Доминация/Роум and consumes no stat. Шэн likewise has no separate action contract: the shared submitted `Attack` consumes it automatically. Each Gordon method returns the ordinary `ActionResult` and pushes fresh owner state after success; the serial and decision kind reject stale/forged modal submissions.

**Meta (account-level, no game):** `RequestStore()` and `AdjustStoreCharacter(name, percentagePoints)` / `ResetStoreCharacter(name)` / `ResetStoreAllCharacters()` return owner-only `StoreState` snapshots (`GameHub.cs:565-603`). The service exposes only seen rollable characters, validates exact ±1/±10 requests against the 0.50×–2.00× **effective** bounds, prices sequential paid steps from 10 ZBS, calculates exact paid refunds, and saves every mutation under the account monitor with full rollback (`WebGameService.cs:1212-1425`; DTO `StoreStateDto`/`StoreCharacterDto`, `GameStateDto.cs:1076-1099`). `StoreCharacter.Multiplier` is effective paid-plus-loot weight and `LootBoxBonusPercentagePoints` identifies its permanent non-refundable part (`DiscordAccountClass.cs:64-83`); resets return only the paid component to ×1.00. unknown_bug is never discovered/listed and forged adjust/reset requests are rejected; its natural roll ignores `CharacterChance`. Web-created, joined, draft-selected and loot-reward public characters enter `SeenCharacters` (`WebGameService.DiscoverStoreCharacter`; `QuestService.GenerateLootBox`, `QuestClass.cs:893-977`; `StartGameLogic` roll weighting). DooM Guy `RequestDoomFortress()` → `DoomFortressState` and `EquipDoomModule(stage, slotIndex, moduleName)` retain their existing contract (`GameHub.cs:609-672`). Daily Quest, Loot Box and Achievement calls retain their save-before-publish and acknowledgement behavior: `AcknowledgeLootBox`, `RequestAchievements`, `ClearNewAchievements`, and `AcknowledgeAchievements` remain the public compatibility surface described below and in [DAILY-QUESTS.md](DAILY-QUESTS.md) / [ACHIEVEMENTS.md](ACHIEVEMENTS.md).

**Blackjack (dead-player mini-game, §10):** `BlackjackJoin` `GameHub.cs:1051-1067`; `BlackjackHit` `GameHub.cs:1069-1082`; `BlackjackStand` `GameHub.cs:1084-1097`; `BlackjackNewRound` `GameHub.cs:1099-1112`; `BlackjackSendMessage(gameId, words[])` `GameHub.cs:1114-1132` — the composed message is injected into the **main game's global logs** as `[Шинигами] {author}: "{message}"` and broadcast (`InjectGlobalLogMessage`, `GameHub.cs:1152-1170`). State pushes are personalized per seated player (`PushBlackjackState`, `GameHub.cs:1134-1150`).

**Battleship (standalone mini-game, §10):** standalone entrypoints are `RequestBattleshipLobby`, `RequestBattleshipStats`, `CreateBattleshipGame`, `JoinBattleshipWebGame`, `LeaveBattleshipWebGame`, `JoinBattleshipGame`, `LeaveBattleshipGame`, `RequestBattleshipState` and `RequestShipCatalog`. Setup actions are `BattleshipConfirmReady`, `BattleshipSelectArmy`, `BattleshipSelectFleet`, `BattleshipPlaceShip`, `BattleshipRemoveShip` and `BattleshipConfirmPlacement` (`GameHub.cs:1234-1424`; catalog request `GameHub.cs:1617-1620`). Combat exposes `BattleshipShoot`, `BattleshipShootOwnBoard`, `BattleshipSelectWeapon(gameId, weaponType, shotType, weaponId?)`, the no-shot Boarding action `BattleshipPassBoardingTurn(gameId)`, `BattleshipDeploySummon`, `BattleshipDeployPendingSummon`, `BattleshipManualMove`, `BattleshipSetCursedBoatDirection` and `BattleshipForfeit` (`GameHub.cs:1426-1603`). The optional weapon ID preserves exact source identity for special weapons; Ballista ignores exact-source selection and is one shared baseline action whose animation source is assigned on resolution. Battleship game IDs are **strings**. Pushes are personalized to both players, then a spectator DTO goes to the room minus player connections (`PushBattleshipStateToAll`, `GameHub.cs:1623-1650`).

## 5. Server→client events

| Event | Payload | Emitted when | Anchor |
|---|---|---|---|
| `Authenticated` | success, discordId (string), playerType, lastPlayedCharacter | after `Authenticate` | `GameHub.Authenticate` |
| `WebAccountCreated` | discordId (string), username | after `RegisterWebAccount` | `GameHub.cs:147-173` |
| `GameCreated` / `GameJoined` | gameId | create/join | `GameHub.cs:199` `GameHub.cs:225` `GameHub.cs:522` |
| `GameState` | full GameStateDto (§6, shape `GameStateDto.cs:8-62`) | on join/request, after each action (actor only), timer broadcast, finish | `GameHub` `PushStateToPlayer`; `GameNotificationService.SendGameStateToPlayer` |
| `ActionResult` | action, success, error | after every hub action | e.g. `GameHub.cs:239` |
| `Error` | string | invalid input / not authenticated / not found | `GameHub.cs:66-71,1689-1692` |
| `GameEvent` | envelope `{eventType, data}` | eventType `RoundChanged` on round flip (`GameNotificationService.cs:267`); `GameFinished` (`GameNotificationService.cs:83`); envelope dispatch `GameNotificationService.cs:218-220`; `GameStory` (`GameStoryService.cs:83-97`; re-sent on join, `GameHub.cs:134-143`); `BlackjackMessage` (`GameHub.cs:1182-1199`) | |
| `LobbyState` | LobbyStateDto | on request | `GameHub.cs:1043-1047` |
| `QuestState` | bilingual quests, daily/mastery/weekly receipts, reset/server times, reroll allowance, streak/ZBS/inventory, pity/odds, FIFO guaranteed-character head/count and optional resumable opening | on request or successful reroll | `GameHub.BuildQuestState`; DTO `QuestStateDto` |
| `LootBoxOpened` | immutable opening result: ID, rarity/ZBS, selected character portrait/tier, actual roll-weight bonus, optional next-game queue receipt/count, current balance/inventory, opening pity and pity-upgrade flag | durable new open or retry before acknowledgement | `GameHub.OpenLootBoxCore`/`MapLootBoxResult`; DTO `LootBoxResultDto` |
| `AchievementBoard` | all live entries plus unlocked/catalog totals and unacknowledged live IDs | on request | `GameHub.cs:903-947`; DTO `GameStateDto.cs:1108-1142` |
| `StoreState` | ZBS balance, base/effective bounds/refundable total, and each seen rollable character's avatar/tier/effective multiplier/permanent loot bonus/paid change count/next 1+10 prices/refund | on request and after every successful durable store mutation | `GameHub.cs:565-603`; DTO `StoreStateDto`/`StoreCharacterDto`, `GameStateDto.cs:1076-1099` |
| `CharacterList` | viewer-scoped name/avatar/tier catalog: public Tier ≥ 0 normally; plus the sole private test choice for `PlayerType == 2` | authenticated request | `GameHub.cs` `GetCharacterList` |
| `DoomFortressState` | four stages: slots, unlocked module definitions, remaining **standard** reward count/chance (special Приручить дракона excluded until its Sirinoks condition unlocks it) | request or successful equip | `GameHub.cs:609-672` |
| `BlackjackState` | personalized table state | after any blackjack action | `GameHub.cs:1051-1170` |
| `BattleshipLobby`, `BattleshipStats`, `BattleshipGameCreated`, `BattleshipGameJoined`, `BattleshipState`, `BattleshipEvent` (eventType `ShotResult`), `ShipCatalog` | lobby list; caller-only persistent W/L/streak/first-win/ZBS snapshot; personalized game state; shot event (`wasSkipped` distinguishes a consumed Penalty/Stun turn from an actual shot); catalog | battleship flow | `GameHub.cs:1234-1289` `GameHub.cs:1441-1480` `GameHub.cs:1617-1650` |

## 6. Push model — who sends `GameState` when

1. **Per-action**: the hub pushes to the **acting player's connections only** right after each action (`PushStateToPlayer`, `GameHub.cs:1694-1702`).
2. **Timer**: GameNotificationService polls every 300 ms (`GameNotificationService.cs:97-103`) and broadcasts a game when the round number changed, elapsed time moved > 0.5 s, any player's `IsReady` flipped, or the monotonic `GameClass.StateRevision` changed (`PushUpdates`, `GameNotificationService.cs:225-272`; triggers at `GameNotificationService.cs:250-256`). The revision trigger publishes Gordon's announcement, pause and decision even when the round/timer/readiness snapshot is otherwise unchanged. A `RoundChanged` event accompanies round flips (`GameNotificationService.cs:268-271`). **Finished games are skipped by the timer** (`GameNotificationService.cs:240-248`) — their final state would race the lootbox/achievement stamping in the finish path.
3. **On finish**: the game loop fires `OnGameFinished`, which does the final personalized broadcast, emits `GameFinished`, conditionally kicks off story generation, and drops snapshots/room tracking (kept while a Blackjack table is still open) (`GameNotificationService.cs:78-96`). Active Madara `Вечное Цукуеми` and every unknown_bug roster skip the shared story because either artifact would cross a private-view boundary (`GameNotificationService` finish callback).
4. **Broadcast shape**: `BroadcastGameState` sends a **personalized DTO to every player with a web connection**, then one **spectator DTO** to the remaining connections in the room (`GameNotificationService.cs:178-208`). `SendGameStateToPlayer` is the single-player variant used by game logic (`GameNotificationService.cs:163-171`).
5. Replay saving rides the same finish path: `OnReplaySave` builds + persists the replay and appends its hash to each human's `ReplayHashes` (`GameNotificationService.cs:50-67`). Activated `Вечное Цукуеми` games never invoke that shared callback (`CheckIfReady.cs:850-858`); unknown_bug rosters are rejected again inside both the callback and `ReplayService` as defense in depth.

**Halflife 3 transition pause:** after `CharacterPassives.HandleEndOfRound` but before status reset, regular-score settlement, replay finalization and `RoundNo++`, either a failed human Gordon review or the one-time successful Release/Wait offer parks that game's replay/watch continuation and returns from `CalculateAllFights`. The public top-level pause/deadline fields drive both clients while Gordon alone receives the decision kind/data. `CheckIfReady` retries the pending continuation first on later ticks; before the deadline it skips only that game and proceeds through the rest of `GamesList`. A submitted decision or the 20-second timeout resumes the shared `CompleteRoundAsync` tail exactly once; failure defaults to `freeze`, successful review to `release` (`DoomsdayMachine.ResumePendingRound`; `GordonFreeman.PrepareHalfLifeSettlement`/`ResolveHalfLifeTimeout`).

## 7. State mapping & hidden information (`GameStateMapper`)

`GameStateMapper.ToDto(game, requestingPlayer = null)` produces the per-viewer projection; null = spectator; `isAdmin` means PlayerType 2 (`GameStateMapper.ToDto`). Top-level shape: `GameStateDto.cs:8-62`; per-player `PlayerDto`: `GameStateDto.cs:66-144`.

Visibility rules (the exact reason the web can't leak hidden info):

| Data | Rule | Anchor |
|---|---|---|
| Opponent character | non-admin, unfinished → name "???", generic masked avatar, stats −1 sentinels, skill/moral "?", empty passives. unknown_bug remains in that ordinary mask for every non-owner even for real admins and after finish; the private missing portrait URL is not exposed by this row | `GameStateMapper.MapCharacter` / `HiddenCharacter` |
| Own/inspectable/finished character | real stats + resists and quality-bonus texts (resists/bonuses remain own-only). `AdminPlayerType` grants only enemy identity/stats/score inspection, not real-admin hidden logs/fights. unknown_bug's owner always receives the deliberately absent 404 avatar URL; the mapper never substitutes valid art | `GameStateMapper.ToDto` / `MapCharacter` / `MapStatus` |
| Passive list | hidden passives are normally absent until `Visible=true`. Dopa therefore emits four visible cards only: Макро, Взгляд в будущее, Permaban and Законодатель меты; the four tactic passives stay hidden, while the Законодатель меты card's description is mutated to the selected tactic text. The sole placeholder exception is the TheBoys owner: every hidden ultimate/combination (and any member re-locked by СуперМудень) is emitted as an anonymous `Visible=false` entry with blank name/description, preserving slot order without leaking text. `Вечное Цукуеми` is omitted permanently | `GameStateMapper.MapCharacter`; `CharacterPassives.ApplyDopaChoice` |
| Score | −1 unless isMe/admin/finished (`canSeeScore`); `Place` always visible | `GameStateMapper.MapStatus` |
| Public Rumbling warning/aftermath | top-level `IsRumblingWarningActive` is identical for players and spectators: true only after the living Eren's public Armin warning has opened round 10, and false from round 11. After resolution, public `RumblingKillCount` is the persistent 0–4 source-victim count used by every match view to scale fire/shake; it remains set in the final DTO after the warning audio flag clears | `ErenYeager.IsRumblingWarningActive` / `GetRumblingKillCount`; `GameStateMapper.ToDto`; M47 |
| Round-transition / release control | top-level `IsRoundTransitionPaused` and ISO-8601 `TransitionDeadlineUtc` are public and identical for players/spectators, allowing action-capable clients to lock the round and any presentation to show the same deadline. `HalfLifeReleaseSerial` is a public monotonic event identity so every live viewer can play the release celebration once. The decision itself remains owner-only | `GameStateDto.GameStateDto`; `GameStateMapper.ToDto`; `GameClass` transition fields |
| Current action status | `IsReady` remains public. During a live game, the real `IsBlock`, `IsSkip` and `ConfirmedSkip` values are emitted only for the owner or an inspectable/admin projection; every opponent and spectator row is projected as false. Finished-game projections may expose the settled values. False on a non-owned live row is therefore a privacy sentinel, not evidence that the player did not Block/Skip | `GameStateMapper.MapStatus`; DTO `PlayerStatusDto` |
| Personal logs, `ScoreSource`, `LvlUpPoints`, `MoveListPage`, `DirectMessages` (from `WebMessages`), `MediaMessages`, ARAM reroll counters | isMe-gated | `GameStateMapper.MapPlayer` / `MapStatus` |
| `ScoreBreakdown` (multipliers + per-source entries) | isMe/admin/finished; the ultra-secret player's breakdown remains owner-only so passive source names cannot leak through admin/final projections. The ordinary finished projection appends still-current entries created after the round snapshot, so final Mitsuki/Осьминожка debits are not lost | `GameStateMapper.MapStatus` |
| Predictions | owner always; **everyone at game end** with correctness + actual character/avatar. Sakura- and unknown_bug-targeted/name guesses are filtered completely; unknown_bug's actual identity/avatar remain masked to non-owners. Madara and unknown_bug owners receive empty assumption catalogs | `GameStateMapper.ToDto` / `MapPlayer`; `WebGameService.Predict` allowlist/target gate |
| `DeathNote` / `PortalGun` / `TerminalState` / `TsukuyomiState`, Darksci/Gleb choice flags | isMe-gated blocks on the player DTO. Terminal state carries buffered points, stream-target/active-node IDs, one-shot active flag and commit serial/amount | `GameStateMapper.MapPlayer`; `TerminalStateDto` |
| Terminal viewer | only its own row has `IsTerminalMode`/`TerminalState`; only its projection receives `HasTerminalMarker=true` on the current carrier. The generic contract deliberately contains no character identifier | `GameStateMapper.MapPlayer`; `PlayerDto` |
| TheBoys viewer | detection keys on character `Name == "TheBoys"` now that the removed `Пацаны` card no longer exists. Marked enemy `PlayerDto`s alone receive `IsTheBoysSupTarget=true`; a marked target's own projection and spectators receive false | `GameStateMapper.ToDto` / `MapPlayer`; `PlayerDto.IsTheBoysSupTarget` |
| Naruto trio | `PlayerDto.IsNarutoAlly=true` only on the other two members of the requesting Naruto's initialized trio; self, unrelated viewers and spectators receive false. This is an attack-UI affordance, while the shared server handler remains authoritative | `GameStateDto.cs` `PlayerDto.IsNarutoAlly`; `GameStateMapper.cs` `MapPlayer` |
| Madara Red Tiger | `PlayerDto.IsMadaraRedTiger=true` on the one row permanently recognized by Madara's round-9 `Красный Тигр!` line. Unlike private character marks, it is identical for every player and spectator projection; Discord derives the same public `🔴🐯` prefix directly from `Madara.IsRedTiger` | `Madara.ResolveRoundNine` / `IsRedTiger`; `GameStateMapper.MapPlayer`; `GameUpdateMess.LeaderBoard` |
| Widget states (`PassiveAbilityStates`) | the per-passive switch runs only for isMe and contains owner state only. TheBoys is the name-keyed exception so removing/locking its old `Пацаны` card cannot remove the widget; its DTO also carries `ButcherLeft`, active combination and ultimate-vs-combination unlock type | `GameStateMapper.MapPlayer`; DTO `TheBoysStateDto` |
| DooM Guy state | owner-only widget contains active/options/nests, BFG/Railgun charges, Chainsaw choices/remaining selections and live module progress: protected Rune grants, extermination count, Shield use/block count, current Counter-attack target names, Shark stance and Mission challenge success/failure. Let's Roll viewers receive empty character catalogs so predictions cannot be reconstructed client-side | `GameStateMapper.cs` `DoomGuyStateDto` mapping; DTO `DoomGuyStateDto` |
| Goblin level-up state | owner-only widget includes the three explicit 0–4 upgrade levels in addition to rates, so the client distinguishes Warrior rate 1/2 at level 3 from the identical rate at level 4 and can present a truthful next/max state | `GameStateMapper.cs` `GoblinSwarmStateDto` mapping; DTO `GoblinSwarmStateDto` |
| Dopa state | owner-only `DopaStateDto` exposes Взгляд cooldown/readiness, chosen tactic, whether the current pending point is the second-level-up meta replacement (`MetaChoiceReady`), and whether Макро still needs its second distinct action. The old top-level game-start `DopaChoiceNeeded` contract and `DopaChoice` hub/service action no longer exist | `GameStateMapper.cs:722-734`; DTO `DopaStateDto` |
| Salldorum state | owner-only `SalldorumStateDto`: Шэн charges; whether the fixed cola cell is occupied, its position/burial round, computed pickup-ready flag/round and internal drink count; rewrite-used/chosen round and completed-round position history | `GameStateMapper.cs` Salldorum mapping; DTO `SalldorumStateDto` |
| Эрен state | owner-only `ErenStateDto`: scheduled Rage gains, **round-10 Rumbling losses**, Attack-Titan active/cooldown state, Titan/Tatake audio serials, Rumbling result, and an aggregation of all per-player hatred marks | `GameStateDto.cs:761-771`; `GameStateMapper.cs:447-470` |
| Наруто state | owner-only `NarutoStateDto`: whether Гарем is active in the current calculation and its remaining two-turn cooldown; no Harem state or cancellation phrase is serialized to an enemy | `GameStateDto.cs` `NarutoStateDto`; `GameStateMapper.cs` Naruto mapping |
| Гордон state | owner-only `GordonStateDto`: resolved fights and 0–2 crowbar progress; wake used/available/Tsukuyomi-reserved flags; rescued and zombie totals; Gordon-owned active headcrabs with target ID/name/rounds left; nested Halflife 3 announced/finished/released/postponement state, announce availability, pending decision kind/serial/deadline, raw points/Подсчет-disabled flag/exponent/final total, exact server-authored button labels and decision message. No ordinary round multiplier is exposed in the decision DTO | `GameStateDto` `GordonStateDto`/`GordonHalfLifeStateDto`; `GameStateMapper` Gordon projection |
| Джон Сноу state | owner-only `JonSnowStateDto`: current Skill/228 target, King/blocked-by-Castle state, removable Bastard Intelligence, active hold/countdown, loyalty victories, whether the hidden resurrection has already fired, and the two current lowest-Skill non-Jon player IDs/names. Before the resurrection fires, the client renders no Watch hint or passive text | `GameStateDto.cs` `JonSnowStateDto`; `GameStateMapper.cs` Jon projection |
| Мадара normal state | empty prediction catalogs/list, empty Skill/Moral/target and zero resist/quality texts; no character-specific DTO was added | `GameStateMapper.cs:130-133,226-251,887-900` |
| Вечное Цукуеми final state | the authoritative round 10 skips every ordinary player and gives Madara one private activation line. Madara and Gordon-with-reserved-Wake receive the real ending with no synthetic fights; unknown_bug also receives the real ending and retains its actual fight log because its submitted action is immune to the round-wide rewrite. Each other non-Madara requester becomes alive/place 1 with projected place history, an exactly-needed `Вечное Цукуеми: +N` score-breakdown source and output-only fake wins against submitted target(s), falling back to Madara when none was submitted. The projected global log contains only that requester's ordinary winner announcement, without a Tsukuyomi-labelled fight-win or bonus sentence. Spectators still receive only `Результат игры скрыт.` with scores/places/history/fights/predictions cleared | `Madara.PrepareEternalTsukuyomiRound`; `Madara.GetProjectedFinalLogs`; `GordonFreeman.SeesEternalTsukuyomiReality`; `UnknownBug.Is`; `GameStateMapper.ApplyEternalTsukuyomiProjection` |
| Cross-character marks | never serialized as identifying annotations to the affected player's card unless the mechanic explicitly declares a public table mark. Owners see only their own kit's targets/objects through owner widgets or viewer-scoped leaderboard annotations; Goblin mines/Ziggurats, DooM nests/counter-attack, Salldorum's fixed cola-cell 🥤, sealed-Мадара 🚫 and Napoleon/Support relationships follow the owner-only rule. Gordon alone sees 🦀 plus remaining rounds on active headcrabs, 🧟 on zombies, and an always-present 🦀 on Краборак. Джон Сноу's fixed place-4 `🏰` cell and two lowest-Skill `🐺` players also follow the owner-only rule (only the Джон Сноу player sees them; the wolves expose current marked players, not Jon's private counters) — they were briefly public (m52) but were reverted to owner-only by M133. The shared round-10 `🚫` (`Tigr.IsRoundTenBanned`, Тигр always and Dopa after his top-1 Permaban trigger) is the only public prefix marker. Admins receive no tactical markers either — only enemy stat/identity through `AdminPlayerType`; spectators receive no populated custom leaderboard | `GameStateMapper.cs` custom leaderboard projection; `GameUpdateMess.cs` `CustomLeaderBoardBeforeNumber`; `Tigr.IsRoundTenBanned`; M35; m52; M133 |
| Global logs | admin raw for ordinary hidden mechanics; every non-owner projection additionally replaces both current and legacy private character identifiers with `???` | `GameStateMapper.SanitizePrivateCharacterText`; `StripHiddenLogs` |
| Fight log | hidden-from-non-admin entries filtered out; non-participants get `ScopeFightEntry` — outcome/participants/drops kept, detailed numbers zeroed, `TotalPointsWon` reduced to sign. Dopa's second Macro action sets both `HiddenFromNonAdmin` and `ShadowAction` for block, skip and resolved outcomes: only Dopa, that target and a real admin receive it, and the latter flag lets **Все бои** render the private row as a gray shadow action. A terminal owner additionally receives full source fights won by its stream target so Vue can render that target's POV. Every non-owner permanently receives the missing identity/avatar plus fully redacted numbers and mechanic-derived flags for any fight involving unknown_bug, even when that viewer was the other participant or is an admin | `GameStateMapper.ToDto` / `ScopeFightEntry` / `MaskPrivateFightIdentity`; `DoomsdayMachine.MarkHiddenFight`; `FightEntryDto.ShadowAction` |
| Full chronicle (Летопись) | built only when finished; usernames are replaced by character names. For every non-owner the ultra-secret player's entire personal-log section is omitted, while any identity literal that reached shared fight/global prose is replaced with `???` | `GameStateMapper.BuildFullChronicle` |
| Newly unlocked achievements | requesting player only, finished games; full paired metadata/rewards from a detached finish-time account snapshot, so another tab cannot mutate the enumerated queue | `CheckIfReady.cs` finish snapshot; `AchievementService.CreateSnapshot`; `GameStateMapper` achievement mapping |
| Locked secret achievement board entries | name/nameRu = `???`, exact descriptions replaced by hints, character list empty, ID changed to stable SHA-256-derived opaque `hidden-…`; full metadata after unlock | `GameHub.cs:1645-1679` |

Draft options are serialized only for the requesting player during the draft phase; first option cost 0, others 5, and every hidden passive is filtered before the option DTO is built. unknown_bug is excluded at option generation and defensive DTO mapping; when it was the requesting seat's natural result, the server auto-confirms that assignment and creates no option list, so the result cannot be inspected then declined (`StartGameLogic.RollDraftOptions`; `WebGameService.CreateGame`; `GameStateMapper` draft mapping). Avatars are rewritten to local /art/avatars when the file exists (`GetLocalAvatarUrl`). The character catalog for prediction dropdowns loads once from characters.json, excluding negative tiers, unknown_bug and "Выдуманный персонаж" (`GameStateMapper` static initializer).

After mapping, `PopulateCustomLeaderboard` adds the per-viewer leaderboard annotations: the same `CustomLeaderBoardAfterPlayer` / `CustomLeaderBoardBeforeNumber` strings the Discord leaderboard renders, converted from Discord markdown/emoji to HTML, plus the `IsInMyHarmRange` flag from speed-quality range vs place distance (`WebGameService.cs:196-228`; emoji map `WebGameService.cs:128-166`; `ConvertDiscordToWeb` `WebGameService.cs:169-190`). Madara never receives a Harm-range flag (`WebGameService.cs:200-205`). Both REST (`WebGameService.cs:115`) and SignalR (`GameNotificationService.cs:169` `GameNotificationService.cs:189`) run it.

## 8. `WebGameService` — the bridge into game logic

Web actions operate on the **same objects and mostly the same handlers as Discord buttons** (`WebGameService.cs:18-24`). Pattern: `FindGameAndPlayer` (`WebGameService.cs:530-543`), validate, then mutate or delegate. `CanAct` = not ready and not skipping (`WebGameService.cs` `CanAct`).

| Web action | Path into game logic | Anchor |
|---|---|---|
| Attack | `HandleAttack(player, null, targetPlace)` — `IsAutoMove` is temporarily forced so the handler reads the numeric botChoice instead of Discord component data; on refusal the message is popped from `WebMessages` and returned as the error. After final submitted/forced queues are sanitized, a Шэн charge consumes this attack; the holder takes the selected target's exact cell from either direction and only that target's existing primary action is redirected before calculation | `WebGameService.cs:473-503`; `Salldorum.ResolveShenDashes` |
| Block | Gordon aliases this shared action to `AnnounceHalfLife3`; a Теневые clone is rejected because only the original Naruto may Block; otherwise direct: `Спарта` → "Спартанцы не капитулируют!!", `Aggress` → "I. WONT. STOP."; Dopa `Макро` registers block as one of two actions via `WhoToAttackThisTurn`; else block+ready, log "Вы поставили блок" | `WebGameService.Block`; `Naruto.CanChooseBlock` |
| AnnounceHalfLife3 / WakeGordon / ResolveHalfLife3Decision | announcement is a turn-ending, no-Change-Mind Block replacement; wake clears Gordon's current Skip/stasis, cancels active Глаза Итачи targeting, or on round 9 reserves the exception only when Вечное Цукуеми is actually active; decision accepts `freeze`/`postpone` plus success-only `release`, requires the current serial/kind, and is the sole action allowed through the paused-transition gate | `WebGameService` Gordon actions; `GordonFreeman.AnnounceHalfLife3` / `CanWake` / `ResolveHalfLifeDecision` |
| AutoMove | direct: auto-move+ready + phrase log | `WebGameService.cs:557-575` |
| ChangeMind | direct: reset ready/block/targets, strike the previous log line through | `WebGameService.cs:577-603` |
| ConfirmSkip / ConfirmPredict | set `ConfirmedSkip` / `ConfirmedPredict` | `WebGameService.cs:605-626` |
| LevelUp | `HandleLvlUp(player, null, statIndex)` (same `IsAutoMove` trick). For Dopa's second upgrade, indices 1–4 choose Стомп/Фарм/Доминация/Роум through `ApplyDopaChoice`, decrement the point and return before any stat mutation | `WebGameService.cs:629-649`; `GameReactions.cs:1075-1095` |
| MoralToPoints / MoralToSkill | `HandleMoralForScore` / `HandleMoralForSkill` (min 5 / min 1 moral) | `WebGameService.cs:726-748` |
| DemandContractReward | full Geralt invoice logic inline (coins, displeasure, advance; ≥11 displeasure → death by "Вилы разъяренной толпы", −500) | `WebGameService.cs:750-836` |
| Predict | validates `characterName` against the public `GetVisibleCharacters` allowlist and rejects Sakura/unknown_bug target seats, then upserts into `player.Predict`; private/forged values are rejected server-side | `WebGameService.Predict` |
| AramReroll / AramConfirm | `HandlePassiveRoll` (slots 1-4) / `HandleBasicStatRoll` (slot 5); confirm sets `IsAramRollConfirmed` | `WebGameService.cs:864-898` |
| DeathNoteWrite / ShinigamiEyes | direct Kira state writes (once per round; eyes cost 25 moral). Sakura and current/legacy ultra-secret targets/identifiers are rejected or treated as nameless even for stale/injected entries; neither appears in the eye-reveal DTO, preventing a guessed-name oracle | `WebGameService.DeathNoteWrite`; `CharacterPassives.HandleEndOfRound`; `GameStateMapper` DeathNote mapping |
| DarksciChoice / YoungGleb | Darksci direct; Gleb transform copies the "Молодой Глеб" character sheet in place | `WebGameService` character actions |
| DoomRoll / DoomChainsaw | round-1 roll-mode activation / validated pending Chainsaw passive choice | `WebGameService.cs:954-977` |
| FinishGame | `EndGame` (bot substitution — same as the Discord Завершить Игру button) | `WebGameService.cs:979-991` |
| RewriteHistory | validates living Salldorum/once/before-round-8/past-round, then the shared resolver steals the historical multiplier from the fight-time-recorded recipient(s) of each distinct winner's point (including every actual Еврей thief), grants +2 Psyche/+2 buffered Justice, and applies the exact available/second-drink cola timeline rule | `WebGameService.cs` `RewriteHistory`; `Salldorum.cs` `RecordWinPointRecipients`/`RewriteHistory`; `CharacterPassives.cs` `HandleJews` |

While `GameClass.IsRoundTransitionPaused` is true, `FindGameAndPlayer` deliberately returns no actor for every ordinary action; only `ResolveHalfLife3Decision` opts into `allowPausedTransition`. This is a server authority boundary shared by REST and SignalR, not merely disabled Vue controls. The submitted serial must still match the current owner-only `DecisionSerial`, so duplicate/reconnected stale choices fail safely (`WebGameService.cs:535-543,655-666`).

Madara action gates are enforced server-side, not only hidden in Vue: round-8/sealed attack attempts and targeting sealed Madara are rejected by the shared `HandleAttack` path (`GameReactions.cs:689-737`); Block, AutoMove, ChangeMind and ConfirmSkip all reject round-8 or sealed Madara through `MadaraActionError`; `LevelUp` and `Predict` reject every Madara request (`WebGameService.cs:505-626,1151-1163,588-608,722-742`).

Initialized Naruto trio members are also forbidden targets in the shared `HandleAttack` path, so a forged REST/SignalR place remains rejected even if a client ignores `IsNarutoAlly` (`GameReactions.cs` `HandleAttack`; `WebGameService.cs` `Attack`).

**Game creation** (WebGameService.CreateGame): ordinary creation rolls the creator directly alongside five bot seats via `HandleCharacterRoll`; bot-mode tier normalization is applied only to those bot accounts, so the creator keeps ordinary human tiers, pity buckets and private natural-roll context. A queued Epic/Legendary loot character retains the guaranteed replacement path: it claims its matching bot, otherwise replaces the bot holding its mutually exclusive counterpart, otherwise replaces the first seat; it is consumed from the FIFO account queue and auto-confirmed rather than exposed as a paid draft alternative. Joining an existing game never consumes the queue. Competing guaranteed reservations are shuffled: only the first exact/incompatible reservation is assigned, and every rejected reservation remains unconsumed for a later game. A `CharacterToGiveNextTime` assignment is instead consumed by the creator's direct shared-roll seat and auto-confirmed. The shared rule defines LeCrisp/Толя and HardKitty/Эрен Йегер as mutually exclusive across natural, guaranteed and draft assignments (`StartGameLogic.AreMutuallyExclusiveCharacters`). The path re-snapshots a possible DooM Guy loadout from the creator account, then creates `GameClass` + Nanobot. Draft mode ordinarily places a newcomer-protected DooM Guy option first/free; an unknown_bug natural result is instead silently locked and auto-confirmed with no draft options. Its baseline natural weight is 40 and becomes 4000 for that lobby once every human participant's private `HasNaturallyRolledUnknownBug` bit is set; the bit is persisted at natural assignment time, so abandoned games count, while forced/admin selections do not. Bot seats do not participate and remain ineligible; legacy completed-game statistics do not backfill the bit because they cannot distinguish natural from forced/admin games. Lobby bot/human counts and joinability use strict `PlayerType == 404` seats: initialized Naruto clones are structural and cannot be replaced, and a pending Naruto roster reserves the two strict bots required for clone initialization. With exactly three strict bots and a bot original, only that original seat may be replaced, leaving the other two for clones (`WebGameService` `GetLobbyState`, `GetJoinableStrictBots`, `JoinWebGame`). Naruto is offered as a draft alternative only with at least two strict bot seats, and `DraftSelect` revalidates that invariant under the game monitor before any debit (`StartGameLogic.RollDraftOptions`; `WebGameService.DraftSelect`). The remaining paid-pick transaction still validates/persists under the account monitor and restores it on failure before replacing the bridge; both draft handlers reject forged private selections, exact duplicates and either mutually exclusive counterpart. Admin-only test creation instead validates through `GetAdminSelectableCharacters`, whose public Tier ≥ 0 catalog appends unknown_bug as its sole private entry, so persistent `PlayerType == 2` accounts can deliberately force it without exposing it to regular callers; its hidden preliminary roster roll explicitly suppresses natural-history recording before the selected test character replaces it. If the selected test character already exists or its mutually exclusive counterpart rolled on a bot, that bot receives the creator's original character before the creator is replaced, preserving the six-character roster invariant.

The queued-reward replacement is `WebGameService.cs:298-316`; the admin-test swap is `WebGameService.cs:1197-1216`.

## 9. Replays

- **Round contract (format v2)**: `ReplayDataDto.ReplayFormatVersion = 2`; each logical `ReplayRoundDto` owns **PreFightPlayers** (the action-locked, per-viewer state before that round's fight calculation), the same round's deep-copied `FightLog`/global logs, and one authoritative post-setup `Players` result. The last round also has explicit personal/global settlement suffixes that exclude the already-captured round-11 setup buffer (`ReplayDto.cs:9-17`; shapes `ReplayDto.cs:45-76`; `ReplayService.cs:51-158`). Each player snapshot still contains that player's own full-visibility DTO plus the leaderboard exactly as that player saw it (`ReplayService.cs:329-371`).
- **Capture lifecycle**: `CalculateAllFights` clears the prior web fight log before `BeginRound` (`DoomsdayMachine.cs:187-224`); `CaptureRoundResult` freezes only combat/global logs before `RoundNo++` so Тигр bans, Darksci tilt and other next-turn messages cannot enter that stream (`DoomsdayMachine.cs:1662-1667`). After next-round effects and sorting, `FinalizeRound` remaps result players atomically, including only current **awarded bonus** score entries (not pending regular points for a never-played next round), so score/place/death/breakdown agree while action flags and combat stats still come from **PreFightPlayers** (`ReplayService.cs:74-116,329-371`; `DoomsdayMachine.cs:1936-1937`).
- **Game end**: `CaptureFinalState` refreshes the existing last combat result after final debits/predictions instead of adding a fake round 11. It subtracts per-player and global post-setup baselines and stores only `HandleLastRound` additions, preserving Geralt's pitchfork/winner/final-score logs without replaying round-11 Darksci tilt (`CheckIfReady.cs:845-853`; `ReplayService.cs:122-158`).
- **Finalize**: on game end `BuildReplayData` assembles `ReplayDataDto` (8-char `ReplayHash`, player summaries, max captured logical round, full chronicle) (`ReplayService.cs:162-213`), saved as camelCase JSON into the Replays folder (`ReplayService.cs:21,217-227`). Legacy JSON deserializes with format version 0 and an empty pre-fight snapshot list; the Vue replay store adapts its boundary snapshots client-side. Its inseparable final raw log buffer is retained for compatibility.
- **Madara privacy exception**: an active `Вечное Цукуеми` has six incompatible final views, so finalization/save is skipped rather than storing one viewer's projection or the authoritative ending (`CheckIfReady.cs:849-858`).
- **unknown_bug privacy exception**: no replay is persisted for a roster containing the private character. `BuildReplayData`/`SaveReplay` fail closed, and load/list/by-game-ID paths reject both current and legacy summary names, so already-existing files cannot re-enter anonymous or account replay surfaces (`GameNotificationService` replay callback; `ReplayService.ContainsPrivateRoster`).
- **Serve**: REST endpoints in §3; the AI story is backfilled into the replay file when generated (`AttachStory`, `ReplayService.cs:257-272`).
- **Capture switch**: `ReplayService.CaptureEnabled` (`ReplayService.cs:36`) gates all per-round capture: with it off, `BeginRound` returns null — which short-circuits the round's remaining capture calls — and the final-state capture no-ops (`ReplayService.cs:51-158`). Production leaves it on; the headless simulator turns it off (`Program.cs:71`) because it neither saves nor serves replays, while capturing one costs a full six-player state projection — localization included — three times per round (finding M51).

## 10. Mini-games

- **Blackjack "Игра 21"** — for dead players of a running game, keyed by the main gameId. `BlackjackService` holds tables in memory (`BlackjackService.cs:14`), max 5 seats (`BlackjackService.cs:49-75`), stale tables cleaned every 5 min (`BlackjackService.cs:38-44`); winners earn the right to compose a message from a fixed word list (`blackjack_words.json`, loaded at `BlackjackService.cs:21-35`) which lands in the living players' global logs (§4). The web client auto-joins it when the player dies to Kira (WEB-CLIENT.md §6).
- **Battleship (Морской Бой)** — fully standalone 1-v-1 minigame with string game IDs, vs-bot creation and a public active-game lobby (`BattleshipService.CreateGame`, `BattleshipService.GetLobbyState`). Live matches remain in memory and a 5-minute sweep removes entries idle for 30 minutes (`BattleshipService.CleanupStaleGames`). Surface = the hub methods catalogued in §4; no REST or Discord game surface.
- Battleship account meta is persistent. Once combat has started, every finished match settles exactly once: human winners/losses update W/L, a win advances a consecutive UTC win-day streak, and the first win on a UTC day awards 10 ZBS (`BattleshipService.cs:45-145`; state fields in `BattleshipModels.cs:23-29`). Leaving before combat does not count; either player's direct leave during combat ends the match as a loss, matching forfeit semantics. `RequestBattleshipStats` returns only the authenticated caller's record, fresh current/best streak, first-win availability/value and ZBS balance (`GameHub.cs:1240-1248`). `BattleshipGameStateDto.MyEndReward` is populated from the per-player reward dictionary only when `ToDto` maps that same player's DTO; spectator state always gets null (`BattleshipService.cs:1662-1688`).
- Game log entries are `LogEntry { Text, VisibleTo }`; `game.AddLog`/`game.AddLogFor` create shared/personal entries (`BattleshipModels.cs:55-90`). `ToDto` filters by viewer—shared entries go to all, personal entries only to their owner, spectators see all logs—and the client receives a plain string array. Opponent fleets remain null for player DTOs. Owners/spectators receive exact deck HP; a player-facing fog board carries only occupancy plus intact/scratched/destroyed and explicit status markers, never opponent ship IDs or armor values (`BattleshipService.cs:1662-1935`).
- Battleship action authority is server-side. `BattleshipService.SelectWeapon` resolves special-weapon IDs to living source ships, living module decks, sufficient ammo and completed Aim. `MapAvailableWeapons` collapses all usable Ballistas into one shared baseline entry while retaining exact entries for Tetracatapults, Incendiary and Greek Fire. The Ballista projectile origin cycles through operational Mid sources from the front line, adds Close sources only in Boarding and skips destroyed sources. `MapPlayer` publishes `HasPendingBoardingDeployment`, server-authoritative `ShotDelayRemainingMs`, and legal weapon metadata in camelCase. During CAPTURE the list is Ballista-only. `CanPassBoarding` is true only on the current no-shot Boarding path with no global mandatory deployment. Duplicate ship definitions are numbered per fleet. Cell DTOs explicitly carry destroyed/whole-ship-sunk/frozen/devastated/captured/permanent-fire state plus the persistent set of every summon type that crossed the cell; Scout-revealed intact occupancy has no ship ID. `BattleshipService.SelectWeapon`/`MapAvailableWeapons`/`SelectNextBallistaAnimationSource`/`MapPlayer`; `CellDto`.
- `ShootOwnBoard` runs the same Penalty/Stun turn-start gate as enemy-board fire and is the only target surface for Greek Fire/CAPTURE. Greek Fire spends ammo only after legality validation, always ends the turn and resets selection; invalid attempts spend nothing. Free pending/boarding summons neither consume nor require one of the four ordinary per-match summon uses. Only a living non-boarding Ram may use turn-back redeployment; its entry cell immediately resolves fire, Freeze, collision, poison and reveal. `ShotResult` identifies the physical target board and projectile type, and carries the opaque source ship/deck identity so the owner can animate a real origin without exposing an opponent's fleet. `BattleshipService.DeploySummon`/`DescribeShot`; `BattleshipGameEngine.RegisterSummonOnTargetBoard`.
- Resolution uses the approved two-stage boundary: immediate shot/explosion result and victory/Boarding check; stable summon movement with per-cell fire → Freeze → live-deck collision → poison/reveal; then a second victory check and reset/switch. Any shot into an already destroyed deck is a miss before hit flags, Mast warnings, reset or bonus logic. Reveal ownership is centralized on the actor: empty Scout/explosion/ship-Space cells receive miss state, intact neighbouring occupancy is anonymous, and final Maneuvering Double death reconciles vacated destroyed-deck history only then. Destroying a CAPTURE-ship on its original owner's board credits the capturer with the ship-Space reveal. Ram damage marks the exact collision deck identically in both projections. `BattleshipGameEngine.ProcessCapturedShipShot`/`ProcessShipHit`/`RevealCell`/`RevealShip`/`ReconcileManeuverHistory`/`MarkRamDamageOnBoard`.
- The bot resolves at most one ordinary shot/pass per pump step. A combo-preserving hit establishes an 8-second server/client gate and `GameHub.RunBattleshipBotPump` waits with the game lock free; a miss has no delay. Human summon deployment remains legal during that response window. Penalty, Stun and invalid actions never move summons. Final Boarding remains one global guarded transition, but only a player without a living Mid line receives mandatory Close/Close-melee deployments. This is a global resolution barrier: all such ships must be placed before movement, reset or turn switch; the bot places its whole mandatory set in one step and cannot shoot through a human's pending set. A sole living Desiccator wins before deployment; two living Desiccators postpone only that auto-win until one dies, without disabling their other abilities. `BattleshipService.CompleteActionResolution`/`ResumeBoardingResolution`/`ProcessBotStep`; `BattleshipGameEngine.TriggerBoarding`/`CheckDesiccatorBoardingWin`; `GameHub.RunBattleshipBotPump`.

## 11. LLM services (outbound Anthropic API)

Two independent callers, both model `claude-haiku-4-5-20251001` (`GameStoryService.cs:29-32` `ClaudeHaikuService.cs:20`), key from `AnthropicApiKey` (`Config.cs:28`):

- **`GameStoryService`** (`GameStoryService.cs:21-42`; limits `GameStoryService.cs:29-32`): fire-and-forget post-game narrative, max 1800 API tokens **per language request**. The RU and EN prompts—not a post-generation trimmer—ask for at most 250 words / 1,700 characters in 8–15 very short lines, selecting only the 3–5 funniest character interactions and favoring action, revenge, ability collisions, log quotes and high-value moments over round-by-round chronicle or filler (`GameStoryService.cs:448-492`). Triggered from the finish path (§6); skips missing key, bot-only games and every unknown_bug roster (`GameStoryService.GenerateStoryAsync`). Its snapshot uses replay v2's captured playable rounds and structured fight pairs, retains explicit `HandleLastRound` suffixes only as `final-settlement`, and falls back to terminal `RoundNo - 1` only if no replay capture exists (`GameStoryService.cs:168-314`). The deterministic director card still varies presentation without changing facts (`GameStoryService.cs:335-563`). Russian and English calls run concurrently and fail independently; an empty response retries only that language once. Russian is always requested. English is default-off: set `"GameStoryEnglishEnabled": true` in `DataBase/config.json` to request it; false or an omitted setting skips that API call (`GameStoryService.cs:65-141`; `Config.cs:27-29`). If at least one call succeeds, both locale containers are stored, pushed as eventType `GameStory`, and backfilled into the replay; a missing language receives a localized unavailable message instead of suppressing the successful story or opening a blank tab (`GameStoryService.cs:77-95,637-647`; `GameHub.cs:134-137`; `GameNotificationService.cs:70-74`). Raw API call: `CallClaudeApi` (`GameStoryService.cs:663-697`).
- **`ClaudeHaikuService`** (`ClaudeHaikuService.cs:15-32`): `GenerateWitcherHintPairAsync` asks once for the same short Geralt clue as adapted `<ru>` and `<en>` variants, max 180 tokens and 5 s timeout. It rejects missing tags, oversized text, a non-Cyrillic Russian half or Cyrillic English half, returning null so the caller constructs a paired static fallback (`ClaudeHaikuService.cs:34-108`). Called from Geralt's Медитация passive for human players only (see DISCORD-INTERFACE.md §10). `Disabled` is forced true in `--sim` (`Program.cs:65-74`) so simulations never spend credits.

## 12. Discord profile widget (the one OAuth-verified flow)

- The Discord command `*widget_s` posts an OAuth authorize link-button (DISCORD-INTERFACE.md §9); the user approves and Discord redirects to the SPA /widget page with an access token in the URL fragment, which the page POSTs to the sync endpoint (§3).
- `TryVerifyAndAuthorizeAsync` resolves the real user via users/@me with the Bearer token, sets `WidgetAuthorized` on the account, then syncs (`DiscordWidgetService.cs:116-143`).
- `SyncAsync` PATCHes the user's Discord profile identity (application ClientId `901706293977432124`, `DiscordWidgetService.cs:18`, bot-token auth) with the 4 most recent **public** characters' avatars and win rates from `CharacterStatistics`; unknown_bug rows are filtered before ordering (`DiscordWidgetService.SyncAsync`). A 403 clears `WidgetAuthorized` (`DiscordWidgetService.cs:95-100`).
- Unrelated to the `SetPreferWeb` toggle (`GameHub.cs:558-573`) despite the shared "widget/web" vocabulary.

## 13. Per-account localization

`GameHub.SetLanguage` normalizes/persists `ru` or `en`, updates the process locale registry and immediately re-pushes the current personalized state (`GameHub.cs:83-96`). The mapper translates only viewer-owned logs, score sources, direct messages, media and the finished chronicle after the existing privacy gates; canonical `GameClass` logs and opponents' hidden state are not mutated (`GameStateMapper.cs:115-126,167-174,1088-1097`). Eternal Tsukuyomi's final-log replacement is localized for the viewer too (`GameStateMapper.cs:939-967`). Character/passive DTO text remains canonical until the Vue presentation boundary so prediction/draft values stay valid.

The general English catalog is copied to deployed `DataBase` and loaded lazily by `GameLocalization`; Vue imports it for ordinary bidirectional presentation. Longest-first fragments include markup-split and partially projected replay forms, such as `**обычных** очков`, `Class: +2 Cкилла`, Justice encouragements, troll outcomes and username-prefixed winner lines. Dynamic rules are idempotent across canonical and partially translated snapshots: Block/Auto Move, Darksci's tilt sentence, nemesis defeat connectors, Rumbling's place and Mitsuki's total finish the sentence while preserving dynamic values (`GameLocalization.cs:34-55`). Deterministic hardcoded character flavor that is not emitted by `PhraseClass` remains in this backend-owned exact catalog, with canonical and markup-stripped forms where required; Gordon and Jon add their exact name/passive/description keys plus direct bilingual dynamic records without changing canonical Russian dispatch strings. Character flavor selected by `PhraseClass` uses the separate backend-owned `phrases.en.json`; startup validates group/count parity and exact Russian source parity by index (`PhraseLocalization.cs:19-69,165-198`). Gordon's removed H.E.V. threshold group is absent from both the C# inventory and JSON catalog; his new round/release lines use replay-safe bilingual `PhrasePayload` records directly. Jon's fixed defeat/King/Castle phrases use paired phrase groups, while enemy-difficulty, redirected-enemy, resurrection and post-Watch replacements use direct `PhrasePayload` records. Dynamic Witcher-senses output follows the same payload boundary: Haiku produces both variants or the backend supplies the paired static fallback, then `PhraseV2` carries both through Discord, web and replay projection (`CharacterPassives.cs` Geralt meditation case). `tools/audit-localization.sh` also rejects a regression to the old single-language generator. Full contract: [LOCALIZATION.md](LOCALIZATION.md).

## 14. Account-reward transaction boundary

All account-economy operations synchronize on the `DiscordAccountClass` instance: the finish path settles account/history/mastery/ZBS/quest/top-two-box/achievement/statistics together; Battleship, Daily Quest, loot, paid draft picks and both Discord/web stores use that same account monitor (`CheckIfReady.cs:744-818`; `BattleshipService.cs:45-119`; `QuestClass.cs:358-508,638-723`; web store `WebGameService.cs:1212-1425`; Discord store `StoreReactions.cs:160-549`). This prevents simultaneous reward, reroll, pick or shop actions from losing a balance/box/progress update.

The conditional +69 ZBS TheBoys government salary is included inside that same finish transaction before saving (`CheckIfReady.HandleLastRound`; `TheBoys.ShouldAwardGovernmentSalary`).

Daily Quest day/week initialization is a durable user-visible mutation: `RequestQuests` snapshots the complete quest/economy state, normalizes/migrates with one UTC timestamp, saves before sending, and restores on failure. `RerollDailyQuest` uses the same contract, including replacement progress and any immediately earned card/daily/mastery/weekly rewards (`QuestClass.cs:283-355,414-508`; `GameHub.cs:675-742`). Game-end progress uses the ordinary settlement policy: retain the completed in-memory transaction for periodic retry if the immediate save fails. Full semantics: [DAILY-QUESTS.md](DAILY-QUESTS.md).

Loot results are retry-safe rather than client-animated randomness. `QuestData.LastLootBox` persists the last `OpeningId`; until acknowledged, every V2 open returns it without another debit/credit. `RequestQuests` returns that unacknowledged record with current account totals and the guaranteed-character queue head/count for reload/reconnect recovery (`GameHub.cs:675-710`). `OpenLootBoxV2` snapshots and restores ZBS, inventory, pity/result state, `CharacterChance`, `SeenCharacters` and `LootBoxCharacterQueue` around the save; acknowledgement retains the same durable boundary. Legacy `OpenLootBox` acknowledges inside the transaction so cached clients without the new acknowledgement call do not become stuck (`QuestService.OpenLootBox`, `QuestClass.cs:842-997`; `OpenLootBoxCore`/`CaptureLootBoxAccountState`, `GameHub.cs:839-940`, `GameHub.cs:1664-1715`). The reveal animation never determines any reward.

New results use `SecureRandom`: reward amounts use its inclusive bounds and rarity rolls use 10,000 equally likely outcomes with cumulative cutoffs 50/300/1500/4000, exactly preserving the published 0.5/2.5/12/25/60% base distribution (`QuestService.RollLootBoxTier`; RNG implementation `SecureRandom.cs:17-25`). The selected public character is another secure draw; Epic limits it to Tier 1–2 and Legendary to Tier 1. Pity upgrades only a tenth consecutive below-Rare natural result; the upgraded result receives the complete Rare ZBS/+3 character package and does not replace a natural Rare+.

User-triggered economy writes are durable before confirmation: Daily Quest, loot, achievement acknowledgement, paid draft, and both store transaction paths save while holding the account monitor, then restore their exact in-memory snapshot before returning a retryable error if replacement fails (web store `WebGameService.cs:1237-1382`; Discord store `StoreReactions.cs:160-549`). `SaveAccount` reports the atomic-replace result (`UserAccounts.cs:113-139`; `UsersDataStorage.cs:28-80`). A real-player game-end save failure is critical-logged and retains its settled in-memory state for the periodic retry; bot accounts deliberately skip the immediate disk write (`CheckIfReady.cs:812-818`).

Achievement V2 generally filters unknown legacy IDs rather than inferring retroactive rewards from match history (`AchievementClass.cs` catalog/board filtering; `GameHub.cs` achievement board). The two retired private-character IDs are the explicit cleanup exception: startup removes their stale progress and acknowledgement entries without clawing back already-issued rewards (`UserAccounts.MigrateUnknownBugAccount`). Daily Quest V1 instead has an explicit same-day economy migration: paid legacy days become settled without a second payout; unpaid days begin V2, while loot state is preserved (`QuestClass.cs:283-355,734-797`).

## 15. Known quirks & pitfalls (backend)

- **Asserted-ID auth**: any client can claim any Discord ID (§2). Never build features assuming web identity is verified; the only verified flag is `WidgetAuthorized` (`DiscordWidgetService.cs:141`).
- **REST is a subset** — new player-facing actions must be added to the hub (and usually only the hub); adding them to GameController is optional parity. Gordon's announce/wake/release-decision trio is an intentional parity exception (§3).
- **ARAM has hub/REST methods but no web UI** — AramReroll/AramConfirm are callable and the reroll counters are serialized, but the Vue client never renders an ARAM pick screen (finding **m24** in AUDIT-FINDINGS.md; WEB-CLIENT.md §13).
- `Attack` pushes state even when rejected (`GameHub.cs:228-235`) — the client relies on this to re-sync after refusals; other actions push on success only.
- Finished games are invisible to the 300 ms timer (§6) — if you add finish-time data to the DTO, populate it **before** the finish callback fires or it will never reach clients.
- New per-player web state needs the whole §7 pipeline: DTO member (`GameStateDto.cs`), mapper case with the right isMe/isAdmin gate (`GameStateMapper.cs`), TS mirror + widget (ARCHITECTURE.md §7 steps 11-14). Missing the gate = information leak to opponents/spectators.
- `KOTGH_PORT` is env-only (`Program.cs:206`); Config carries the bot token, Anthropic key, and default-off `GameStoryEnglishEnabled` switch (`Config.cs:9-29`).
- Discord IDs cross the hub as strings; the server parses with `ulong.TryParse` (`GameHub.cs:69-74`) — never let the client send them as JS numbers.
- Blackjack tables and live battleship games are in-memory only—restarts drop active matches. Battleship account W/L, streak and earned ZBS persist with the account; its cleanup timer sweeps every 5 minutes and removes matches idle for 30 minutes (`BattleshipService.cs:23-33,1647-1660`).
