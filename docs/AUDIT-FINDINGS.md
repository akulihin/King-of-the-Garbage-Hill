# Design-vs-Code Audit — Findings

> Original audit of `DataBase/characters.json` (+ `Game/GameDesign.txt` intent notes, root-level update notes) against the 2026-07-01 working tree (v4.1.8); statuses and fix notes re-verified through 2026-07-16 (v4.6.9). Historical “Code” bullets describe the pre-fix implementation when a later **Fixed** note is present. `CP` = `Game/GameLogic/CharacterPassives.cs`.
>
> Severity: **Critical** = player-visible wrong outcome / broken kit promise; **Major** = mechanic silently missing/misfiring or balance-relevant hidden behavior; **Minor** = cosmetic, flavor, dead code, small numeric drift; **Design question** = code self-consistent but intent ambiguous.

## Critical

### C1. Cyrillic "Салдорум" vs JSON name "Salldorum" — four dead branches
- **JSON**: character `Name` is Latin `"Salldorum"` (`characters.json:1316`). Live logic (fights, web actions, bot moral/target/block/build cases at `BotsBehavior.cs:645,2420,3356,4759`) correctly checks `"Salldorum"`.
- **Dead branches checking Cyrillic `"Салдорум"`** (can never match):
  - the pre-fix end-game branch — "Великий летописец: испорчено N записей" log never prints.
  - the pre-fix bot moral branch — always-moral→skill policy never applies.
  - the pre-fix bot attack-preference branch — never applies and also reads the *legacy* `SaldorumKhokholList`.
  - the pre-fix bot level-up branch — PSY-first policy never applies.
- **Impact**: bot Salldorum plays with untuned moral/level-up/targeting AI; an end-game log is lost. Human play unaffected.
- **Fix direction**: rename the four checks to `"Salldorum"`; rewrite the old targeting preference for the current kit (Шэн/rewrite), not the Khokhol legacy.
- **Fixed:** 2026-07-03 (pre-approved string bug) — renamed the three `Name == "Салдорум"` checks to `"Salldorum"` (current anchors: `CheckIfReady.cs:143-144`, bot moral `BotsBehavior.cs:645-648`, level-up `:4758-4759`). The dead Cyrillic targeting case was **deleted**, not renamed; the live Chronicler case is `BotsBehavior.cs:2420-2438`. Removed `BAD-NAME|Салдорум|C1` from `tools/known-warnings.txt`. (`SaldorumKhokholList` remains dead — leave for m6.)

## Major

### M1. Goblin "round-10 Ziggurat at place 1 ⇒ win" is a log line, not a win
- **Described** (`GameDesign.txt:508`): "Если на 10м ходу Гоблины строят зиккурат, находясь на 1м месте — они выигрывают."
- **Actual**: `DoomsdayMachine.cs:1574-1585` fires at the **start** of round 10 (after `RoundNo++`), requires place 1 + a ziggurat built at position 1, and only prints "…побеждает!". `HandleLastRound` re-sorts purely by score (`CheckIfReady.cs:400`) with no ziggurat rule. A ziggurat built *during* round-10 processing (`CP:6181-6251`, runs when `RoundNo` is already 11) can never trigger the message.
- **Impact**: the documented win condition doesn't win; a premature "wins!" message can appear a round early and then be false.
- **Fix direction**: enforce at `HandleLastRound` (like Premade's enforced win, `CheckIfReady.cs:494-516`) or drop the message + design line.
- **Fixed:** 2026-07-03 (designer verdict БАГ) — removed the premature/broken log in `CalculateAllFights` (it checked `BuiltPositions` before the round-10 Ziggurat is built) and added an authoritative enforced win in `HandleLastRound` (`CheckIfReady.cs`, right after the Premade block): a non-dead Стая Гоблинов with a Ziggurat at place 1 (`BuiltPositions.Contains(1)`) is bonus-pointed to 1st, re-sorted, and announced — mirroring Premade's overtake.

### M2. "Еврей" web widget renders for Толя with LeCrisp's state
- `GameStateMapper.cs:369-372` keys the widget on passive "Еврей" (both LeCrisp and Толя have it) but fills it from `LeCrispAssassins.AdditionalPsycheCurrent` — LeCrisp-only state. A Толя player sees a dead widget with stolen-psyche 0.
- **Fix direction**: gate on `Name == "LeCrisp"` (pattern: the Геральт case at `:686`).
- **Fixed:** 2026-07-03 — gated the mapper's Еврей case on `player.GameCharacter.Name == "LeCrisp"` (`GameStateMapper.cs:369`, mirroring the Геральт Name gate at `:686`), so the Jew/PROFIT widget is emitted only for LeCrisp; Толя (shares the passive but has no LeCrisp assassin state) no longer gets a dead PROFIT:0 widget. Frontend unchanged — the widget block (`PlayerCard.vue:1177-1185`) renders only when its jew key is present, now undefined for Толя. (The value shown is LeCrisp's assassin-psyche gain, the intended PROFIT display.)

### M3. AWDKA is silently forced to last place for every fight calculation
- `CheckIfReady.cs:1158-1172`: right before bots act (and before all fights), the "Произошел троллинг" holder is moved to the end of `PlayersList` and places re-assigned (comment `//end //AWDKA last`). Score order returns only at end of round.
- **Impact**: during fights AWDKA's place is ~6 regardless of score — inflates his underdog moral, changes Harm kite ranges, place-based passives and bot targeting against him. Documented nowhere.
- **Fix direction**: confirm intent; either document it in the passive description or delete the block (it mirrors the HardKitty "Никому не нужен" block right below it, so it may be a copy-paste leftover).

### M4. Toxic Mate "INT" negative-win rule applies only when he attacks
- `DoomsdayMachine.cs:843-849` negates the winner's point for "Никому не нужен"/"INT" holders only in the attacker-win branch; a defending Toxic Mate who wins gets a normal +1 (`:901-913`), and `CP:3033-3044` adds nothing on wins. JSON: "Побеждая — теряет очки" (unqualified). HardKitty's Mute wording ("если напал и победил") matches the code; INT's does not.
- **Fix direction**: extend the negation to the defense branch for "INT" (or reword the passive).
- **Fixed:** 2026-07-03 (designer verdict БАГ) — added an INT-only negation in the defender-win branch (`DoomsdayMachine.cs:978-981`): a defending Toxic Mate who wins now gets `AddWinPoints(-1)` like the attacker branch. Scoped to `PassiveName == "INT"` so HardKitty's "Никому не нужен" keeps its attacker-only "если напал и победил" behavior.

### M5. "Тигр топ, а ты холоп" has an undocumented second window at game start
- Initial `TimeCount = 3` (`Tigr.cs:10`) **plus** a swap in `HandleEventsBeforeFirstRound` (`CP:212-225`) put Тигр at place 1 immediately, consuming one count; the end-of-round swap (`DoomsdayMachine.cs:1384-1412`) then keeps him there for rounds 2–3. The random trigger (`TigrTopWhen`, rounds 1–8, 1–2 times) later resets the counter to 3 (`CP:4974-4978`) for the *described* "случайный момент" window. Тигр also collects +1 Psyche/+3 Мораль per round at #1 (rounds 2–9, `CP:5960-5967`).
- **Evidence of unintendedness**: designer note `GameDesign.txt:74-75` — "перемена местами должна срабатывать только когда тигр не топ1 (недавно оно вообще будто 2 раза за игру сработало)".
- **Fix direction**: start `TimeCount = 0` and drop the first-round case (keep only the random window), or document the opening window as intended.

### M6. Тигр "Лучше с двумя, чем с адекватными" counts Тигр himself
- `CP:3454-3470` loops `game.PlayersList` without excluding `player` — Тигр's own Int/Psyche trivially match, so at the end of round 1 he pockets +3 bonus points for "recruiting" himself (once, via FriendList dedupe).
- **Fix direction**: `if (t.GetPlayerId() == player.GetPlayerId()) continue;`.

### M7. Butcher pays his point on any win, spec says on a Drop
- the_boys.txt / theboys_update_commit: "+1 point **on drop**" ("очко если удалось его **Скинуть**" — Скинуть is the established Drop term). Code: `CP:3346-3347` awards +1 bonus (+2 SD) whenever Butcher *wins* against a marked sup. Wins are far more common than Drops — balance-relevant.
- **Fix direction**: decide win-vs-drop; if drop, hook into the Harm/Drop path (compare `dropsAfter > dropsBefore` in `DoomsdayMachine.cs:903-949`).
- **Fixed:** 2026-07-03 (designer verdict БАГ — "10 скилла за нападение, и очко если удалось ЕГО скинуть… скинуть при нападении") — the +1 bonus (+2 SD) moved out of the win check (`CP:3350-3351` removed) into the attacker-win Harm/Drop path (`DoomsdayMachine.cs:956-961`): awarded only when `dropsAfter > dropsBefore` and the attacker is Butcher and the target has `TheBoysSupMark`. The +10 Skill hunt bonus (win or loss) stays in the CP "Butcher" case.
- **Corrected:** 2026-07-10 — the first fix still paid only once per fight and used non-multiplying bonus points. The reward is now **`dropsAfter − dropsBefore` regular points** (×2 under СуперМудень), so five Drops of a marked sup pay +5 ordinary (+10 SD), all multiplied at round settlement (`DoomsdayMachine.cs:1075-1085`).

### M8. Toxic Mate "Tilted" rewards skips, not "психует"
- JSON: "Получает бонусное очко каждый раз, когда кто-то __психует__". Code (`CP:4317-4325`): +1 bonus per enemy whose `IsSkip` is set at end of round — no connection to psyche-loss ("психанул") events at all. (The +50 "все не смогли походить" half matches, `CP:4327-4332`, including the intentional "+20" joke log.)
- **Fix direction**: hook the +1 into `MinusPsycheLog`/psyche-rage events, or reword the passive to "за каждый пропуск хода".
- **Fixed:** 2026-07-03 (designer verdict БАГ — skips only pay when no battle happened) — removed the +1-per-skip bonus entirely; the payout is now the single **+50** given only when the whole round had **zero battles** (`game.PlayersList.All(x => x.Status.IsWonThisCalculation == Guid.Empty)`, `CP:4322-4330`). Uses the fight-resolution signal rather than the old all-blocked/skipped proxy, so forced fights (Монстр/Штормяк) correctly suppress the payout. Kept the intentional "+20" joke log; JSON text untouched.

### M9. Котики "Кошачья засада" (Штормяк) eats half of *total* score
- JSON: "сожрёт половину очков, которые враг получил **пока на нём сидел** этот кусок кота". Code (`CP:3176-3188`): on the return win, victim loses `Floor(GetScore()/2)` — half of their **entire score**, regardless of when it was earned (no snapshot at deploy time exists).
- **Impact**: a late Storm return can wipe 20+ points instead of the earned-while-sat handful — swingiest single effect in the game.
- **Fix direction**: snapshot the victim's score at deploy (`KotikiAmbush`) and halve the delta.
- **Fixed:** 2026-07-03 (designer verdict БАГ) — added `AmbushClass.StormScoreSnapshot` (`Kotiki.cs:21`), captured at Storm deploy (`CP:3251-3253`) and reset on return (`CP:3189`); the return steal is now `Floor((currentScore − snapshot) / 2)`, i.e. half of what the victim earned while the cat sat, not half of their total score (`CP:3176-3191`). Non-positive delta steals nothing; the −1 Psyche on the win is unchanged.

### M10. Premade's anti-skip un-bans a round-10-banned Carry
- JSON: "Carry никогда не пропустит ход. **(кроме банов)**". Code (`CP:5822-5835`) clears *any* involuntary skip (`IsSkip && !ConfirmedSkip`) on the Carry — including Тигр's round-10 "Стримснайпят и банят" ban (`CP:4956-4963` sets exactly that state) and Школьник's brother-ban. The freed Carry may then act on round 10 despite being "banned" (other systems — targeting refusal, Тигр-топ suppression — still assume he's banned).
- **Fix direction**: skip the anti-skip when the skip source is a ban (e.g. check the ban passive + round, mirroring `CheckIfReady.cs:1328`).
- **Fixed:** 2026-07-03 (designer verdict БАГ "добавляй") — the Premade anti-skip (`CP:5833-5841`) now computes `markedIsBanned` (round 10 + "Стримснайпят и банят и банят и банят") and leaves a banned Carry skipped. Scoped to the canonical Тигр round-10 ban (the "ban" the description's "кроме банов" means); ordinary involuntary skips (Митсуки no-PC, АФКА) are still lifted.

## Minor

### m1. "Вампур_" typo kills a flavor Easter egg
- Before the fix, the condition now at `GameUpdateMess.cs:1508` checked `Name == "Вампур_"` (JSON: "Вампур") — the garlic level-up placeholder never showed.
- **Fixed:** 2026-07-03 (pre-approved string bug) — `Name == "Вампур_"` → `"Вампур"` (`GameUpdateMess.cs:1508`); removed `BAD-NAME|Вампур_|m1` from `tools/known-warnings.txt` (audit re-run: no reappearance).

### m2. "Vampyr Позорный" logic is commented out
- `GameReactions.cs:1003-1009` (level-up denial) disabled; only the phrase object remains. Remove or restore.
- **Fixed:** 2026-07-03 (designer verdict — Вампур не должен прокачивать статы; если качает — забрать) — **restored** the block (`GameReactions.cs:1003-1009`): a Вампур level-up sets `skillNumber = 0`, so the stat switch adds nothing (the point is still spent at `:1134`) and "Никаких статов для тебя" is logged. Вампур has the `Vampyr Позорный` passive (`characters.json:588`), so the check is live, not a GHOST. Gematophagia bites (a separate win-reward mechanic) are unaffected.

### m3. Young Gleb transform keeps `Name == "Глеб"` → three misfiring Name checks
- Transform (`GameReactions.cs:256-268`) deliberately doesn't set the name (mylorik's Акула transform at `CP:6141-6148` *does*). Consequences: `GameUpdateMess.cs:1216` "Понизить один из статов" caption never shows post-transform; `CheckIfReady.cs:447` AWDKA-trolling flavor for Молодой Глеб unreachable; `GameReactions.cs:1134` old-Gleb psyche-10 phrase can fire for the transformed character. (`GameStateMapper.cs:297` / `GameUpdateMess.cs:1599` guards are harmlessly always-true.)
- **Fixed:** 2026-07-03 — kept the deliberate design (the transform leaves Name as Глеб so prediction, bot AI and Geralt logic keep matching) and repointed the three cosmetic sites to the young form's Main Ирелия passive — the unique marker the level-up nerf already uses (verified single occurrence in characters.json; Глеб lacks it). The level-up caption (`GameUpdateMess.cs:1216`) and the AWDKA-troll line (`CheckIfReady.cs:447`) now show the young-form text when that passive is present; the sleeping-Gleb psyche-10 phrase (`GameReactions.cs:1134`) is suppressed for it. Added a warning comment at the transform (`GameReactions.cs:258`) not to uncomment the rename. The level-up *mechanic* was never affected — the nerf keys on the Main Ирелия passive, not the Name — so this was cosmetic only. The two always-true guards (`GameStateMapper.cs:297`, `GameUpdateMess.cs:1599`) were left as-is per the finding.

### m4. `PassivesClass.GlebSkip` declared as `bool … = new()`
- `PassivesClass.cs:91` — compiles to `false`; clearly unintended syntax.
- **Fixed:** 2026-07-03 — changed the initializer to `= false` (`PassivesClass.cs:91`). It was a copy-paste of the surrounding reference-type `= new()` lines; for a bool `new()` already yields `false`, so no behavior change — GlebSkip is a plain flag (set at `CP:511`, tested/reset at `CP:2676/2687`). Pure clarity fix.

### m5. Exploit rotation runs without its owner
- `GameClass.RollExploit` + the round pipeline used to rotate/count exploit state even when nobody could consume it. Harmless bookkeeping.
- **Fixed:** 2026-07-04 — `RollExploit` gained an owner guard, so the rotation no longer flips flags nobody reads.
- **Superseded by the 2026-07-13 unknown_bug rework:** ownership is now detected by `UnknownBug.FindOwner`; `RollExploit` keeps exactly one living non-owner marker and advances it once per round. If the previous carrier is missing/dead/last, or its immediate successor is absent/dead, rotation restarts at the first living non-owner instead of skipping ahead. It permanently stops after `UnknownBug.TryCommitExploit` calls `GameClass.CloseExploit`. Draft and ordinary creation rebuild the candidate references before the first roll; a private natural result under draft is auto-locked and never enters the option dictionary (`GameClass.RollExploit`; `CheckIfReady` draft completion; `WebGameService` / `General` game creation).

### m6. Dead legacy code catalogue
- `LolGod.cs` + `PassivesClass.LolGodUdyrList` — "Бог ЛоЛа" doesn't exist; the only live reference is an always-true guard inside Darksci's "Не повезло" (`CP:2885`).
- `Saldorum.cs` (single-L "Хохол" design) vs live `Salldorum.cs`: orphaned cases "Парень с сюрпризом" (`CP:902, 2161`), "Сало" (`CP:916, 2175`; `GameUpdateMess.cs:634`), "Ниндзя" (`CP:1489, 2194`) — those passive names exist in no character and are never added at runtime.
- `CraboRack.BokoBoole` (in `CraboRack.cs`, pre-deletion lines 16-19) — zero references.
- "Молодой Глеб" JSON entry has `Tier: -2` — excluded from the roll pool by `CharactersPull.GetRollableCharacters` (Tier ≥ −1, `CharactersPull.cs:44-51`); transform template only. Note the tier semantics (`CharactersPull.cs:29-33`): **Tier −1 = secret but rollable** — Sakura and unknown_bug roll for humans at baseline range 40 (bots never roll tier <4). unknown_bug is additionally excluded from predictions, drafts, stores, achievements and every public character catalog. *(Corrected in verification — originally attributed to a range-0 roll.)*
- **Fixed:** 2026-07-04 — deleted: `LolGod.cs` and `Saldorum.cs` (whole files); the six orphaned GHOST cases (CP defense «Парень с сюрпризом»/«Сало», before-fight «Ниндзя», attack «Парень с сюрпризом»/«Сало»/«Ниндзя») and the «Сало» display case in `GameUpdateMess.cs`; the commented-out "LOL GOD, EXAMPLE" block (the "Бог ЛоЛа" BAD-NAME source) inside Darksci's «Не повезло»; dead state `PassivesClass.LolGodUdyrList`/`SaldorumKhokholList`/`SaldorumNinjaHidden` (live `SaldorumCorruptionCount` kept); dead phrases `SaldorumSurprise`/`SaldorumSalo`/`SaldorumNinja` (live `SaldorumChronicler` kept); `CraboRack.BokoBoole`. Removed the four `|m6` lines from `tools/known-warnings.txt` (audit re-run: clean). The Молодой Глеб tier note is informational — no change. In-code anchors above are historical (pre-deletion coordinates).

### m7. Stale comments (cosmetic)
- `CalculateRounds.cs:27` says TooGood sets "75 or 25" — code sets 70/30 (`:238, :249`).
- `CP:684` comment says tunnel escape is 33% — code rolls 50% (`:645`).
- **Fixed:** 2026-07-04 — the tunnel-escape comment now says 50% (`CP:682`). The `CalculateRounds.cs:27` half was already fixed in an earlier change-set (the comment reads "(sets 70 or 30)"); no code values touched, comments only.

### m8. Толя "Подсчет" recharge is 4–5 rounds, description says 2–3
- Initial cooldown *is* 2–3 (`PassivesClass.cs:31`), but after each use `Cooldown = Random(4,5)` (`CP:1130`), decremented once per round (`CP:6107-6116`). Net: ~2 uses per game instead of ~3.

### m9. Итачи Цукуеми recharge is 4 rounds, description says 2
- On activation `ChargeCounter = -2` (`CP:3016`); +1 per round (`CP:4210-4216`) → 4 rounds to full. Initial charge (0→2) matches the described 2.

### m10. Francie Хим.оружие ignores enemy-difficulty scaling
- Design note (the_boys.txt): "(normal +1, toogood +1, toostronk +1; если мы ту-гуд/стронк = +0) × прокачки". Code (`CP:3298-3307`): flat `chemLevel` bonus, zeroed when TheBoys were TooGood/TooStronk vs the victim. The "harder enemy pays more" half is missing.
- **Fixed:** 2026-07-03 (designer verdict БАГ "добавляй") — the bonus is now `chemLevel × (1 + (enemy TooGood for TheBoys ? 1 : 0) + (enemy TooStronk ? 1 : 0))` (`CP:3301-3315`), read from the attacker's `FightEnemyWasTooGood/Stronk` flags (= "my enemy was too good/stronk"). Normal enemy ×1, harder enemy ×2 (the TooGood/TooStronk tiers are set in exclusive threshold branches in `CalculateRounds`, so a win vs either pays double). The existing "+0 if TheBoys was TooGood/TooStronk vs the victim" gate is unchanged.

### m11. Ziggurat costs differ from the design note
- Code (`CP:6186-6231`): requires ≥1 of each type **and score ≥ 3** (undocumented gate), costs −3 bonus + a *permanent* −1 Worker deduction. Design note: "умирает 1 Трудяга (т.е. если каждый 9й — Трудяга, то умирает 9 гоблинов)" — i.e. population loss, not a permanent worker-slot loss. Current implementation is milder early, harsher late.
- **Fixed:** 2026-07-03 (designer verdict: build needs **>3** points, and exactly **−1 Трудяга** is correct — description unchanged) — the score gate `GetScore() < 3` became `<= 3` (`CP`), so the build now requires strictly more than 3 points; the −1 permanent Worker deduction was already the intended behavior and is kept. Documented the >3 gate in CHARACTERS.md (not in the player-facing text, by design).

### m12. Saitama's round-1 "serious targets" are effectively arbitrary
- SeriousTargets = top-2 by `GetSkill()` (`CP:284-293`), which at game start is 0 for almost everyone → stable-sort picks the first two in list order. Recomputed properly from the end of round 1 (`CP:4023-4032`). Also note "боевая мощь" = skill only (stats ignored) — Кратос-style stat monsters are never "serious".

### m13. HardKitty's opening −30 is score, logged as Мораль
- `CP:201-203`: `HardKittyMinus(-30)` lowers **Score** by 30 (bypassing the floor), while the personal log says "Никому не нужен: -30 *Морали*". One of the two is wrong; players reading the log get misdirected.

### m14. Butcher sup marks only exist from round 2
- Marks are assigned in `HandleNextRoundAfterSorting` (`CP:5908-5942`), which first runs at the end of round 1 — no sups (not even superheroes) during round 1. Probably fine; worth one line in the passive text if intended.

### m15. Salldorum's history rewrite ignores the round multiplier and the Еврей redirect
- Design note (`GameDesign.txt:549`): steal "(1 × множитель раунда)" from each winner, "Но следи за Евреями. Если они украли эти очки — то отнимается у евреев". Code (`WebGameService.cs:982-993`): flat −1/+1 bonus per winner, no multiplier (the comment even says "could scale"), no Jew redirection.
- **Fixed:** 2026-07-03 (designer verdict БАГ — "сделай как должно быть по описанию") — the steal became `1 × roundMultiplier(rewrittenRound)` (`roundNumber switch { <=4 => 1, <=9 => 2, _ => 4 }`) with the then-available co-winning-Еврей redirect.
- **Rework follow-up:** 2026-07-12 — the fight resolver now records the actual recipient(s) of every positive winning point against Salldorum after Jew and Штормяк redirects. The shared rewrite resolver uses that ledger, so an attacking Jew who lost but stole another winner's point—and multiple Jews who each received the duplicated point—are all debited exactly; repeat losses to the same winner remain deduplicated, while ally losses and suppressed/negative base points are explicitly ignored.

### m16. Геральт's Lambert fumble is 20%, design note says 10%
- `CP:4499` (`_rand.Luck(20)`, one-time) vs `GameDesign.txt:654` "10% Шанс". Also worth knowing: the meditation hint for human players calls the Anthropic Haiku API synchronously inside the round pipeline (`CP:4456-4484`) with a static fallback.
- **Fixed:** 2026-07-03 (designer verdict 10%) — `_rand.Luck(20)` → `_rand.Luck(10)` (`CP:4501`); BALANCE-CONSTANTS row updated. (`Luck(p)` with no range = `p >= rand(0,100)` ≈ p%.)

### m17. Dopa "Взгляд в будущее" also procs on blocks
- **Actual before the fix:** either dual-target could attack the other **or either target could block**. The description promised only the "attacked his next target" case. Lenient in Dopa's favor.
- **Fixed:** 2026-07-05 — removed the two block-based proc conditions; Vision now fires only when one of Dopa's two targets actually attacked the other (`CP:4687-4713`).
- **Bot follow-up:** the stale Фарм block heuristic was removed too. Legacy L1 retains its actual submitted co-attacker plan; fair L2/L3 instead use resolved historical target attention through `BotsBehavior.ApplyFairCharacterPreference`, never the current hidden attack queue.

### m18. "Привет со дна" counts skip *events*, not skipping players
- `CP:3683`: bonus = `game.SkipPlayersThisRound` (incremented once per skipped **fight**, `DoomsdayMachine.cs:595` — two attackers into one skipper = 2) + count of blockers. Mildly inflated vs "когда кто-то пропускает ход".
- **Confirmed intended** 2026-07-04 (designer verdict «ОК — по событиям»: каждый сорванный бой = очко). No code change; exact per-event behavior documented in CHARACTERS.md.

### m22. Latin "Saitama" vs JSON "Сайтама" — dead "👑 King" flair
- Before the fix, the leaderboard condition now at `GameUpdateMess.cs:751` checked `Name == "Saitama"` while the character is named "Сайтама", so the current #1 never received Saitama's "👑 King" marker. Same bug family as C1/m1. *(Found by `tools/audit-passives.sh` on its first run.)*
- **Fixed:** 2026-07-03 (pre-approved string bug) — `Name == "Saitama"` → `"Сайтама"` (`GameUpdateMess.cs:751`); removed `BAD-NAME|Saitama|m22` from `tools/known-warnings.txt` (audit re-run: no reappearance).

### m23. Dopa's `dopa-attack-select` menu is dead UI — selections silently ignored
- Before the fix, the now-deleted GetDopaMenu built a second-action select with custom-id dopa-attack-select and a dead "Dopa" passive branch attached it in the game-buttons builder. The component dispatch switch had **no case for it** (`GameReactions.cs:157,417-421`) — a click would defer and do nothing.
- The working Макро second action flows through the regular attack/block handlers instead (`GameReactions.cs:732-746`, `GameReactions.cs:329-352`), so the menu is pure decoration that looks interactive. *(Found 2026-07-04 during the interface-docs audit; docs/DISCORD-INTERFACE.md §5.)*
- **Fix direction**: delete `GetDopaMenu` + its attach (Макро already works via `attack-select` and `block`), or route the custom-id into `HandleAttack`.
- **Fixed:** 2026-07-04 — deleted `GetDopaMenu` and its `case "Dopa":` attach from `GameUpdateMess.cs` (Макро's real second action already flows through the regular `attack-select`/`block` handlers). Verification correction to the finding: the menu never actually **rendered** — the attach switch iterates `passive.PassiveName` and no passive named "Dopa" exists in `characters.json` (the character's passives are Макро/Пассивный импакт/…), nor is one added at runtime, so the `case "Dopa":` was itself dead and the select was unreachable UI rather than silently-ignored UI. Removed the `dopa-attack-select` row + §11 quirk line from `docs/DISCORD-INTERFACE.md`; downstream `GameUpdateMess.cs` anchors in the docs re-pointed (−49/−53 lines).

### m24. ARAM pick phase has no web UI (hub methods exist, screen doesn't)
- The backend and contract fully support web ARAM picks: `AramReroll`/`AramConfirm` on the hub (`GameHub.cs:332-352`) and REST (`GameController.cs:159-177`), `isAramPickPhase` + reroll counters serialized (`GameStateMapper.cs:952-970`), store wrappers wired (`game.ts:439-447`) — but **no Vue component calls them**; Game.vue's phase branches cover only the Draft overlay.
- During an ARAM game a web-preferring player sees only the waiting screen and must reroll/confirm from the Discord ARAM page (`GameUpdateMess.cs:1616-1640`). *(Found 2026-07-04 during the interface-docs audit; docs/WEB-CLIENT.md §13.)*
- **Fix direction**: an ARAM overlay in Game.vue mirroring the draft overlay (buttons → the store's `aramReroll` slots 1-5 / `aramConfirm`), or suppress the web-link DM during ARAM picks.

### m25. audit-passives.sh truncated PASSIVE-MAP.md when killed by the hook timeout
- The script wrote the report **directly into `docs/PASSIVE-MAP.md` while generating it**, and its per-passive greps took ~90 s on a `/mnt/*` (WSL2 9P) checkout — longer than the 60 s `PostToolUse` hook timeout (`.claude/settings.json`; the hook re-runs the audit after every edit to a passive-bearing file, `tools/hook-post-edit.sh`). A killed hook run left a clean-looking but truncated map (committed history oscillates: 171 → 119 → 171 → 118 table rows across `a17d86e`/`c6762f0`/`ce6a772`/`e0f7384`) and silently dropped the GHOST/BAD-NAME sections — which also broke the hook's own new-warning diff on the next edit. *(Found 2026-07-04 when the user noticed ~53 rows vanish from the map.)*
- **Fixed:** 2026-07-04 — two changes to `tools/audit-passives.sh`: (1) **atomic write** — the report is generated into `$OUT.tmp.$$` and `mv`-ed over the map at the end, so a killed run can never leave a partial file; (2) **single-pass indexing** — owners (one `jq`), CP case counts (one `grep -oP | uniq -c`) and cross-file refs (one `grep -HoF -f patterns` over the code list) are pre-computed into assoc arrays instead of ~350 per-passive greps. Runtime 86 s → **2.3 s**; output verified byte-identical to the last complete map (`ce6a772`) modulo the m6 deletions, and deterministic across runs.

### m27. Butcher's sup marks leak to their targets but are missing from the owner's leaderboard
- The owner DTO carried the marked players' nicknames in `TheBoysStateDto.SupMarks` and rendered them inside TheBoys' own left-side widget, rather than marking the corresponding enemy rows. Conversely, `TheBoysSupOnMe` was serialized onto a marked enemy's own private card and explicitly told them `Помечен как «суп»`. These removed DTO/UI fields are retained here as the pre-fix finding, not as current code references.
- **Impact**: the owner has to cross-reference a nickname list instead of seeing the table target, while enemies receive hidden Butcher targeting information they should not know.
- **Fix direction**: expose a viewer-scoped boolean on each marked enemy row only when the requester is TheBoys; render a distinct sup icon on that row; remove the owner nickname chips and the target-facing sup DTO/widget.
- **Fixed:** 2026-07-10; owner detection updated 2026-07-16 — `PlayerDto.IsTheBoysSupTarget` is populated only when the requester character is `TheBoys` (the obsolete `Пацаны` card no longer exists). Web enemy rows render a 🦸 `СУПЕР` badge; Discord appends 🦸 only in TheBoys' DM. The obsolete SupMarks nickname chips and TheBoysSupOnMe DTO/type/widget remain removed, so targets and spectators receive no mark disclosure (`GameStateMapper.ToDto` / `MapPlayer`; `PlayerCard.vue`; `GameUpdateMess.CustomLeaderBoardAfterPlayer`).

### m21. `SecureRandom` is not secure (naming hazard)
- `Helpers/SecureRandom.cs:25-45`: the crypto implementation is commented out; the service is a plain `System.Random` wrapper. Fine for a game, but the name misleads — and `PassivesClass` carries a private copy that *does* use `RandomNumberGenerator` (`PassivesClass.cs:280-299`), so trigger schedules are crypto-random while combat rolls aren't. Unify or rename.
- **Fixed:** 2026-07-04 (user chose **unify**) — one RNG for the whole game: `SecureRandom` gained a static core `Next(min,max)` wrapping `RandomNumberGenerator.GetInt32` (thread-safe — the old shared `System.Random` instance wasn't); the instance `Random` and `Luck` delegate to it, and the `PassivesClass` private crypto copy was deleted (ctor Толя-cooldown roll + `GetWhenToTrigger` now call `SecureRandom.Next`). All call-site semantics preserved exactly: inclusive max, the `Random(n, n−1) → n` edge (relied on by `GetWhenToTrigger(…, range 0)`), Luck's 0–100 roll. Out of scope, documented in ARCHITECTURE §9: the handful of direct `new Random()`/`Random.Shared` sites (exclusive-max semantics; converting them risks off-by-ones for no behavioral gain). In-code anchors above are historical.

## Design questions

### D1. Darksci can dodge "Дизмораль" by hoarding the round-9 level-up
- The −5 Psyche fires only inside `GetLvlUp` while `RoundNo == 9` (`GameReactions.cs:1235-1240`); saving the point until round 10 skips it (and the psyche-0 skip check). Bots always spend immediately. Intended tech or loophole?
- **Resolved 2026-07-03 (designer chose consistency, reversing the earlier «ОК»)**: the hoard was only ever possible on the WebUI via the level-up banking bug (M15) — on Discord the forced level-up page and the round-end auto-move both spend the point in round 9. M15's general web gate closes it, so Darksci now eats the −5 on both platforms.

### D2. Goblin Ziggurat can duplicate "Еврей" (and other Standalone passives)
- LeCrisp's "Еврей" is `Standalone: true` (`characters.json:140`), so Goblins can learn it (`CP:6215-6225`) despite the roll-time LeCrisp/Толя exclusivity (`StartGameLogic.cs:204-222`). `HandleJews` supports multiple jews (`CP:6739-6817`), each earning +1 while the victim's point is suppressed once. Verify which Standalone passives are safe to copy (full matrix in the Phase-3 audit).
- **Fixed:** 2026-07-03 (designer verdict ЗАПРЕТИТЬ) — the Ziggurat copy filter (`CP`, `standalonePassives` where-clause) now excludes `PassiveName == "Еврей"`, so Goblins can never learn it. Other `Standalone` copies are left as-is per D10.

### D3. Sakura's "Одна из трех" is a narrative win only
- `CheckIfReady.cs:519-531`: top-3 Sakura is declared `playerWhoWon` (logs, phrases; ZBS-100 only if scores tie) but keeps her real place for stats/mastery/TotalWins. Note she *is* a rollable secret character (Tier −1, m6) that nobody can predict — the soft win may be the intended compensation; confirm.
- **Fixed:** 2026-07-03 (designer verdict: place stays by fact, stats & rewards as 1st place) — the payout loop now computes a per-player `rewardPlace` = 1 for the top-3 `top3Player` (Sakura) else the real place, and keys TotalWins, mastery, ZBS, the top-2 loot box, and per-character Wins off it (`CheckIfReady.cs:664-761`). Her `GetPlaceAtLeaderBoard()` and MatchHistory record her real finish. The actual 1st-place player is unaffected (still gets their 1st-place payouts).

### D4. Passives whose logic keys on character Name, not the passive
- "Булинг": DeepList's "Стёб" spares LeCrisp by name (`CP:2583-2601`); `HandleJews` skips stealing from DeepList by name (`CP:6766-6770`).
- "Го играть": the block/skip bypass vs friends is implemented inside "Заводить друзей" (`CP:1259-1270`) — the passive named "Го играть" has zero references of its own.
- "lvl-мяк": the +1-Justice level-up is `Name == "Котики"` (`GameReactions.cs:906-913`).
- All three work today but break silently on rename/transfer; consider keying on the passive names. (Also the inverse hazard: transferred Standalone passives *do* dispatch for new holders — e.g. Ziggurat copies.)

### D5. "2kxaoc" exists only to stay visible
- Its only special handling: `GameUpdateMess.cs:798-811` masks *other players'* passive names in the stats display ("Неизвестно"/"❓ …"), and "2kxaoc" is one of four names **exempt from masking** (with Запах мусора, Чернильная завеса, Еврей) — the meme is deliberately left readable. No gameplay effect; confirm none is intended. *(Corrected in verification — the original finding described this backwards.)*

### D6. Вампуризм copies Justice instead of draining it
- "подсасывает себе **всю** Справедливость цели" — code adds the target's current Justice to Вампур's next-round buffer (`CP:1909-1913`) but never removes it from the target (contrast: Kimiko's Живое Оружие, which drains it; Близнец was changed in M26 to copy without draining). Confirm copy-vs-drain.

### D7. External stat changes on Стая Гоблинов are overwritten every round
- `CP:6166-6168` re-`Set`s Str/Int/Psyche from population each round end — debuffs like Спартанец's −1 Str vanish; Speed debuffs persist (Speed isn't population-driven). Inherent to the population design; document or special-case.
- **Fixed:** 2026-07-03 (designer verdict БАГ — external debuffs should persist) — added `GoblinPopulationClass.LastApplied{Str,Int,Psyche}Base` and a shared `ApplyGoblinPopulationStats` helper (`CharacterPassives.cs`) used at both the before-first-round init and the end-of-round recompute. It sets each stat to `populationBase + externalDelta`, where `externalDelta = currentStat − lastAppliedBase` (0 on the first run), so external Str/Int/Psyche changes now carry across the recompute like Speed already did.

### D8. "Пейзаж конца света" +7 очков is round-multiplied ×4
- The non-pawn reward for attacking Монстр on round 10 is +7 **regular** points (`CP:4420`) — committed with the round-10 ×4 multiplier = effectively **+28** (+10 bonus on top). If "+7 очков" was meant literally, use bonus points.

### D9. Premade copies the Carry's fight-moral instead of transferring it
- `CP:2435-2438`: the Support `AddMoral(carryMoral)` while the Carry keeps theirs. JSON "Добываемая в боях Мораль так же передается" reads as a transfer. Same copy-vs-drain question as Вампуризм (D6).

## Phase 3 — cross-character interactions

### M11. Шэн and Штормяк forced attacks ignore the round-10 Тигр ban
- Монстр's no-escape has an explicit ban carve-out (`CheckIfReady.cs:1324-1329`). The other two forced-fight sources don't:
  - **Шэн** (`CheckIfReady.cs:1232-1250`) forces everyone below the position to attack Salldorum — a round-10-banned Тигр (IsSkip, stats nuked) gets Salldorum added to `WhoToAttackThisTurn`, and the fight loop processes forced fights even for skipping players (`DoomsdayMachine.cs:427-443`) — the "banned, can't act, can't be targeted" promise breaks (0-stat Тигр is forced to fight).
  - **Штормяк taunt** (`CheckIfReady.cs:1269-1306`) excludes dead players but not the banned Тигр — he can be provoked into attacking Котики on round 10.
- **Fix direction**: reuse the Монстр carve-out condition in both sites (and consider dead-player checks ✓ already present).
- **Fixed:** 2026-07-03 (designer verdict БАГ "если нет исключения, Тигр остаётся в бане") — mirrored the Монстр carve-out `!(game.RoundNo == 10 && …Passive.Any(x => x.PassiveName == "Стримснайпят и банят и банят и банят"))` into the Шэн below-position pull (`CheckIfReady.cs:1265`) and the Штормяк taunt eligible-targets filter (`CheckIfReady.cs:1302`), so a round-10-banned Тигр is no longer forced to fight.
- **Rework follow-up:** 2026-07-12 — Шэн's pull now follows its attack-bound leap, but `Salldorum.ResolveShenDashes` retains the same explicit Тигр exclusion; Штормяк retains its readiness-stage carve-out.
- **Rework follow-up:** 2026-07-13 — Шэн no longer creates forced fights: it redirects only an already-existing primary attack for players crossed by the forward dash. The explicit Тигр exclusion remains, so his banned queue is never rewritten.

### M12. Монстр's apocalypse can kill Стая Гоблинов
- Every other kill source has an explicit goblin immunity: Кира's note (`CP:4100`), L-arrest (`CP:4799`), Кратос (`CP:1726`). "Пейзаж конца света" pawn deaths (`CP:4395-4406`) have **no** goblin check — Goblins guessed by Монстр become pawns and die on round 10, contradicting `GameDesign.txt:509` "Гоблинов нельзя Убить механикой 'убийства'".
- **Fix direction**: `if (pawn.GameCharacter.Name == "Стая Гоблинов") continue;` in the pawn loop (or block goblins from becoming pawns).

### D10. Ziggurat-copyable (`Standalone`) passives — a risk inventory
The Ziggurat copies any `Standalone: true` passive from the last attacked enemy (`CP:6213-6225`). Current inventory by behavior when a Goblin holds them:
- **Fully functional** (probably intended): Одиночество, Месть, Импакт, Еврей (see D2), Обучение, Лучше с двумя, 3-0 обоссан, Запах мусора, Я пытаюсь!, Произошел троллинг (⚠ also inherits the M3 forced-last!), Неуязвимость, Привет со дна, Лежит на дне, Ничего не понимает, Им это не понравится, Гематофагия, Панцирь, Болевой порог, Хождение боком, Питается водорослями, Оборотень, Безжалостный охотник, Клинки хаоса, Вороны, Изанаги (2 free auto-win defenses!), Аматерасу, Сомнительная тактика (huge self-nerf — must lose first fight vs everyone).
- **Self-brick**: **Булькает** — a Goblin who learns it loses all Мораль/Skill gains including the Ziggurat's own +5 Мораль (`CharacterClass.cs:963, 1010, 1125`). Funny, probably not intended to be learnable.
- **Dead copies** (game-start-only or Name-gated hooks): Лысина, Первая кровь, Похищение души (init-only — no effect when learned mid-game); Ведьмачьи заказы (every case gated `Name == "Геральт"`).
- **Fix direction**: maintain an explicit copyable-whitelist, or at least exclude Булькает and the dead copies.

### D11. Цукуеми × Чернильная завеса double-charges the same point
- If a player beats Осьминожка (ink fake-win: they get +1 now, owe it back at round 11) while under Итачи's Цукуеми, Итачи *also* copies that +1 at end of round and deducts it again at game end (`CP:4180-4208`, `CheckIfReady.cs:394-412`) — the victim repays the same point twice (once to Octopus's restore, once to Итачи). Rare, but both effects are "secretly repaid later" designs that don't know about each other.
- **Fixed:** 2026-07-03 (designer verdict — Итачи и Осьминожка each get their point, victim loses it once; duplicate for two receivers) — the round-11 ink restore (`CP:4887-4904`) now **skips its victim-debit** when that victim is in any Itachi's `ItachiTsukuyomi.StolenFromPlayers` ledger (the Цукуеми deduction at `CheckIfReady.cs:390` charges them instead). Octopus still applies its own `+N` credit (a positive RealScore entry), so the point is duplicated for both receivers while the victim pays once. Best-effort: the ledger is per-victim not per-round, so a victim stolen-from in a *different* round than the Octopus beat would also be skipped — a rare double-edge.

### Phase-3 checks that passed (selection)
Монстр no-escape spares pickle-Рика (he has neither IsBlock nor IsSkip); dead players are excluded from every forced-attack pool; Geralt's contract injection skips while he blocks/skips; Тигр-топ/Portal-Gun/Шэн/Storm-bite/Drops all respect the Ziggurat lock; the Глеб/Молодой-Глеб tea skip spares a charged Portal Gun; Premade anti-skip doesn't touch pickle-Рика; Котики are immune to a *transferred* Storm's taunt and a transferred Минька/Штормяк won't buff against its owner; PointFunnel observes authoritative resolved wins and awards its owner an independent +1 regular point, so the source payout and Еврей redirect are irrelevant; Октопус's ink correctly debits Евреи who stole the point.

## Phase 4 — plumbing (web/state layers)

### m19. Итачи's Crows and Izanagi are invisible in the web UI
- Only Tsukuyomi state is mapped (`PlayerDto.TsukuyomiState`, `GameStateMapper.cs:326`); crow counts per enemy and remaining Izanagi charges exist only in backend state. Recently-added character with the least UI coverage; every comparable kit (Goblins, TheBoys, Геральт) has a full widget.

### m20. Geralt's "Чеканная монета" demand economy is entirely undocumented
- A full hidden system: post-round demand/advance buttons, invoice totals, a Displeasure ledger, +2 regular per advance, and **death by pitchforks with −500 at Displeasure ≥ 11** (`CP:4567-4604`). Neither `characters.json` nor `GameDesign.txt` mentions displeasure or the death. (The design note's *other* hidden Geralt mechanics: "психует when a contract holder is killed" — **not implemented** anywhere; "dies on place 6" — implemented as a log line only, `CheckIfReady.cs:267-273`.)
- Also of note: the meditation hint calls the Anthropic API synchronously inside the round pipeline (`CP:4456-4484`).
- **Fixed:** 2026-07-05 (documentation-only) — the full demand economy is now documented in `docs/CHARACTERS.md` (Геральт's «Чеканная монета» entry): the two web/bot-only billings (immediate «За прошлый» + deferred «За следующий» advance), the `CalculateInvoice` coin/Displeasure tiers, and the pitchfork death checked in **both** the web handler (`WebGameService.cs:750-836`) and the end-of-round advance resolution (`CP:4596-4603`), plus the place-6 pitchfork *log-only* line. Web plumbing was already covered (WEB-BACKEND.md hub/service rows, WEB-CLIENT.md widget rows, BALANCE-CONSTANTS row, INTERACTION-MATRIX kill-source, GAME-DESIGN end-game). **Two design-note mechanics were deliberately left unimplemented and flagged for a designer decision, not built here:** "психует when a contract holder is killed" (absent anywhere) and "dies on place 6" (log line only). `characters.json`/`GameDesign.txt` are the designer's surface — not edited (CLAUDE.md).

### m28. Final Mitsuki/Осьминожка point debits are absent from the web score feed
- `CombineRoundScoreAndGameScore` snapshots `ScoreEntries` into `PreviousRoundScoreEntries` before the round-11 passive hooks (`InGameStatusClass.cs:245-328`). `Запах мусора` and Осьминожка's `Чернильная завеса` repayment then call `AddBonusPoints(-N)` after that snapshot (`CP:6404-6426,5142-5167`). The score itself changes, but `MapStatus` used only `PreviousRoundScoreEntries`, so the final web combo showed no negative row.
- **Fixed:** 2026-07-10, tightened 2026-07-11 — the finished `ScoreBreakdown` now concatenates still-current **awarded bonus** entries after `PreviousRoundScoreEntries` (`GameStateMapper.cs:1111-1131`). Pending regular entries belong to a never-played next round and are excluded. In-progress rounds keep the old snapshot-only timing, and the Vue feed renders the signed actual/multiplied value (`PlayerCard.vue:1768-1825`).

### M24. Replay snapshots attach previous fights to next-round state
- **Intended:** replay round R must combine the actions/stats locked before round R combat, round R's own fight/log stream, and the settled score/place from round R. Later `HandleNextRound` effects must not appear between those fights.
- **Actual before the fix:** the boundary snapshot was taken before the previous `WebFightLog` was cleared, then paired that old fight list with a freshly mapped current player state. The game-end capture appended the round-11 boundary as another apparent combat round. The client compensated only by taking the previous snapshot's character while retaining the current snapshot's status. Thus Тигр's round-10 ban and Darksci's next-round tilt/skip could appear amid round-9 fights, while round 1 was empty and the final result appeared as round 11.
- **Impact:** the replay falsely suggested that bans/skips interrupted already-submitted attacks and that combat was resolving against different turn states. This was display/persistence desynchronization; the live fight loop itself still locked the round's actions before resolving fights in leaderboard order (`CheckIfReady.cs:1114-1478`; `DoomsdayMachine.cs:220-224`).
- **Fixed:** 2026-07-11 — replay format v2 stores explicit `PreFightPlayers`, combat logs and settled `Players` for the same logical round, plus separate game-end log suffixes (`ReplayDto.cs:9-76`). `BeginRound` runs only after the old fight log is cleared; `CaptureRoundResult` freezes fights/global logs before `RoundNo++`; after next-round score effects and sorting, `FinalizeRound` remaps score, place, death and breakdown together while the fight-facing state stays frozen (`ReplayService.cs:35-104,301-342`; `DoomsdayMachine.cs:220-224`; transition `DoomsdayMachine.cs:1662-1667`; finalize `DoomsdayMachine.cs:1936-1937`). Final settlement refreshes that existing last round rather than appending a fake round 11, and exposes only log text added after the round-11 setup baseline, so Darksci tilt cannot leak back into round 10 (`ReplayService.cs:106-144,352-366`; `CheckIfReady.cs:845-853`). The Vue adapter keeps old raw URL round keys working while pairing each legacy result boundary with its predecessor; v2 uses real round numbers and explicit settlement deltas (`replay.ts:21-120,133-268`). Historical final raw log buffers remain visible for legacy files because they cannot be split after the fact. Regression fixtures cover legacy deep links/logs, v2 numbering, post-setup score/death, skip isolation and final personal/global settlement (`replay.spec.ts:66-223`).

### Plumbing checks that passed
Every remaining `PassiveAbilityStatesDto` member is mapped and rendered (no dead DTO/TS fields); mapper `case` strings all exist in `characters.json`; cross-character target marks are deliberately absent from the affected player's DTO and remain visible only to the passive owner (or explicitly public by design). unknown_bug's owner-scoped projection deliberately rides on the generic `PlayerDto.TerminalState`/`HasTerminalMarker` contract rather than `PassiveAbilityStatesDto`; no character/passive identifier is present in that browser contract.

## Verified-consistent highlights (no finding)

Worth stating because they're easy to suspect: Francie's final-turn contract win is settled in the per-fight hook and clears `OrderTarget` before the round-transition expiry/new-target branch; the display counter is no longer an eligibility gate, and any still-active target at the boundary always incurs its −1 (`CP:3559-3572,6114-6135`). Saitama's Неприметность deferral/reclaim matches the recent fix note exactly (incl. the manual round-10 moral flush, `CP:4864-4871`); Rick's pickle/portal player-control flow matches rick_update (bot never takes over, pickle stays attackable, gun charge music one-shot); the Тигр round-10 ban is respected by targeting, Монстр forced attacks and the Тигр-топ swap; Кира's +2/+4(L) numbers, 25-Мораль eyes cost, L-arrest from round 8 and −500 all match; Goblin growth/death percentages match the goblins commit (death is 10+0.5R²/3%, the *older* design note's 1·R²/3 was rebalanced); Ziggurat Standalone-only learning with duplicate protection; Выгодная сделка pays both the +1 bonus per deal and +5 Мораль per deal; Октопус's ink ledger correctly redirects debits to Евреи who stole the point; the Глеб-tea skip spares a charged Portal Gun ("ничто не помешает").

## Phase 5 — simulation harness & bot robustness

### M13. Discord service-channel debug calls freeze headless sims and mask the real exception
- Four diagnostic Discord-channel calls in the per-tick hot path were **not** null-guarded. In headless simulation (and during a Discord reconnect) they could throw and freeze the round. Current guarded sites are the two bot fallbacks (`BotsBehavior.cs:3478-3516`), the attack-handler catch (`BotsBehavior.cs:3562-3570`), and the ready-check fallback (`CheckIfReady.cs:1438-1451`).
- **Impact**: ~1/1000 sim games froze (sweep-20260702-191714 hit 5 batches) and the true exception (M14) was invisible — every recorded error read as a generic ready-check NRE. In production a Discord reconnect at the wrong moment can freeze a live game the same way until reconnect.
- **Fixed:** 2026-07-03 — added null-safe `Global.TrySendServiceMessage(string)`, routed the bot fallbacks through it (`BotsBehavior.cs:3478-3516`), and ordered the catch as `_logs.Critical` + `SimErrorSink?.Invoke(...)` before the guarded send (`BotsBehavior.cs:3562-3570`). Verified: previously-freezing line-up 0 stuck; 5000-game natural sim 0 errors/0 stuck.

### M14. Bot `HandleBotAttack` throws IndexOutOfRange when it has no valid targets
- The pre-fix random fallback built `players = allTargets.ToList()`. When the pool was empty after deaths/late-round filters, indexing it threw an argument-out-of-range exception. Кира-correlated; observed rounds 8–10.
- **Impact**: the bot's turn crashes. Post-M13 it is recorded as a per-game sim error; in production the bot fails its action and falls back to an automatic block plus debug-channel spam.
- **Fixed:** 2026-07-03 — `if (players.Count == 0)` blocks, resets preferences and returns (`BotsBehavior.cs:3478-3489`). Verified: Kira line-up 500 games → 0 errors/0 stuck. (The early `allTargets.First()` remains round-<5 guarded.) **See M16** — its sibling `Братишка` branch (`BotsBehavior.cs:2969-2988`) runs earlier.

### M15. WebUI let players bank level-up points instead of spending before continuing
- On Discord a granted level-up (rounds 3/5/7/9, `DoomsdayMachine.cs:1477-1481`) flips `Status.MoveListPage` to 3, so only the level-up menu renders. The web action handlers originally set readiness regardless of `LvlUpPoints`; a web player could act while carrying points into a later round. Bot/auto-move spending occurs before action in `BotsBehavior.HandleBotBehavior` (`BotsBehavior.cs:91-107`; called by `CheckIfReady.cs:1299-1310`).
- **Impact**: platform inconsistency and a real advantage — banking points to dump on demand, and dodging the round-9 Дизмораль −5 Psyche (see D1). Discord-impossible; WebUI-only.
- **Fixed:** 2026-07-03 — generalized the `Main Ирелия` guard into `WebGameService.LevelUpGate` (blocks any `LvlUpPoints > 0` from the four turn-ending actions; Ирелия keeps "Риоты не прощают, нерфа не избежать", everyone else gets "Остались очки прокачки — потрать их!"). Mirrored client-side: `store/game.ts` adds a `mustSpendLevelUp` computed + early-returns in `attack/block/autoMove/confirmSkip`; `pages/Game.vue` disables Block/Auto/Skip, tightens the Leaderboard `:can-attack`, and shows the same prompt. No soft-lock: every character always has an enabled level-up button in `PlayerCard.vue` while points remain, and the round-end auto-move force-spends any leftover.

### M16. Bot `HandleBotAttack` throws "Sequence contains no elements" in the Братишка per-character switch on an empty target pool
- The pre-fix `Братишка` block branch ran `allTargets.Min(...Justice...)` without checking the filtered pool. When late-round filters left no targets, it threw before M14's fallback guard. Кира-correlated; all three failures in the cited sweep were round 10 with Братишка + Тигр + Кира.
- **Impact**: the Братишка bot's turn crashes on round 10 when no legal target remains. Post-M13 it is recorded as a per-game sim error (3/100300 in the sweep); in production the bot fails its action and falls back to an automatic block.
- **Fixed:** 2026-07-03 — wrapped the Justice-min nudge in `if (allTargets.Count > 0)` (`BotsBehavior.cs:2976-2986`); with no targets the ordinary block path handles the turn. The other `allTargets.First()` use is round-<5 guarded (`BotsBehavior.cs:1501`) and the later checks use `.Any()`.

### M17. СуперМудень does not implement its x2 / resist-chain / disable contract
- Exact x2 is off by the untouched base term: fight Skill and base Harm use normal `1 + PokerCount`, but СуперМудень uses `1 + 2×PokerCount` (`CP:1627-1634`, `DoomsdayMachine.cs:1001-1010`). At Кочерга #4 that is ×5 → ×9, only ×1.8 instead of ×10. The resist chain then watches only Strength drops (`DoomsdayMachine.cs:1012-1024`), ignoring Intelligence/Psyche pool breaks, and ordinary Harm immunity interceptors still return before damage (`CharacterClass.cs:198-229`).
- The promised shutdown of other members also leaks: an already seeded virus spreads (`CP:2401-2432`) and pays at game end (`CheckIfReady.cs:366-388`), M.M.'s final multiplier and `IsCalm` immunity remain live (`CheckIfReady.cs:331-363`, `GamePlayerBridgeClass.cs:111-126`), a stale Francie order still excludes a sup candidate (`CP:6313-6328`), and Kimiko's next-round state can still emit a recovery phrase (`CP:6075-6093`).
- **Impact**: the advertised capstone is materially weaker in its own bonuses but simultaneously retains forbidden benefits from three disabled members; the missing Int/Psyche recursion changes the capstone's central late-game damage loop.
- **Fix direction**: double the full normal Skill/Harm result; have every actual Int/Strength/Psyche negative resist effect enqueue another Harm, with the designer's 50-drop-per-turn and zero-score stops; bypass enemy Harm immunities; gate every remaining Francie/Kimiko/M.M. path while SuperМудень is active.
- **Fixed:** 2026-07-10 — fight Skill and base Harm now double the complete `(1+poker)` result (Кочерга #4 ×5→×10 / 5→10); `LowerQualityResist` returns the number of actual Int/Str/Psyche negative effects, and the Super chain queues one Harm per effect recursively (`CP:1627-1637`; `DoomsdayMachine.cs:1001-1057`; `CharacterClass.cs:198-345`). Super Harm bypasses Madara/Boole/Kimiko/Испанец, may keep deducting Drop points at place 6, stops at victim score 0, and shares a 50-Drop counter across the turn (`TheBoys.cs:60-67`; `CP:6319-6322`). Virus spread/end payout, final компромат, calm immunity, stale-order exclusion and Kimiko recovery are all gated; post-Super member level-ups are inert/consumed (`CP:2401-2438,5914-5972`; `CheckIfReady.cs:331-392`; `GamePlayerBridgeClass.cs:111-126`; `GameReactions.cs:977-983`).
- **Reworked:** 2026-07-16 — СуперМудень now also sets every non-Butcher member, ultimate and combination card back to anonymous locked state and clears the active-combination badge. Убийственная Парочка explicitly refuses to dispatch under SuperМудень; the already-fixed Francie/Kimiko/M.M. mechanical gates remain (`TheBoys.LockNonButcherPassives`; `GameReactions.GetLvlUp`; `TheBoys.ApplyKillingCoupleJustice`).

### M18. Active Огурчик Рик can block/skip an incoming fight and lose his guaranteed win
- **Intended:** the player text says the pickle loses the ability to act but defeats everyone who attacks him (`characters.json:864`, Рик Санчез → «Огурчик Рик»); designer report 2026-07-10 confirms he must always accept the fight and win.
- **Actual:** block conversion cleared `IsBlock` only on the activation calculation (`DoomsdayMachine.cs:282-291` before this fix). An already-active pickle could retain a newly applied Block/Skip and hit the no-fight branch. Separately, the pickle's defensive `IsAbleToWin` override ran before attacker hooks (`CP:686-692`; `DM:472-486`), so a later attacker auto-win could set Rick unable to win and cancel the guarantee.
- **Impact:** attacks against the active pickle could be logged/resolved as Block/Skip, or resolve by ordinary fight math instead of a Rick win.
- **Fixed:** 2026-07-10 — active pickle state now strips Block and Skip at calculation start (`DoomsdayMachine.cs:278-296`). After both before-fight dispatchers, the fight loop authoritatively clears them again, disables the attacker win and explicitly enables Rick's win before the Block/Skip gates (`DoomsdayMachine.cs:586-615`).

### M19. Legacy achievements/loot boxes were mostly catalogue-shaped, not a reliable reward system
- **Actual before the fix:** the 69-entry achievement list mixed lifetime counters, generic thresholds, character facts and secrets, while many evaluator fields were never or inconsistently populated by live hooks. It had no unlock economy, no paired RU/EN metadata, no character relationships, and exposed semantic secret IDs. Loot opening used an unsynchronized check/decrement/credit with no pity, opening identity, acknowledgement or reconnect recovery; a sparse dismissible overlay was the entire reward moment.
- **Impact:** displayed requirements could be impossible, accidental or unrelated to the real mechanic; collection progress did not give players meaningful goals or rewards. Concurrent/retried loot calls were not transaction-safe, and a disconnect could lose the reveal even though account state had changed. The UI made both systems feel administrative rather than celebratory.
- **Fixed:** 2026-07-10 — replaced the placeholder list with code-wired goals; the live catalog was expanded again for Gordon Freeman on 2026-07-14 and now contains 106 cards: 12 global mechanics, 40 Normal/Hard character pairs (80 cards), and 14 secret interactions. The two former unknown_bug cards were retired so the character has no achievement metadata or public association; account startup purges their legacy IDs without clawing back issued rewards. `tools/audit-achievements.sh` enforces the exact live set. Multi-step progress is explicitly best-single-match; unlocks pay rarity-based ZBS/boxes exactly once and survive in an acknowledgement queue (`AchievementClass.cs` `AllAchievements`/`SetBestProgress`/`TrackGameEnd`). Locked secrets use hints plus opaque DTO IDs (`GameHub.cs` `MapAchievementEntry`). Loot has published base odds/reward ranges, a 10-box Rare+ guarantee, cryptographic rolls, idempotent stored openings and explicit acknowledgement/reconnect recovery (`QuestClass.cs` loot methods; `GameHub.cs` loot handlers). Economy settlement is account-locked and immediately persisted (`CheckIfReady.cs` game-end settlement; `UserAccounts.cs` `SaveAccount`; `UsersDataStorage.cs` persistence methods). The Vue experience is a persistent reward hub + dedicated achievement center, staged loot reveal and accessible rarity-scaled celebration (`Lobby.vue`; `AchievementBoard.vue`; `AchievementPopup.vue`; `LootBox.vue`). Full spec: [ACHIEVEMENTS.md](ACHIEVEMENTS.md).

### M20. Achievement/loot economy is not one durable transaction boundary
- **Actual:** V2 loot/game-end paths use `lock (account)`, but draft spends and every Discord-store spend/refund mutate `ZbsPoints` outside that monitor (`WebGameService.cs:389-400`; `GameReactions.cs:519-525`; `StoreReactions.cs:203-329`). Storage catches write/replace failures and returns no status (`UsersDataStorage.cs:28-55`), so the hub emits a loot result even when its debit/reward was not persisted (`GameHub.cs:696-724`). Finished-game mapping also enumerates the same live achievement lists that another tab can acknowledge concurrently (`GameStateMapper.cs:174-206`; `GameHub.cs:813-843`).
- **Impact:** simultaneous reward/spend operations can lose ZBS updates; disk failure can show a reward that disappears after restart; a concurrent acknowledgement can abort the final SignalR broadcast with `InvalidOperationException`.
- **Fix direction:** serialize every account-economy mutation on the account monitor, make persistence success observable and keep retryable state on failure, and attach an immutable finish-time achievement snapshot to the match DTO path.
- **Fixed:** 2026-07-11 — paid web and Discord draft picks plus all six store transactions now validate/mutate under the shared account monitor, persist before confirmation, and restore their debit/chance snapshot on failure (`WebGameService.cs:371-466`; `GameReactions.cs:512-610`; `StoreReactions.cs:196-515`). `SaveAccount`/storage report atomic-replace success (`UserAccounts.cs:113-139`; `UsersDataStorage.cs:28-80`). V2 loot open and both acknowledgement families snapshot, save-before-send and rollback with a retryable hub error; legacy cached clients use one-shot compatibility, while recovered reveals receive current totals (`GameHub.cs` loot/acknowledgement handlers). Final DTO mapping reads a detached achievement snapshot (`CheckIfReady.cs` game-end reward settlement; `AchievementClass.cs` `CreateSnapshot`); a failed game-end save is visible in critical logs and its settled memory remains for the periodic retry.

### M21. Reward-modal and reconnect failure paths can hide or deadlock celebrations
- **Actual:** the global achievement popup and lobby loot dialog could mount together with independent background isolation and focus traps (hosts `App.vue:13-15,202-206`; `Lobby.vue:109-113,369-381`; isolation `AchievementPopup.vue:41-68`; `LootBox.vue:68-95`). A failed first SignalR `start()` left a non-null dead connection that blocked later retries (`signalr.ts:1055-1062,1170-1173`); reconnect reauthenticated but did not refresh quests/loot. Loot opening had no concurrent timeout/cancel/error state, and achievement dismissal cleared locally before acknowledgement succeeded (`Lobby.vue:51-68`; `LootBox.vue:167-181`; `game.ts:667-676`). The mandatory five-second game-over gate also ignored reduced-motion preference (`Game.vue:616-620,1481-1485`).
- **Impact:** the visible reward modal can become inert, a transient network failure can require a page reload, recovered inventory/reveals can stay stale, and a hung/failing acknowledgement can strand or prematurely erase the most important reward moment.
- **Fix direction:** centralize modal priority, reset failed connections and rehydrate both reward states after auth/reconnect, give opening/acknowledgement explicit retry UX, and honor reduced motion in the podium gate.
- **Fixed:** 2026-07-11 — the store now owns loot-modal priority and Lobby uses render-tick handoffs, so the two focus traps never coexist (`App.vue:13-17`; `Lobby.vue:43-146`). Failed/stale SignalR connections are disposed, login is visibly retryable, authenticated reward calls require session readiness, and every auth/re-auth hydrates quests plus achievements (`signalr.ts:1053-1262`; `game.ts:302-324`; `LoginProcess.vue:1-87`). Loot opening has a bounded idempotent retry/return state; achievement acknowledgement keeps the popup visible with busy/error retry until success (`game.ts:666-704,742-774`; `LootBox.vue:129-201,267-387`; `AchievementPopup.vue:95-166,195-301`). Reduced-motion users bypass the podium delay and animation (`Game.vue:609-632,3122-3132`).

### M22. Daily Quests were random-roster hostile, misleading and structurally repetitive
- **Actual:** the seven-goal pool could roll nested finish-1 + finish-3 + finish-5 goals, used process-dependent `string.GetHashCode()`, included “play 3 different characters” despite random/repeating rolls, and mixed one-game and five-game effort at the same advertised 25 ZBS. Worse, a completed card paid nothing unless all three finished: the UI promised each reward while partial completions expired unpaid. The lobby surface was a hardcoded-English list with no reset contract, receipts, agency, loading/error state or accessible progress semantics (pre-V2 implementation replaced by `QuestClass.cs:200-260,638-678`; UI replacement `Lobby.vue:365-375`).
- **Impact:** the board could be monotonous or effectively blocked by random character assignment; players were given false reward feedback and a missed/unlucky goal destroyed both the day and the old 500-ZBS seven-day cliff.
- **Fixed:** 2026-07-11 — Daily Quest V2 now assigns a fixed Anchor plus personalized SHA-256-stable Skirmish/Ambition lanes from 12 bilingual, character-neutral contracts (`QuestClass.cs:200-260,510-540`). Cards auto-pay 20/30/30 exactly once; any 2/3 pays +20 and advances streak/week, 3/3 adds one loot box, and any 5/7 completed days pays +100 (`QuestClass.cs:638-723`). One unfinished random card can be rerolled from persisted all-day metrics with save-before-send rollback (`QuestClass.cs:414-508`; `GameHub.cs:685-725`). Non-Madara Цукуеми views count only privacy-safe participation (`QuestClass.cs:380-408`). The inline Daily Quest board adds paired copy, reset countdown, receipts, free-swap announcement/focus recovery, clearly labelled weekly stamps, controlled icons, ARIA progress/loading/error states, mobile and reduced-motion support without adding a competing modal; in-flight refreshes queue one trailing request so UTC rollover is not dropped (`Lobby.vue:8,365-375`; `DailyQuestBoard.vue:1-604,1490-1621`; `game.ts:244-255,685-717`). `tools/audit-quests.sh` enforces the catalog and character neutrality. Full spec: [DAILY-QUESTS.md](DAILY-QUESTS.md).

### M23. Public replay links are hidden behind an unrelated account login
- **Intended:** replay files are shareable and the single-replay REST action is anonymous (`GameController.cs:179-186`); `Replay.vue` needs only that public payload through `replay.ts:164-186`.
- **Actual before the fix:** the app-wide login branch rendered for every unauthenticated route, including the `/replay/:gameId` route (`router.ts:42-47`; `App.vue:180-188`), so a recipient had to create a throwaway web account before the already-public replay UI could mount.
- **Impact:** shared replay URLs could not be watched anonymously, adding account friction to replay review and player support even though no protected data or authenticated operation was involved.
- **Fixed:** 2026-07-11 — `App.vue` now recognizes the named replay route as public and suppresses only the login overlay there (`App.vue:11-18,180-188`). All other routes retain the existing authentication screen; replay loading continues to use the anonymous endpoint unchanged.

### M25. Монстр's no-escape mark was tied to a resolved fight and could not represent two overlapping turns
- **Intended:** every declared Monster attack marks its target even if Block/Skip prevents the fight; that target must attack on its next two turns, and different victims can have overlapping windows.
- **Actual before the fix:** the mark was a single bool assigned from `HandleAttackAfterFight`, after both no-fight exits, and cleared wholesale by Monster's next-round case. A blocked/skipped target was never marked and the state could not encode an independent two-turn expiry.
- **Fixed:** 2026-07-11 — marking moved to the attack-before-fight dispatcher, ahead of the Block/Skip gates, and stores an absolute per-victim `MonsterNoEscapeUntilRound = max(old, current+2)` (`CP:1066-1073`; `PassivesClass.cs:280-285`). The readiness pass enforces the attack-only state through the expiry while preserving the round-10 Тигр ban carve-out (`CheckIfReady.cs:1435-1458`).

### M26. Близнец drained and summed every block attacker's Justice and also received generic block Justice
- **Intended:** Monster gets no ordinary +1 Justice from blocks. He copies, without draining, the highest Justice among enemies attacking that block and receives bonus points equal to that maximum.
- **Actual before the fix:** every blocked attacker was zeroed and their full Justice was added to Monster immediately; several attackers therefore summed, and the generic block path also buffered +1 Justice.
- **Fixed:** 2026-07-11 — the block path suppresses generic Justice for a Близнец holder. A per-round maximum now sets Monster's live Justice without touching the attacker and awards only the incremental difference, so total bonus equals the maximum rather than the sum (current implementation `DoomsdayMachine.cs:604,658-716`; reset `CP:6186-6188`; state `PassivesClass.cs:283`). M44 later moved the copy itself into that authoritative branch so successful blocks actually trigger it and bypassed blocks cannot.

### M27. Round-8 bot games could wait out the turn gate before challenging Мадара
- **Intended:** strict bots immediately accept the Клоны Сусано challenge on round 8; a game where Madara is the only human must not sit on the ordinary readiness delay.
- **Actual before the fix:** bot actions were chosen only after the human readiness/timer gate opened. Madara was unable to act, but bot attacks did not exist yet, so bot-heavy games could appear paused until timeout processing.
- **Fixed:** 2026-07-11 — before readiness counting, every live, non-skipping strict bot was idempotently committed to one ordinary attack on Madara. Bot dispatch preserved that forced choice, and round-8 Madara was exempt from the ordinary readiness floor. Genuine forced skips remained authoritative. This historical implementation is superseded below.
- **Live-human follow-up fixed:** 2026-07-11 — after round 7 entered round 8, the post-calculation human prediction loop overwrote Madara's earlier `ConfirmedPredict = true` with `false`; all-bot simulations skipped that loop, while human Madara had no prediction control that could restore readiness (`CheckIfReady.cs:1542-1567`). The loop now preserves/reasserts Madara's full unable-to-act state, and the readiness precommit reasserts the same invariant every tick before counting (`Madara.cs:197-230`). Web Block/Auto Move/Change Mind/Skip are rejected server-side during the locked round, `isMyTurn` stays false, and the misleading Change button is hidden (`WebGameService.cs:505-616,1151-1163`; `game.ts:112-116`; `Game.vue:1220-1237`).
- **Superseded by designer rework, 2026-07-12:** bots must no longer auto-attack Madara or advance immediately. A live round 8 now waits 30 seconds; after spending level-ups, L0/L1 receive the scripted exact Madara prediction, while L2/L3 receive no hidden row and use fair player-visible prediction/inference. Every bot then chooses its ordinary action through normal AI (`Madara.ForceRoundEightBotPrediction`; `BotsBehavior.HandleBotBehavior`; `CharacterPassives.HandleFairBotPredict`; `CheckIfReady.TickAsync`). The Madara action lock and web gates from the follow-up remain.
- **Designer exception fixed, 2026-07-13:** strict-bot Наруто, Sakura and Итачи now always lock the exact Madara prediction and submit their ordinary attack against him after the reaction delay, pending level-ups and any genuine forced Skip. This exception applies at every AI level; the existing Клоны Сусано injection then appends its separate prediction fight, so each eligible challenger attacks twice (`Madara.MustAcceptRoundEightBotChallenge`; `BotsBehavior.HandleBotBehavior`; `CheckIfReady.TickAsync`). Other L2/L3 bots keep the fair inference/ordinary-action rule above.

### M28. Rumbling counted Eren's losses from the entire match instead of round 10
- **Intended:** the fewer-than-two-loss gate considers only resolved losses during round 10.
- **Actual before the fix:** every resolved loss from rounds 1–10 incremented the same `Eren.State.Losses` counter, so early-game losses could permanently disable Rumbling before its round existed.
- **Fixed:** 2026-07-11 — the loss hook now requires `game.RoundNo == 10`; the existing post-fight gate therefore reads only round-10 losses (`CP:2662-2667,3672-3680`). The owner DTO shape is unchanged; its loss field now has the intended round-10 meaning.

### M29. Шоковый щит's forced skip could be replaced by bot automation and still waited for confirmation
- **Intended:** the first attacker stopped by the one-use shield automatically skips their next turn.
- **Actual before the fix:** the next-round hook set `IsSkip` but left it unconfirmed; bot behavior did not honor attacker-side `IsSkip` and could immediately choose an attack, while a human could remain in the readiness wait.
- **Fixed:** 2026-07-11 — the shield clears queued attacks and auto-submits the skip (`IsReady`, `ConfirmedSkip`, `ConfirmedPredict`) when it lands (`CP:5280-5293`).
- **Bot follow-up:** bot behavior treats an existing forced skip as a complete action through `BotsBehavior.CompleteForcedSkip` and returns without selecting an attack.

### M30. DooM Rune penalties consumed base and externally earned stats without a floor
- **Intended:** Вознесение may take back only its granted +8 Int, and Маневры only its granted +5 Speed.
- **Actual before the fix:** every later loss/Harm applied another −1 forever, even after all Rune-granted points had already been removed.
- **Fixed:** 2026-07-11 — module activation seeds remaining-grant counters (`DoomGuy.cs:145-153,281-285`); loss/Harm penalties decrement and apply only while the matching counter is positive (`CP:2635-2645`; `CharacterClass.cs:213-221`). Base and later externally earned stats are no longer consumed by the Rune clawback.

### M31. BFG destroyed Step-3 randomness only for the primary attack
- **Intended:** every fight in the BFG shockwave that reaches the random stage must destroy that random stage exactly like the primary attack.
- **Actual before the fix:** the primary spent the charge and forced `pointsWined = 1`, but secondary direction-marked wave fights called ordinary `CalculateStep3`; a random loss could stop a branch.
- **Fixed:** 2026-07-11 — direction-marked BFG fights now use the same Step-3 override without trying to spend the already-consumed charge (`DoomsdayMachine.cs:832-865`). Decisive pre-random losses, blocks and skips still stop a branch, while random-stage wave fights win and continue through the existing queue logic (`:809-827`).

### M32. Bot Darksci can attack after round-9 Дизмораль sets his Psyche to 0 and skips him
- **Intended:** spending Darksci's mandatory round-9 level-up applies −5 Psyche; if that leaves him at 0 Psyche, «Да всё нахуй эту игру» clears his action and the turn remains skipped.
- **Actual before the fix:** bot behavior checked `IsSkip` only on entry, then spent the pending level-up. Spending subsequently set `IsSkip` and cleared the target list, but processing continued into ordinary attack selection and queued a new target. The fight loop deliberately processes a skipped player with a non-empty queue for legitimate forced fights (`DoomsdayMachine.cs:464-489`), so the illegal bot action resolved. Against Геральт, the contract injector rejected the skipped attacker and added no repeats (`DoomsdayMachine.cs:374-398`), producing the reported signature of exactly one real fight.
- **Impact:** bot and auto-moved Darksci can act on the round where 0 Psyche is supposed to remove their action; the same missing post-level-up gate can turn any future level-up-triggered forced skip into an ordinary bot attack.
- **Fix direction:** re-run the existing bot forced-skip completion immediately after pending level-ups, before any character sub-action or attack selection. Keep the fight-loop forced-action behavior unchanged.
- **Fixed:** 2026-07-11 — pending level-ups are spent before the action gate (`BotsBehavior.cs:91-107`). Skip finalization then runs through `CompleteForcedSkip` (`BotsBehavior.cs:4831-4840`), so a Дизмораль-triggered Skip returns before attack selection; later readiness-stage forced-fight injection remains unchanged.

### M33. Fight win/lose audio and the R3 random bar could drift away from the visual timeline
- **Expected:** each round result starts on its reveal; R3 shows modifiers, visibly rolls to its outcome, announces that settled result, and only then reveals the whole-fight result.
- **Actual before the fix:** every `playClipsBatched` awaited the slowest primary/percussion/vocal/meme fetch before starting any source, with no lateness bound (`sound.ts:286-313`). A cold optional layer therefore delayed an already-cached win/lose clip and could start it after later visual steps. The 4.0.6 fight-layout extraction also collapsed R3 modifiers and roll into one 800 ms step, ran the needle for only 500–700 ms and announced the R3 result when the roll began rather than when it settled (`FightAnimation.vue`, pre-fix step/sound/needle blocks).
- **Impact:** the issue appears intermittent after several fights because each result sequence and random decorative layer has its own cache entry; on a cold combination, several voices can arrive late/out of order. R3 itself ends too quickly and pushes final visuals ahead of its audio.
- **Fixed (superseded timing):** 2026-07-11 — timing-critical result/draw/random clips now preload and batched playback gives the primary a 180 ms maximum start window (`sound.ts:287-374`). The first implementation admitted only already-cached optional layers; M36 repairs that regression without relaxing the bound. This change-set also restored a settlement-owned R3 timeline and made the between-fight delay cancelable; the exact current timing is recorded by the follow-up below.
- **Timing follow-up fixed:** 2026-07-12 — all R1/R2/optional R3/final visual reveals now use the speed-scaled `[0,850,850,850]` ms grid. R3 begins with R2 and is authoritatively snapped at 850 ms; its result audio normally lands with that snap, except `3_lww` starts at 750 ms so Final follows it after 950 ms without moving either visual beat (`FightAnimation.vue:103-110,692-823,956-985,1111-1234`). Replay navigation clears both scheduled R3 callbacks, and settlement cancels the animation-frame loop before snapping so a late frame cannot move the needle again (`FightAnimation.vue:538-544,609-679,807-817`).

### M34. Bot fast paths could commit attacks or forced skips before spending level-up points
- **Intended:** every strict bot spends all pending level-up points before choosing or confirming any attack, defense or skip; this also applies to the early round-8 Клоны Сусано challenge.
- **Actual before the fix:** ordinary attack selection followed level-up, but `CompleteForcedSkip` and the Madara round-8 branch returned first. The M27 readiness precommit also assigned the Madara attack before bot behavior ran at all.
- **Impact:** forced-skipping bots and every bot challenging Madara on round 8 could carry unspent points into a committed action, making their fight snapshot weaker and violating the same spend-before-action rule enforced for humans.
- **Fixed:** 2026-07-11 — bot playstyle selection and the complete level-up loop now run before every action fast path in `BotsBehavior.HandleBotBehavior`. The then-current readiness tick also prepared every live strict bot before the idempotent Madara challenge precommit, which was later removed by the M27 designer rework.
- **Designer rework follow-up, 2026-07-12:** the Madara readiness precommit was removed with M27's forced attack. The invariant remains: bot behavior spends all pending points before the L0/L1 scripted round-8 prediction, any fair L2/L3 prediction, a forced Skip, or the bot's ordinary action (`BotsBehavior.HandleBotBehavior`).

### M35. Character-specific leaderboard icons reveal masked opponents
- **Expected:** before the game finishes, character identity remains a guessing mechanic. Character-specific leaderboard prefixes and widget state must be visible only to the character owner (or an admin), while spectators and opponents receive no annotation that identifies a masked character. Тигр's round-10 `🚫` is the explicit exception: it represents a public system ban and remains visible to everyone as part of the joke.
- **Actual before the fix:** the shared Discord/web prefix builder exposed Goblin mines, Ziggurats and protection, DooM Guy demon nests, sealed Мадара and active Salldorum Шэн to every player (`GameUpdateMess.cs:219-270`). Recipient-side `🤝`/`⚔️` annotations also identified Napoleon to his ally and Support to the marked Carry (`GameUpdateMess.cs:730-748`). Because the web custom-leaderboard prefix/text reuse those methods, the same leak reached live games and player-perspective replays; only spectators escaped it because they receive no populated custom board.
- **Impact:** a single special icon could solve an opponent's character without a reveal mechanic, undermining predictions and the core hidden-roster game.
- **Fixed:** 2026-07-12 — character-derived board objects are now owner/admin-scoped except for Тигр's intentionally public system-ban `🚫`. Goblin and DooM owners retain their target/position markers, self-only seal/Шэн status remains available, and Napoleon/Support retain their complete owner-side alliance views; opponents and recipients no longer receive the other identifying icons. The owner-only `PassiveAbilityStates` mapper and all other per-character annotations were audited and already satisfied this boundary (`GameUpdateMess.cs:219-811`; `GameStateMapper.cs:367-830`).
- **Rework follow-up:** 2026-07-12 — the obsolete active-Шэн icon was replaced by the fixed-cell cola 🥤, still projected only to Salldorum/admin through `CustomLeaderBoardBeforeNumber`; the owner widget carries charges/drink history without exposing either to opponents.

### M36. Preloaded fight-result audio silently excluded every cold randomized layer
- **Expected:** eligible folk percussion, lose memes and Геральт vocals play on the same Web-Audio frame as their corresponding win/lose clip. Random selection and the percussion no-repeat pool must not make a selected layer inaudible.
- **Actual before the fix:** the M33 bounded batch started as soon as its preloaded primary was available, then included an optional layer only if its randomized URL was already in `audioBufferCache` (`sound.ts:297-334`). The selected URL's fetch had only just started, so it was almost always omitted; folk percussion then removed that unheard URL from its per-fight pool, repeating the failure for each new selection.
- **Impact:** after the fight-animation timing repair, the primary sounds remained correctly synchronized but percussion, lose-meme and Геральт vocal layers appeared to stop working.
- **Fixed:** 2026-07-12 — a batch now gives cold selected layers the unused portion of the same 180 ms primary deadline and starts every buffer that reaches that single cutoff on one AudioContext frame (`sound.ts:297-345`). The optional wait cannot extend or reorder primary playback, so M33's anti-drift bound remains intact.

### m29. Three V2 achievement descriptions do not match their evaluators
- `x_spartan_mylorik` RU copy said the **next** fight, while the tracker intentionally accepts any later fight. `c_darksci_unstable` requires finishing alive at actual place 1 but omitted “alive” in both languages. `c_kratos_olympus` said five “enemies,” although team mode counts every other player, including teammates (`AchievementClass.cs` `AllAchievements`/`TrackGameEnd`; `DoomsdayMachine.cs:1567-1582`; `CharacterPassives.cs:1864-1882`).
- **Impact:** the achievement center can tell players a stricter, looser, or team-inaccurate requirement than the code actually evaluates.
- **Fix direction:** change presentation metadata only: “later fight,” “finish alive,” and “all five other players”; do not change the mechanics or canonical passive strings.
- **Fixed:** 2026-07-11 — aligned the paired catalog copy with the existing evaluators: “later fight,” “finish alive in 1st,” and “all 5 other players” (`AchievementClass.cs` `AllAchievements`). No gameplay condition or canonical passive identifier changed.

### m30. Localized logs and dynamic UI labels fell through to their source language
- **Expected:** a viewer's RU/EN choice applies to every presentation string without translating canonical character/passive/action identifiers in game state.
- **Actual before the fix:** `GameLocalization.Text` tried `exact` only against the entire input and returned immediately after adapting any `|>Phrase<|` entry. Since personal/global logs are multi-line blocks, the presence of one character phrase prevented the remaining system/stat lines from reaching normal rules, while exact translations for individual lines could never match the whole block. The Vue translator had the same whole-node limitation in both directions, so interpolated English widget labels stayed English in RU mode. Finished chronicles and Eternal Tsukuyomi projections also overwrote/bypassed the localized log projection, and four Cyrillic passive titles had descriptions but no English display-name mapping.
- **Impact:** English players regularly saw Russian passive titles and personal/global/chronicle system text; Russian players saw English widget/status/navigation labels. Gameplay dispatch remained correct because the untranslated values were presentation-only.
- **Fixed:** 2026-07-11 — both localization engines now run dynamic templates and longest-first exact fragments across composite strings, with character-phrase adaptation continuing into the rest of the log (`GameLocalization.cs:89-142`; `Web/VueClient/src/i18n.ts:47-61,155-196`). The finished chronicle and Eternal Tsukuyomi projection now pass through the viewer locale (`GameStateMapper.cs:168-175,932-960`). The shared catalog fills the four passive-title holes and the reported RU widget/UI labels; `tools/audit-localization.sh` now requires display mappings for every Cyrillic character/passive identifier and scans `terms` values for Cyrillic leaks. Canonical Russian character phrases and all dispatch strings are unchanged.

### m31. English replay viewers still saw Russian phrase bodies, system logs and class values
- **Expected:** changing a public replay to English must localize the selected player's saved log perspective and every presentation-only class label without changing canonical replay/action identifiers.
- **Actual before the fix:** replay snapshots store each player's mapped private perspective (`ReplayService.cs:301-339`), so an RU-origin snapshot can later be opened by an English viewer. The Vue translator understood ordinary exact/term fragments but not the `|>Phrase<|Passive: quip` protocol; it translated the passive title and left the Russian quip, producing mixed lines such as `Party of One: Что делаешь?`. Its dynamic rules also omitted the timeout, `#life`, Eternal Tsukuyomi winner and score-source forms, while `PlayerCard` split `Сильный`/`Быстрый` into standalone nodes with no English entries. `SkillsPanel` converted the complete canonical passive description to marked-up HTML before localization, splitting an otherwise-catalogued translation into unmatchable fragments (`PlayerCard.vue:769-842`; `SkillsPanel.vue:125-142`). Reproduced in replay `39567a79`, round 11, player 0.
- **Impact:** an English replay could still require Russian to understand personal logs and the class card even after the broader m30 localization pass. Live English `#life` logs had the same missing dynamic template.
- **Fixed:** 2026-07-11 — Vue now decodes canonical and older partially translated phrase records before ordinary fragment replacement, using the same passive-aware fallback catalog as the backend (`Web/VueClient/src/i18n.ts:146-196`; `GameLocalization.cs:128-142`; catalog `localization.en.json:939-1067`). Added every reported status/score/winner/class fragment, and localize complete passive descriptions before markdown rendering (`SkillsPanel.vue:125-142`; catalog `localization.en.json:44-64`). Character/passive source and English descriptions are paired bidirectionally at load time, without changing any Russian `characters.json` field (`Web/VueClient/src/i18n.ts:16-31`). Focused fixtures cover the reported strings, old mixed phrase records, class values and RU↔EN passive descriptions (`Web/VueClient/src/i18n.spec.ts:6-51`). The audit now requires display/fallback coverage for all 132 `PhraseClass` identifiers and rejects Cyrillic in every English catalog value (`tools/audit-localization.sh:13-78`).
- **Completed phrase rework:** 2026-07-11 — the passive-wide fallback is now legacy-only. `phrases.en.json` supplies a context-aware English counterpart for every one of the 796 runtime Russian variants across all 235 fields. `PhraseClass` selects and consumes aligned indices and stores both rendered variants in replay-safe payloads; media DTOs likewise retain both languages. Vue, Discord and story boundaries resolve the same record without altering Russian text (`PhraseLocalization.cs`; `CharactersPhrases.cs:1713-1857`; `i18n.ts:151-208`). Remaining hardcoded live phrase markers for Madara, Rick and Eren were migrated too. Startup plus `audit-phrases.sh` enforce complete parity; focused fixtures cover AWDKA's Wukong/Ignite line, Mylorik's Vietnamese Tristana line, RU switching and direct messages.
- **Follow-up fixed:** 2026-07-11 — completed the remaining mixed replay/UI vocabulary and dynamic fragments: `Последний шанс!`, mixed-script `Cкилла`, bold-split `обычных`/`бонусных`, all seven Justice encouragements (including emoji spacing), every AWDKA troll-result variant, and the dynamic last-second point line. Both canonical and already-partially-translated replay forms are covered bidirectionally in the shared catalog; regression fixtures reproduce all seven reported leaks (`localization.en.json`; `i18n.spec.ts`). Russian producers remain unchanged.
- **Full legacy-pair follow-up:** 2026-07-11 — the English-only phrase arrays were replaced with validated `{ russian, english }` pairs plus paired passive titles for all 235 fields / 796 variants. Backend and Vue now resolve old marker records by passive context and exact body before fallback, including duplicate passive titles, ASCII-only memes and multi-line phrases. A regression loop exercises every catalogued legacy form; targeted fixtures cover the newly reported Saitama one-punch/training/quips, Rick's full name, target-class rewards, Weedwick drop, mixed attack text, Geralt Meditation/Witcher senses, and doubled Contract score source (`PhraseLocalization.cs:19-198`; `i18n.ts`; `i18n.spec.ts`). Geralt's 31 English static hints moved from a duplicate C# dictionary into the shared catalog. Russian source text and dispatch identifiers are unchanged.
- **`afffa305` follow-up fixed:** 2026-07-11 — the Chronicle had converted Discord markdown to HTML before localization, splitting templates around bold dynamic player names. It now translates the complete source first. Mirrored backend/client rules finish canonical and already-mixed Auto Move, Darksci tilt, and all three dynamic nemesis defeat connectors without constraining or changing the username. Added the missing Character Highlights heading, uppercase Carapace widget title and HardKitty finale adaptation; replay-specific fixtures cover every reported form plus a punctuation-heavy synthetic name (`FightAnimation.vue:1024-1039`; `GameLocalization.cs:34-49`; `i18n.ts:104-119`; `i18n.spec.ts:69-92`). The replay's canonical Russian records and all Russian producers remain unchanged.
- **UI/character-log follow-up fixed:** 2026-07-11 — added the two missing empty-state labels and adapted the deterministic All Talk, Armin, Young Schoolkid and Dragon lines in the backend-owned exact catalog. Mirrored dynamic rules complete canonical or mixed Block actions, Rumbling places and Mitsuki point totals; the Shock Shield suffix covers both `ход` and a previously projected `turn` (`GameLocalization.cs:34-55`; `i18n.ts:104-125`; `i18n.spec.ts:101-123`). No canonical Russian log producer, character description or passive identifier changed.

### m32. AI-generated Witcher hints and malformed Stories could be permanently single-language
- **Expected:** every generated artifact stored in live state or a replay must retain both adapted languages so the viewer can switch RU/EN later.
- **Actual before the fix:** `GenerateWitcherHintAsync` asked Haiku for only Geralt's current account language, then wrote the arbitrary response as a plain personal-log string (`ClaudeHaikuService.cs`, pre-fix method; `CharacterPassives.cs`, pre-fix Медитация branch). No catalog can translate that unknown prose later. `GameStoryService` requested both tagged stories, but if either tag was absent it formatted and published the raw response, allowing the same permanent language leak into the Story tab and replay file.
- **Impact:** changing language after a successful AI hint—or opening its replay under another locale—showed the generation language; malformed Story responses could do the same for every viewer.
- **Fixed:** 2026-07-11 — Witcher hints now request matching `<ru>`/`<en>` adaptations once, validate/normalize both, fall back as a pair, and store the result in a self-contained PhraseV2 payload (`ClaudeHaikuService.cs:34-108`; `CP:4791-4826`). At the time, Story used the shared tagged parser, retried malformed output once, and rejected a second invalid response rather than publishing raw single-language HTML (`BilingualGeneratedText.cs:6-32`; pre-m35 `GameStoryService.GenerateStoryAsync`). The localization audit forbids the old hint generator, and the client fixture switches arbitrary generated hint prose in both directions.
- **Story follow-up:** the tagged one-call Story transport was superseded by m35 after production responses repeatedly omitted one or both wrapper tags. Witcher hints still use the compact paired-response parser.

### m33. Madara web audio did not enforce its CDN and private-ending boundaries
- **Expected:** the round-8 `madara_tsukuemi_theme.mp3` streams from `https://r2.ozvmusic.com/kotgh/sound/character_passives/madara/madara_tsukuemi_theme.mp3`. Under Вечное Цукуеми, each fooled enemy locally hears only their own character's victory theme, when that character has one.
- **Actual before the fix:** ordinary sound helpers used the R2 base, but `MediaMessages` passed the server's relative `/sound/...` URL directly to `Audio`, leaving Madara's theme dependent on the app's static-file route. End-game themes used the generic place-1 scan and had no explicit branch for the private fake-victory source.
- **Fixed:** 2026-07-11 — the shared resolver now normalizes relative media sound paths to the R2 CDN and `MediaMessages` uses the resolved URL for autoplay, replay and expiry bookkeeping (`sound.ts:170-180`; `MediaMessages.vue:90-130`). The finish watcher recognizes the viewer-owned Вечное Цукуеми score entry and dispatches only that viewer's character theme through the supported-theme map (`Game.vue:233-274`; `sound.ts:974-983`).

### m34. AI stories expose the hidden terminal boundary as a playable round
- **Expected:** a normal story covers combat rounds 1–10; a Kratos resurrection story may continue through every actually captured combat round. `HandleLastRound` additions are an epilogue, not another fight round, and round-11 setup noise must stay excluded just as it is in replay v2.
- **Actual before the fix:** `GameStoryService.CaptureSnapshot` copied the terminal `game.RoundNo` (11 in a normal game) into `RoundCount`, and `BuildPrompt` emitted every integer through that value as `<round>`. The prompt therefore advertised a fake round 11 and could attach the hidden setup buffer to it; extended games inherited the same terminal-counter off-by-one. Story input also ignored replay v2's same-round `FightLog`, leaving the model to infer character interactions from truncated prose logs (pre-fix `GameStoryService.CaptureSnapshot` / `BuildPrompt`; replay contract `ReplayService.cs:35-164`).
- **Impact:** the generated Story tab could describe a non-playable round and was biased toward repetitive, single-character chronological summaries even though the replay beside it showed the correct playable range.
- **Fix direction:** derive story rounds from `game.ReplayRounds`, pass structured attacker/defender outcomes, isolate explicit `FinalSettlement*` suffixes as an epilogue, and vary the narrative framing deterministically per game without changing recorded facts.
- **Fixed:** 2026-07-12 — story snapshots now enumerate `game.ReplayRounds`, so normal games expose 1–10 while Kratos automatically includes every captured extension; the replay's explicit settlement suffixes are emitted under `final-settlement`, never as another round (`GameStoryService.cs:168-314`; replay isolation `ReplayService.cs:106-144`). Each round now carries structured attacker/defender/outcome facts plus an aggregate rivalry summary. A game-ID-seeded director card independently selects one of six frames, structures and chaos devices (216 combinations), while the rewritten prompt prioritizes multi-character action→reaction chains and permits invented presentation but not invented gameplay (`GameStoryService.cs:335-563`).

### m35. Tagged bilingual Story responses could reject the entire generated feature
- **Expected:** a valid story in either requested language should reach the Story tab and replay even if the other language is disabled for testing or its API request fails.
- **Actual before the fix:** `GameStoryService` asked one request to return both `<ru>` and `<en>` blocks. Production responses omitted the required wrapper structure twice (`Invalid bilingual output` followed by `Rejected non-bilingual output`), so the parser discarded otherwise usable prose and no Story event or replay backfill occurred (pre-fix `GameStoryService.GenerateStoryAsync`; production report 2026-07-12 16:13).
- **Impact:** Story mode could disappear completely after a successful Anthropic generation, and testing the Russian path always spent tokens on English.
- **Fixed:** 2026-07-12 — Russian and English now use independent, language-specific requests run concurrently; each accepts plain non-empty prose and retries only its own empty result (`GameStoryService.cs:65-141,335-445`). Failure in one language no longer discards the other, and the combined replay-safe HTML supplies a localized unavailable message for a missing side (`GameStoryService.cs:77-95,637-647`). `GameStoryEnglishEnabled` defaults off, skipping the English request unless it is explicitly enabled in `DataBase/config.json` (`Config.cs:27-29`; `GameStoryService.cs:37-42,65-71`).

### m36. Death Note could kill an already-dead round-10 settlement victim twice
- **Actual before the fix:** Rumbling and then Naruto's Теневые settlement run before the ordinary round-10 `HandleEndOfRound` dispatcher. If Kira's queued note targeted one of those newly dead players, the Death Note branch set `IsDead`/`DeathSource` again, paid Монстр another death point and counted a kill even though the target had already died.
- **Fixed:** 2026-07-12 — the Death Note resolves an already-dead target as an invalid duplicate: it writes a private explanation, clears the queued target/name and exits before the kill, Kira reward and Монстр payout (`CharacterPassives.cs` Death Note branch). This applies to every earlier death source, including Rumbling and Теневые.

### m26. Legacy L1 attack-scoring flags are never reset inside the per-target loop
- The boolean flags declared once per legacy attack-scoring invocation (`BotsBehavior.cs:1061-1069`) remain set and are not cleared between targets. Per-character compensations later read them (mylorik/Спартанец path `:1601-1670`, Глеб path `:1816-1855`) and can therefore compensate a different target. M45 routes L2/L3 around this scorer.
- **Impact**: mild L1 bot mis-weighting in mixed line-ups; not player-visible and not exception-producing.
- **Status:** observation, **not fixed** — the AI-difficulty change-set is L1-preserving by contract, so touching this shared scoring pass was out of scope. Fixing it (reset the flags at the top of each iteration) would change L1 bot behavior and should be a standalone change with its own sim comparison.

## Phase 6 — full cross-mechanic re-audit (2026-07-12)

> **Scope:** end-game resolution order (`HandleLastRound`), round-10/11 specials × the Кратос extension, interaction-matrix re-verification with missing-cell enumeration, mechanical invariant greps (raw psyche/justice, ForOneFight placement, DeepCopy coverage, `RoundNo` hardcodes), all characters incl. the mid-audit Наруто/DooM additions, plus simulation batches (baseline 106×2 + 8 special-character lineups ×30 + Наруто lineup ×30 — **586 games, 0 exceptions, 0 freezes**). Premade/team-mode verified by code reading only (not sim-testable). Anchors verified against the post-`e5b537f` tree.

### M37. A dead top-scorer keeps place 1 and takes the win statistics from the announced winner
- **Actual:** the final sort is purely by score with dead players included (`CheckIfReady.cs:414-418`). The *announced* winner is the first **alive** player (`:554-556`, `:629-633`), but the payout loop derives `rewardPlace` from the raw board place (`:662`), so a dead player at place 1 receives `TotalWins++`, character `Wins++` and place-1 performance stats (`:730`, `:769-778`) while the announced alive winner (place 2+) gets no win credit. (ZBS/lootbox/mastery dead-gates are correct; quest `isMatchWinner` follows the announced winner — so quest-wins and account-wins disagree too.)
- **Reachability:** any kill source that leaves score intact — Кира killing the round-8-10 leader, Пейзаж, Rumbling, Кратос event. Sim evidence: **48 of 346 games** finished with a dead player at place 1 (e.g. dead HardKitty at place 1 with 146–153 points in the Мадара lineup; 3 occurrences even in the 106-game baseline).
- **Impact:** player-visible: the profile records a win for a game the announcement said someone else won.
- **Fix direction:** designer decision — either sort dead players below all alive players at game end, or make `rewardPlace`/win-stats follow the announced winner. (Наруто's dispersed clones already do the former via `MoveDispersedClonesToBottom` — the pattern exists.)
- **Fixed:** 2026-07-13 (designer choice A) — central `Naruto.OrderLeaderboard` now orders every living seat before every dead seat; the final payout therefore follows the announced living board leader. `GameClass.WinnerPlayerIds` additionally records the settled solo/team/Sakura winner for consumers that must not infer victory from place.

### M38. A bot Кратос never fights his own resurrection event
- **Actual:** `HandleBotBehavior` hard-blocks every bot after round 10: `botChoice = -10` is a literal Block (`BotsBehavior.cs:109-113`; `GameReactions.cs:695-706`).
- This catches **Кратос himself**, so the legacy event-aware logic `BotsBehavior.CanBreakKnownDefense` (`BotsBehavior.cs:254-270`) is unreachable during the event.
- With everyone else force-blocked (`CheckIfReady.cs:1170-1178`) and the only kill path being Кратос winning **as attacker** (`CP:1853-1867`), a bot-Кратос event produces zero kills.
- **Evidence:** the bot even has a `Kratos:Ragnarok` playstyle that engineers the round-10 loss to *trigger* the event it then blocks through. Кратос winrate across event lineups: 0–6.7%.
- **Impact:** the marquee event is dead content in bot/mostly-bot games; see M39 for the compounding grind.
- **Fix direction:** exempt the event Кратос (and only him) from the round>10 bot block, routing him into `HandleBotAttack`.
- **Fixed:** 2026-07-13 (designer override) — the event start requires a non-bot (`PlayerType != 404`) Kratos. Test/simulation bot Kratos never opens the event, so no bot-specific event action path exists (`CP:2798-2805`).

### M39. The Кратос event usually grinds to the round-20 hard cap instead of ending at 16
- **Described:** `docs/CHARACTERS.md` (Кратос): "event ends at round ≥16 or when 5 players are dead".
- **Actual:** the round-16 check additionally requires **fewer than 5 alive** (i.e. ≥2 dead, `CP:3850-3860`); otherwise only the `RoundNo >= 20` hard cap ends the game (`CheckIfReady.cs:1057-1060`). Combined with M38, most event games run rounds 11–19 with everyone blocking, **each round scoring ×4** (`InGameStatusClass.cs` `GetRoundScoreMultiplier`, `_ => 4` arm).
- **Evidence:** rounds distribution in event lineups: 24/30 (settlers lineup), 28/30 (Спартанец lineup), 16/30 (special-win lineup) games hit the round-20 cap. Block-proof round-farming kits dominate those games — Осьминожка won **80%** of the settlers lineup.
- **Impact:** balance-relevant hidden behavior: up to 9 bonus rounds of ×4 income for kits that monetize passive rounds.
- **Fix direction:** designer decision — make round ≥16 end the event unconditionally (matching the docs), or deliberately document/curate the ×4 extension economy.
- **Fixed:** 2026-07-13 — rounds 11–16 are exactly six Kratos actions (Block included). All-enemies-dead ends early; after action six any living enemy ends the event as failure. The round-20 cap remains only a defensive engine guard (`CP:3872-3882`; `DoomsdayMachine.cs:1729-1735`).

### M40. A dead Штормяк holder keeps taunting, forcing the living to fight a corpse
- **Actual:** dead players are auto-set `IsBlock = true` every tick (`CheckIfReady.cs:1168-1172`), and the Штормяк taunt loop selects taunters by `IsBlock` + passive with **no `IsDead` filter** (`:1339-1373`). The taunt-bypass then turns the forced fight into a *real* fight against the blocking corpse (`DoomsdayMachine.cs:636-639`), and the fight-target selection has no dead guard either (`DoomsdayMachine.cs:487-492`). The corpse's `FightCharacter` has full stats, so it can win and keep earning score after death (feeding M37).
- **Reachability:** Котики die to Кира/Пейзаж/Rumbling/Кратос. The original Котики is capped at one taunt per enemy per game; a **transferred** Storm cat (Кошачья засада) taunts every round, forever.
- **Fix direction:** add `!t.Passives.IsDead` to the taunter filter; matrix §1 needs a "dead as *source*" row (the table only covers dead as target).
- **Fixed:** 2026-07-13 (broadened designer verdict) — dead players are inert across the main game: fight queues exclude dead attackers/targets, readiness forced-action sources and bot actions exclude them, round/passive dispatch skips them, buffered ordinary score is discarded, and final prediction/steal sources ignore them. Only the two death-state revival cases (`Глаз Шусуи`, `Боги мне не указ`) may dispatch while dead. The source/target rule is now explicit in INTERACTION-MATRIX §1.

### M41. BattleShip_update was checkmarked while action boundaries still broke its limits and turn rules
- **Actual before the fix:** `ShootOwnBoard` never called `ProcessTurnStart`, so a player could bypass a queued Penalty/Stun by firing at an approaching summon. `SelectWeapon` changed `SelectedShotType` before proving that a live loaded weapon existed, and `Shoot` trusted that state; a forged Greek Fire/Incendiary selection worked without the upgrade, while a depleted Incendiary stayed selected and fired forever. Two own-board summon-death paths decremented `SummonSlotsUsed`, turning the four-normal-summons-per-match limit into a partially reusable counter. Conversely, free ship/boarding pending summons were blocked by and consumed that normal limit despite `PendingSummonDeploy.IsFree` explicitly promising no slot cost; full normal usage could therefore soft-lock mandatory boarding deployment.
- **Fixed:** 2026-07-12 — own-board fire shares the turn-start gate and skipped results carry `WasSkipped`; every special selection resolves to a living loaded weapon before state changes, every shot revalidates ammo, and the final finite projectile restores Ballista. Normal uses are never refunded, while free pending/boarding summons ignore the normal cap. Turn-back redeployment reuses the existing summon without a new cap/reveal check, and forged fresh Cursed Boats are rejected (`BattleshipService.Shoot`, `ShootOwnBoard`, `SelectWeapon`, `DeploySummon`, `DeployPendingSummon`; death paths in `BattleshipGameEngine`).

### m37. Simulation winrate counts only place 1 — special winners and dead leaders are misattributed
- **Actual:** `SimulationRunner` credits a "win" solely to final place 1 (`SimulationRunner.cs:687,679`). Sakura's «Одна из трех» soft wins are invisible (she actually won **11 of 61** sampled games while every report shows 0%); dead place-1 players (M37) are counted as winners; Наруто's clone seats inflate his denominator (21/90 for 30 games).
- **Impact:** harness-only, but it skews every balance read taken from sim winrates.
- **Fix direction:** attribute wins by the game's announced winner; count Наруто as one entity.
- **Fixed:** 2026-07-13 — per-game sim rows carry `IsWinner` from `GameClass.WinnerPlayerIds`; aggregates count those declared wins and exclude dispersed structural Naruto clones from games/wins/averages. Printed summaries now show declared wins/games (`SimReport`; `SimulationRunner.BuildCharacterRows`).
- **Corrected:** 2026-07-14 — the separate large-sweep merger still recomputed `winRate` from `top1`, reopening the same attribution bug after otherwise-correct batches. The four-level merger now aggregates authoritative per-game `isWinner` rows (and exact score/place samples), so special winners, dead leaders and structural clones stay correct through the final charts (`tools/sweep-report.py`; `tools/sweep.sh`).

### m38. A score tie suppresses Sakura's declared win but not her win announcement
- **Actual:** Sakura's win fires her global line and reveals the passive (`CheckIfReady.cs:556-565`), then the final announcement separately prints «Ничья» if anyone ties `playerWhoWon`'s score (`:629-633`). A Sakura tied at top-3 gets both: «Я одна из легендарной тройки…» **and** «Ничья» (while still being paid first-place rewards via `rewardPlace`).
- **Fix direction:** designer call on whether a tie beats «Одна из трех»; make the two messages agree either way.
- **Fixed:** 2026-07-13 — `Sakura.HasUncontestedSoloTopThree` requires no more than three living players at or above Sakura's score. A fourth tied player suppresses the passive, reveal, line and first-place reward override.

### m39. `HandlePostGameEvents` computes its own winner, so two win narratives can both print
- **Actual:** the flavor dispatcher recomputes `playerWhoWon` as *first alive by score* (`CheckIfReady.cs:88-88`), ignoring the Sakura override decided later — with a Sakura soft-win, the real place-1 character's win phrase (Кратос/Сайтама/Монстр/Рик/HardKitty) prints alongside Sakura's win line.
- **Fix direction:** pass the settled winner in, or skip character win-phrases when «Одна из трех» triggered.
- **Fixed:** 2026-07-13 — `HandlePostGameEvents` receives the already settled winner; Sakura's soft win no longer prints the raw score leader's character victory line.

### m40. The score floor at 0 exists only in `AddBonusPoints` — the regular-point flush can leave anyone negative
- **Actual:** `AddBonusPoints` floors `Score` at 0 unless the player has «Никому не нужен» (`InGameStatusClass.cs:234-240`); the end-of-round flush `AddScoreWithMultiplier` has **no floor** (`:324-350`). Multiplied negative regular points (e.g. «СОсиновый кол» −1 ×2/×4) can push a non-HardKitty player's visible score below zero, where it stays until the next bonus event incidentally floors it.
- **Fix direction:** either floor in the flush too, or declare negative scores legal for everyone (then the `AddBonusPoints` floor is the anomaly).
- **Fixed:** 2026-07-13 (designer choice A) — `AddScoreWithMultiplier` now enforces the same zero floor as bonus points. HardKitty remains the passive exception; audited lethal effects use the explicit `AddBonusPointsIgnoringFloor` path.

### m41. Random forced-attack pools exclude the dead but not the round-10-banned Тигр
- **Actual:** the Монстр no-escape random redirect (`CheckIfReady.cs:1458-1477`) and the Aggress auto-attack (`:1288-1301`) pick any living target, including a round-10-banned Тигр — unlike the Шэн/Штормяк/Монстр-victim carve-outs. The attack fizzles on his forced skip (no penalty), so the victim just wastes their forced action and inflates the skip-event counter.
- **Fix direction:** add the ban carve-out to both random pools for consistency.
- **Fixed:** 2026-07-13 — both Aggress auto-targeting and Монстр's random no-escape fallback exclude a round-10 holder of `Стримснайпят и банят и банят и банят` (`CheckIfReady.TickAsync`).

### m42. Round-8 Клоны Сусано attacks are granted to dead predictors
- **Actual:** dead players are auto-`ConfirmedPredict` (`CheckIfReady.cs:1168-1172`) and the clone-attack loop has no `IsDead` filter (`:1418-1443`), so a player killed before round 8 (Death Note resolves from round 1) whose locked prediction named Мадару attacks him as a corpse.
- **Fix direction:** add `!p.Passives.IsDead` to the predictor filter.
- **Fixed:** 2026-07-13 — the round-8 predictor source must be living; a dead Madara also cannot arm the scene (`CheckIfReady.TickAsync`).

### m43. Кира's L-arrest "−500 очков" actually floors to 0
- **Actual:** the arrest applies `AddBonusPoints(-500, "Арест Киры")` (`CP:5349`), which floors at 0 (`InGameStatusClass.cs:239`); the global log and `docs/CHARACTERS.md` both claim −500. Zeroing is probably the intent (drop him to the bottom), but a 0-score Кира ties other zero-score players instead of sitting below them.
- **Fix direction:** designer wording call: keep the floor and fix the log/doc text, or bypass the floor for the arrest.
- **Fixed:** 2026-07-13 (designer choice B) — L-arrest calls `AddBonusPointsIgnoringFloor(-500, "Арест Киры")`, applying the real −500 change through the global floor.

### m44. Чернильная завеса entries created during Кратос-event rounds never restore
- **Actual:** the ink restore runs exactly once, at round 11 (`CP:5421-5450`). Ink ledger entries created in event rounds 11–19 (fights still reach Осьминожка via taunt/no-escape/event bypasses) are never flipped — the fake result becomes permanent.
- **Fix direction:** re-run the restore at game end for entries newer than round 11, or stop ink recording after round 10.
- **Fixed:** 2026-07-13 — `RestoreOctopusInk` is an idempotent settle-and-clear pass used at the round-11 boundary and again before final effects. The second pass safely catches any post-boundary remainder (`CharacterPassives.RestoreOctopusInk`; `CheckIfReady.HandleLastRound`).

### m45. INTERACTION-MATRIX §7 omitted the Goblin Ziggurat step in the end-game order
- **Actual:** the settlement order in `docs/INTERACTION-MATRIX.md` §7 listed "…sort → AWDKA → Premade → Sakura" while the code runs the M1 Goblin-Ziggurat enforced win between Premade and Sakura (`CheckIfReady.cs:538-554`).
- **Fixed:** 2026-07-12 — §7 order line now names the Goblin step (this change-set; docs-only).

### m46. Battleship feedback lied about skipped shots, Brander deaths, trails and re-entry coordinates
- **Actual before the fix:** every disappearing Brander played the explosion sound, including quiet Drakkar freeze and harmless live-deck collision. A skipped Penalty/Stun turn was emitted as a miss at default A1, inflating client shot statistics and triggering fake projectile/miss feedback. Mast re-entry warnings always formatted row 1 even when a summon entered at row 10 or from a side. The trail cache recorded the previous state rather than the incoming spawn/re-entry state, and penalty-zone copy omitted the no-penalty spawn exception.
- **Fixed:** 2026-07-12 — Brander sound now requires both disappearance and a newly received detonation log; `wasSkipped` suppresses shot VFX/sound/stat changes; Mast warnings receive actual row+column; incoming state positions seed trails and new matches clear cached trails/marks; the tooltip states the spawn exception. `SummonDto.moveDirection` now drives a dedicated “Вернуть на карту” flow that highlights the actual bottom/side edge and adjacent lanes, so a sideways Cursed Boat is operable without using a fake row-1 proxy (`useBattleshipStore`, `CombatPhase.vue`, `SummonBar.vue`, `CellComponent.vue`, `GenerateMastWarning`, `GameHub.BattleshipShoot*`).

### m47. Геральт's pitchfork "−500" was also swallowed by the ordinary zero floor
- **Actual before the fix:** both the web demand path and end-of-round advance settlement logged death and −500 but called `AddBonusPoints(-500, "Вилы разъяренной толпы")`; like m43, this floored most victims at zero instead of applying the documented lethal score change.
- **Fixed:** 2026-07-13 — both paths use the same audited `AddBonusPointsIgnoringFloor(-500, ...)` escape hatch as Kira's arrest (`WebGameService.DemandContractReward`; CP:5007-5028). This is part of the designer's m40 rule that only explicit character mechanics may break the otherwise universal floor.

### m48. Darksci's round-9 «Нахуй эту игру» reached the global log one turn after its personal log
- **Expected:** when the mandatory round-9 level-up drops the Дизмораль holder to 0 Psyche, the personal and public «Нахуй эту игру» records describe the same immediate forced Skip.
- **Actual before the fix:** HandleNextRound tried to predict the public record before the level-up with `Psyche < 4`, while the actual −5 and personal record ran later in GameReactions. A holder entering with 4 Psyche could raise it to 5, fall to 0 and see the personal record immediately, but the failed early check left the public record to the generic zero-Psyche hook after `RoundNo` advanced to 10 (`CP:6625-6644`; `GameReactions.cs:1344-1361`). Replay 2fb5f271 (v4.3.37) records exactly that split: round-9 pre-fight state has 0 Psyche and the personal Дизмораль/phrase records, while the public phrase first enters allGlobalLogs in round 10.
- **Regression history:** the decoupled level-up handler originally added the public record at the real transition (`68ba3ac`); `be0dd94` removed that line while splitting Darksci's hooks and left the pre-level-up prediction unable to cover the 4→5→0 case.
- **Fixed:** 2026-07-13 — removed the speculative pre-level-up check and now writes the public phrase beside the actual −5/Skip/personal transition. The existing cumulative-log guard prevents the round-10 zero-Psyche hook from duplicating it (`GameReactions.cs:1344-1364`; `CP:6625-6638`).

### M42. Salldorum's active kit was unreachable or resolved after it could matter
- **Actual before the fix:** Шэн required hidden web-only `ActivateShen`/`DeactivateShen` calls with no Vue/store caller, then cleared `ActiveThisTurn` in `HandleEndOfRound` before its post-sort movement case, so it could taunt but never move. Великий летописец set its ×3 Skill multiplier in the after-fight hook, after `CalculateRounds` had already decided the result. The Chronicle rewrite existed in Hub/SignalR but had no player-facing button; bots merely flipped rewrite flags without any score/stat/cola effects. Finally, the cola never cleared `Buried`, so its pickup flag reset allowed repeated natural drinks.
- **Fixed:** 2026-07-12 — the shared readiness-stage `Salldorum.ResolveShenDashes` auto-spends the next submitted attack, performs the leap before fight calculation, retains the below-position pull and arms the one-round random-target magnet; Цукуеми suppresses the whole activation and post-pull Madara/Naruto sanitation protects sealed/mutual queues. A forced second Геральт target receives the consolidated contracts. The ×3 multiplier moved to both attack-before and defense-before and is reset per opponent; bots and web use the same `Salldorum.RewriteHistory`, whose server/UI gates reject dead Salldorum; every live completed Chronicle round exposes a guarded glowing action; and the central cola resolver consumes/hides the fixed-cell object while permitting only the specified one historical second drink. `c_salldorum_double_cola` covers that intended timeline interaction.
- **Rework follow-up:** 2026-07-13 — a successful forward attack now places the holder on the selected target's exact cell and preserves it through the following full action round. Only players strictly crossed by the movement have their first existing real target replaced; no extra fight is appended. The cola widget exposes its ready round/state and collapses all consumed states to `Потрачено`; `c_salldorum_double_cola` is an unbound secret whose visible description/hint is only the designer-supplied cola sentence.

### M43. The ultra-secret character escaped through public and off-game projections
- **Actual before the fix:** the identity could be forced or inferred through forged prediction/Death-Note submissions, store/discovery/statistics/last-played state, achievement metadata, public/draft alternatives, final projections, replays/stories and the static Vue character/localization bundle. Participant/admin fight DTOs could additionally expose its combat factors after the name itself was masked.
- **Fixed:** 2026-07-13 — renamed the private identity to `unknown_bug`, made the missing portrait authoritative, and centralized owner/public checks in `UnknownBug`. A natural draft roll is silently auto-locked; public submissions validate against public allowlists; bots, team seeding, final predictions, ordinary admin mutations, store/stat/widget surfaces, service diagnostics and account migration all fail closed. The two character achievements were retired, private rosters create neither replay nor story, non-owner state/chronicle/fight projections mask the identity and redact combat factors, and the Vue build strips the exact private definition/localization payload from its public catalog modules. The owner alone receives the terminal DTO and CRT/glitch presentation (`UnknownBug`; `StartGameLogic`; `WebGameService`; `GameStateMapper`; `ReplayService`; `GameStoryService`; `vite.config.ts`).
- **Admin-test correction:** 2026-07-13 — the admin-only web “Тестовая игра” picker and numeric Discord `*st <int>` overload are the two deliberate selection exceptions. Both require the persistent account's `PlayerType == 2` and use `CharactersPull.cs` `GetAdminSelectableCharacters`, which appends only this private definition to the unchanged public Tier ≥ 0 catalog. Regular users, drafts, `SetCharacter`/`SetStat`, stores and public character-list consumers still cannot select or enumerate it; service diagnostics continue to print `(private)`.

### M44. Близнец did not copy Justice on successful Block and copied it on bypassed Block
- **Intended:** only attackers actually stopped by Monster's Block contribute to Близнец's per-round maximum and bonus payout; an ArmorBreak, Штормяк-taunt, summon or railgun bypass resolves as a fight and must not count.
- **Actual before the fix:** the copy lived in `HandleDefenseAfterFight`, but a successful Block exits the fight loop without calling that hook. It therefore copied nothing on the outcome named by the passive. Conversely, an ArmorBreak or another bypass could resolve a real fight while the defender retained `IsBlock`, causing the after-fight case to copy Justice and pay the bonus on the inverse outcome.
- **Fixed:** 2026-07-13 — persistent attacker Justice is snapshotted before any ForOneFight override, but the max/copy/bonus now runs only inside the successful-block branch (`DoomsdayMachine.cs:604,658-716`). The old after-fight dispatch is an explicit no-op (`CP:1001-1016`), and the same block branch continues to suppress generic block Justice for every Близнец holder.

### M45. L2/L3 bot AI read hidden identities, stats and live opponent actions
- **Expected:** AI levels 2 and 3 must decide from the same projection as a regular player: public leaderboard rows and owner-visible marks, legal action targets, sanitized global logs, the bot's current/last personal logs, public resolved outcomes and exact detail from its own fights. Neither level may know who currently chose Block/Skip or inspect an opponent's submitted attack. L3 may know the rules and infer more from longer evidence, but may not receive privileged facts.
- **Actual before the fix:** the shared `HandleBotAttack` scorer dereferenced opponent `GameCharacter`, `Passives`, score/Justice and private status/history, including live `IsBlock`, `IsSkip` and `WhoToAttackThisTurn`. L3 predictions were filled from every opponent's true character and its fight estimate used real target stats/Justice. The simulation factory also prefilled exact characters for every AI level. Separately, `GameStateMapper.MapStatus` sent live opponent Block/Skip flags to regular web viewers, so even a human client could observe the forbidden answer.
- **Impact:** L2 counter-picked actions that no player could see; L3's extra strength came primarily from omniscience instead of better reasoning. Simulation results for the old L2/L3 policies overstated legal bot strength, and web players could react to current defenses.
- **Fixed:** 2026-07-13 — strict L2/L3 now leave the legacy scorer through `BotsBehavior.HandleFairBotAttack`, `HandleFairBotKira` and `CharacterPassives.HandleFairBotPredict`. `BotInformation.CaptureVisibleRound` records a viewer-scoped `GamePlayerBridgeClass.AiKnowledge` snapshot from sanitized logs, resolved public results, public places and that bot's own fight details; fair targeting consumes owner-scoped leaderboard markers rather than raw target state. L2 keeps coherent builds and broad character objectives with weighted choices. L3 becomes stronger through longer memory, confidence-weighted public-catalogue hypotheses, public roster/rule constraints, inferred Justice/fight edge and best-target selection; every hypothesis can remain wrong. Exact predictions require a player-visible reveal except for the explicit M27 round-8 Naruto/Sakura/Itachi challenge script; simulation prefill is L1-only, and live `IsBlock`/`IsSkip`/`ConfirmedSkip` are projected false for opponents/spectators by `GameStateMapper.MapStatus`. L1 remains the deliberately frozen privileged legacy control. Verified with `dotnet build`, L1 10/10, L2 smoke+coverage 27/27 and L3 default smoke+coverage 108/108, all with zero errors/stuck games. Full policy review: [BOT-AI-DESIGNER-REVIEW.md](BOT-AI-DESIGNER-REVIEW.md).

### M46. Naruto sibling exclusions inflated count-based bot blocking
- **Intended:** the two living Naruto siblings are illegal attack targets, but removing them from the menu must not make an L0/L1 Naruto bot defend more often than another bot in the same six-player state.
- **Actual before the fix:** both count-based action rolls used the three-entry legal Naruto target list directly. L0 therefore blocked in one of four slots instead of one of six, while L1 compared its 1–4 Block roll against 3 instead of the normal 5 and blocked on every roll of 4 (`BotsBehavior.HandleBotAttack` / `HandleBotAttackRandom`).
- **Impact:** all three Naruto pilots overused Block/Гарем solely because Теневые made their own two siblings untargetable, rather than because of fight state or character strategy.
- **Fixed:** 2026-07-13 — `Naruto.GetBotActionTargetSlotCount` adds one virtual attack slot per living sibling only to the L0/L1 attack-vs-Block roll. The siblings remain absent from every real target pool and submission gate; dead siblings add no slot. L2/L3 already base Block on target quality and history rather than target count and are unchanged (`BotsBehavior.HandleBotAttack`; `BotsBehavior.HandleBotAttackRandom`).
- **Rework follow-up:** 2026-07-16 — Теневые clones can no longer choose Block/Harem at any AI level; they attack and use Skip only when every candidate is rejected. The virtual sibling slots remain only for an L0/L1 original Naruto, bot or player-facing behavior otherwise stays unchanged (`Naruto.CanChooseBlock`; `BotsBehavior`; `GameReactions.AttackOrSkipInsteadOfBlock`).

### M47. The global Rumbling theme depended on seeing Eren's masked identity
- **Intended:** when the public Armin warning opens round 10, every connected web viewer hears the Rumbling cue and the long theme continuously until round 11 begins.
- **Actual before the fix:** `Game.vue` searched the viewer-scoped `players` projection for the literal name `Эрен Йегер`. During an unfinished game, every non-owner receives `???` for an opponent's character, so only Eren's own client found him and started the audio. The long theme was also a one-shot untracked clip with no round-11 stop contract (`GameStateMapper.MapCharacter`; pre-fix `Game.vue` Rumbling watcher; pre-fix `sound.ts` `playErenRumblingWarning`).
- **Impact:** the public round-10 warning had owner-only audio in ordinary hidden-identity games, and theme duration was determined by the MP3 rather than by the round lifecycle.
- **Fixed:** 2026-07-13 — top-level `GameStateDto.IsRumblingWarningActive` exposes only the already-public warning window, derived from `RumblingWarningPlayed` and round 10 without revealing Eren's player row. Every player and spectator projection now drives the same watcher. The long theme runs on a cancellable loop channel and is stopped when the flag clears at round 11 (or the page unmounts); the short Rumbling cue remains one-shot. Pending channel loads are generation-guarded so a slow fetch cannot start the theme after it was stopped (`ErenYeager.IsRumblingWarningActive`; `GameStateMapper.ToDto`; `Game.vue`; `sound.ts`).

### D12. Every bonus-point "steal" mints points at the 0-floor
- Because `AddBonusPoints` floors the victim at 0 (`InGameStatusClass.cs:239`), each transfer-shaped mechanic credits the taker the **full** amount while the victim may lose less: «Выгодная сделка» (`CP:1804-1816`), Смертельный вирус, Глаза Итачи deduction, Сайтама's ledger reclaim, «Запах мусора». Zero-sum on paper, positive-sum at the floor. Acceptable pity cushion, or should takers receive only what victims actually lose?
- **Confirmed intended:** 2026-07-13 (designer choice A) — keep the victim's zero-floor cushion and credit the receiver's full nominal amount. Documented in GAME-DESIGN §6 and INTERACTION-MATRIX §4; no code change.

### D13. Forced-fight sources pierce the Кратос event's "everyone blocks" rule
- **Actual before the fix:** during the event the loop forced `IsBlock = true` on all non-Кратос players every tick, but Штормяк's taunt and a spanning Монстр no-escape window could replace that defense with real attacks. Aggress/Атакующий Титан/pickle conversions likewise stripped the forced block in the fight phase. Result: real fights (at ×4) between non-Кратос players during the event, and an Октопус-style fake loss could even hand Кратос his final death.
- **Fixed:** 2026-07-13 (designer choice B) — only Kratos ordinarily acts. Readiness suppresses forced sources; the fight phase suppresses action conversions, contract/Naruto/Storm/Justice setup, and `EnforceKratosEventActions` authoritatively clears every ordinary non-Kratos queue before combat. The 2026-07-14 unknown_bug rework adds the sole explicit exception: its own submitted action survives, while no forced source is re-enabled.

### D14. Sakura's «Одна из трех» applies inside team mode
- In a team game the announcement path is team-score-based and ignores `top3Player`, but the payout loop still pays a top-3 Sakura individual first-place rewards (`CheckIfReady.cs:579-646` vs `:662`). Should the hidden solo win condition exist in team games at all?
- **Fixed:** 2026-07-13 (designer choice B) — `Sakura.HasUncontestedSoloTopThree` immediately rejects any game with teams; team payout uses factual individual place only.

### Phase-6 checks that passed (selection)
- Mechanical invariants (re-run after the Наруто/DooM commits): no raw `AddJustice`; no live `GameCharacter.*ForOneFight` (only the commented-out Оборотень blocks, `CP:494-502,1240-1241`); every raw `AddPsyche(-…)` is a self-cost («Безумие», «Дерзкая школота», «СОсиновый кол») or the documented Дизмораль exception; `CharacterClass.DeepCopy` covers its only collection.
- End-game resolution order matches the §7 spec; Итачи deduction correctly precedes the sort; AWDKA re-sort correctly precedes the Sakura check; Sakura's alive-gate, the dead-player ZBS/mastery/lootbox gates and the Мадара projection's authoritative-payout isolation are all correct (post-`041dbab` round-8 flow matches its docs).
- Round-10 forced moral conversion cannot loop forever: every `AddMoral` interceptor still decrements or zeroes (Let's Roll zeroes moral at activation).
- Kill census: exactly 7 `IsDead = true` sites = the 6 documented sources + Кратос's own event death; no undocumented kill source; kill×immunity rows verified incl. the Мадара/Гоблин exemptions inside the event kill path.
- «Претендент русского сервера» ×3 regular, Тигр round-10 ban state, unknown_bug's owner-gated `RollExploit` rotation/one-shot closure (the then-missed defensive payload branch was later found as M53), Сайтама round-11 reclaim (zero-sum ledger + manual `BonusPointsFromMoral` flush), `HandleOctopus`×Еврей redirect, Пейзаж survival rules, Монстр death/drop rewards, Маневры/Вознесение remaining-grant caps otherwise matched their docs.
- unknown_bug's prediction exclusion is enforced at catalog, web/Discord submission, bot auto-fill, team seeding, score settlement and final projection boundaries. (The older blanket “35-name pool” statement was removed: the generic Монстр catalog/scoring rules are not identical and are outside this rework.)
- Наруто integration spot-checks: `ResolveScoreSuccessor` redirects all five late liabilities (virus `CheckIfReady.cs:377`, Цукуеми `:395`, Сайтама `CP:5392`, Октопус `CP:5445`, Школьник `CP:6532`); `SettleShadowClones` runs right after Rumbling (`DoomsdayMachine.cs:1549-1550`); clone deaths incidentally satisfy the M39 `alive<5` gate, so Наруто games end events at 16. Наруто-forced sim: 30/30 clean.
- Simulations: 586 games across 11 runs — 0 exceptions, 0 stuck games.

### Phase-6 balance observations (sim data — for the designer, not findings)
Historical fixed-lineup winrates, 30 games each, from the **pre-M45 omniscient AI-3 implementation**; retain only as a baseline and do not compare them directly with the fair AI-3 policy:
- **Стая Гоблинов dominated every lineup they appeared in**: 80% (kill lineup), 86.7% (copy-chain lineup), 43.3% (Мадара lineup).
- **AWDKA >45% twice** (53.3% special-win lineup, 46.7% Спартанец lineup) — the troll-win converts very reliably at difficulty 3.
- **Осьминожка 80%** in the Кратос-settlers lineup is mostly the M38/M39 grind payoff.
- Bottom across lineups: mylorik 0%, Рик Санчез 0%, Sirinoks 0–3.3%, Котики 0–17.6%, Кира 0–3.3%, Кратос 0–6.7% (see M38).
- Sakura converted top-3-alive in 11/61 games (18%) but died in 34/61 — kill-heavy metas hit her hardest (invisible in reports until m37 is fixed).

## Production runtime errors (2026-07-13)

> **Scope:** triage of three production stack traces (Discord 50035 `BASE_TYPE_REQUIRED`, `Convert.ToInt32("Обмен")` in `SortLogs`, embed > 6000 in `EmbedBuilder.Build`) reported from the live server (`runtime_errors`, Game #3 rounds 7/11).

### M48. Newline-less personal-log writes glue entries into one line and crash the «Обмен Морали» log aggregator
- **Actual:** six `AddInGamePersonalLogs` writes did not end with `\n` — the four end-game loser phrases and Salldorum's «Великий летописец» summary (`CheckIfReady.cs:130-148`) and Глеб's «Я щас приду: +9 *Морали*. …Празднуем!» (`CP:930`). The next log entry (typically the moral exchange, written by every player who presses the exchange button and by the round-10 forced conversion at `CheckIfReady.cs:1547-1550`) is appended to the same line. `SortLogs`' «Обмен Морали» aggregation then does `Split(":")[1].Split(".")` and `Convert.ToInt32` on each `Морали`/`Cкилла` segment (`GameUpdateMess.cs:941-979`, the `Обмен Морали` case in `SortLogs`); a glued line with a colon and a period before the exchange text yields a non-numeric segment — e.g. `…с врагом.` + `Обмен Морали: …` produces exactly `Convert.ToInt32("Обмен")` — and the whole `FightPage`/`UpdateMessage` render throws (production: Game #3, rounds 7 and 11).
- **Failure scenario:** a Глеб friend gets the «Я щас приду» bonus and exchanges moral the same round → that player's Discord game message stops updating for the round (every render throws until the logs rotate out).
- **Fix direction:** newline-terminate every personal-log write; make the aggregation parse tolerant (`int.TryParse`, mirroring the «Класс» guard).
- **Fixed:** 2026-07-13 — all six writers now end with `\n`; the «Обмен Морали» case skips non-numeric segments via `int.TryParse`. Fuzz-model of `AddInGamePersonalLogs`+`SortLogs` confirmed the glue produced the crash family and that no crash inputs survive the guard.

### M49. Game embeds/components can violate Discord's server-side payload rules (50035, embed > 6000)
- **Actual:** three gaps between Discord.Net's local validation and the server's rules, all funneled through `ModifyGameMessage`:
  1. `FightPage` chunks long logs into 1020-char fields with no cap on the count; description + leaderboard + chunk fields can exceed the 6000-char total, so `EmbedBuilder.Build()` throws inside `ModifyAsync` (production error #3).
  2. A 1020-char chunk that lands on whitespace-only text throws `ArgumentException` locally at `AddField` (latent sibling of the same overflow).
  3. A select menu with **zero options** passes `Build()` locally but the server rejects the payload with `50035 BASE_TYPE_REQUIRED` (production error #1). The only producible empty select was `GetLvlUpMenu`'s DooM Guy branch: `DoomGuy.StageForRound` returns `""` outside rounds 3/5/7/9, `GetOptions(state, "")` returns an empty list, and the menu had no fallback (every other select has a `kratos-death`-style guard) — reachable whenever a module point survives past its stage round (see M50).
- **Fixed:** 2026-07-13 — `ModifyGameMessage` now sanitizes the localized payload before sending: whitespace-only field values → `\u200b`, embed trimmed to ≤ 6000 by truncating the longest fields (description last), empty select menus and empty action rows stripped (`HelperFunctions.cs` `SanitizeEmbedForDiscord`/`RemoveEmptySelectMenus`); `FightPage` skips whitespace-only chunks; the DooM Guy level-up menu shows a disabled «Нет доступных модулей» placeholder when no modules are available. Verified against real Discord.Net 3.20.1 via reflection harness (8182→6000 trim, empty-select strip, valid payloads untouched).

### M50. A DooM Guy level-up point can become unspendable and spin the auto-move level-up loop forever
- **Actual:** `GetLvlUp`'s DooM Guy branch re-rolled an out-of-range pick into the module range **only for bots** (`GameReactions.cs:876-883`); auto-move humans arrive through the same bot path (`HandleLvlUpBot`) with stat picks 1–4. With 2 starter modules, a deterministic pick of 3/4 returned without decrementing `LvlUpPoints`, and both `HandleLvlUpBot` loops (`BotsBehavior.cs:4582`, `:4593`) recompute the same pick — an infinite loop freezing the game tick. Off-stage rounds (stage `""`, zero options) made **every** pick unspendable, which is also what stranded the point behind M49's empty menu.
- **Fixed:** 2026-07-13 — auto-move humans get the same in-range re-roll as bots; both bot level-up loops break when a `HandleLvlUp` call makes no progress (the point stays banked for the next module round instead of spinning). Covers Геральт's maxed-oil `return` paths too.

### M51. Replay capture + an O(catalog) localization engine made every game ~10-15x slower, which made every sweep report mass STUCK
- **Expected:** a headless bot sim spends its time on game logic; a 2 000-game sweep batch finishes clean, as it did on 2026-07-05 (2 006 games, 0 stuck, 44 s).
- **Actual:** by 2026-07-13 a 2 007-game batch finished only ~400 games and reported ~1 600 "STUCK at round 2" plus a handful of `NullReferenceException`s. Per-game cost had gone from ~0.04 s to ~0.45 s (identical flags; measured across the `sim-*.json` reports of 07-11..07-13). Two multiplied causes, both confirmed by profiling (`dotnet-trace`), not by reading:
  1. **Replay capture ran in the simulator.** Per-round replay capture projects all six players through the full per-viewer state mapper, localization included, three times per round (`ReplayService.cs:51-158`). The harness registers no replay-save hook (`Global.cs:46`) and serves no clients, so every snapshot was built and immediately discarded — 57 % of the simulator's runtime, plus the heap for 2 000 concurrent games.
  2. **The localization engine was O(catalog) with a recompiled regex per entry.** `ReplaceTerm` built a fresh pattern string per catalog row and passed it to the *static* `Regex.Replace`; .NET's static regex cache holds 15 entries, so cycling ~1 000 rows (`exact` + `terms` + phrase pairs) recompiled nearly every pattern, for every string translated. Cost therefore scaled with catalog size, which is why the slowdown arrived in two steps as `localization.en.json` grew on 07-11.
- **Impact:** sweeps were unusable (exit 1 on every batch). The reported STUCK games were **healthy**: a tick pass is single-threaded across the whole batch, so at 2 007 games one pass exceeded `SimulationRunner.StuckGameSeconds` (30 s) and the watchdog culled games that had simply not been reached yet — every stuck entry has `secondsStalled == 30` and `gamesFinished == 0` at the cull. The NREs are downstream of the cull, not of game logic: the watchdog removes a game from `Global.GamesList` on the main thread while the timer thread is mid-round inside it, so `GamesList.Find(...)` returns null and `game!` throws (`GameReactions.cs:720`).
- **Fixed:** 2026-07-13 — the simulator disables replay capture (`Program.cs:71`; the round's opening capture call then returns null, which short-circuits the rest). The localization engine keeps identical output but pre-filters each catalog row with an ordinal substring scan (a row that is not literally present cannot match either branch), compiles each row's word-boundary regex once at load, and memoizes the bounded hot inputs (`GameLocalization.cs`). Verified output-identical against a reference copy of the old engine over 13 988 translations (both languages, both bilingual modes, every catalog/phrase entry plus composite forms): 0 mismatches. Result: 100-game sim 51 s → 6 s; 2 007-game sweep batch 350 s/1 608 stuck → 66 s/0 stuck/0 errors.
- **Follow-up fixed:** 2026-07-16 — wall time no longer classifies an individual game. `CheckIfReady` increments `GameClass.ReadinessLoopVisits` only when the loop actually reaches that game; the watchdog requires five such visits at an unchanged round before removal. A game delayed behind a large batch or unrelated CPU load therefore accumulates time but no visits. New stuck rows record `loopVisitsWithoutProgress`; `secondsStalled` is diagnostic only. The separate 60-second no-global-progress guard remains for a round calculation that pins the loop (`CheckIfReady.TickAsync`; `SimulationRunner` wait loop; `SimStuckDto`).
- **Still open:** `Global.GamesList` is a plain `List<GameClass>` mutated by the web/Discord/sim threads while the 100 ms timer thread iterates it — a latent production race independent of the false-STUCK classifier. → **Resolved 2026-07-17 as M111** after it killed a sweep arm: all mutations now lock the list instance and `TickAsync` iterates a locked snapshot.

### M52. Parallel simulator processes crashed each other at startup (exit 134) through the shared account store
- **Expected:** several `--sim` processes can run at once (e.g. one sweep per `--ai-difficulty` level) without interfering.
- **Actual:** a batch aborted the whole sweep with `harness failure (exit 134)`. Exit 134 is SIGABRT — an unhandled `FileNotFoundException` in `UserAccountsDataStorage.LoadAllAccounts`: the starting process listed `DataBase/UserAccounts`, and a file vanished before it was read (`UsersDataStorage.cs`, read was outside the `try`). Cause: every process shares that directory, mints 6 bot accounts per live game, and flushes **all** accounts every 60s; the flush replaces each file via temp-file + move, which is *not* atomic on a Windows-backed mount, so a concurrent reader can observe the gap. Beyond the crash, the arms silently corrupted each other's measurements: each `--sim` start resets **all** bot accounts (`IsPlaying`, `TierPity`, `MatchHistory`, `CharacterPlayedLastTime`) while the other processes are mid-run, and pity/last-played feed the character roll.
- **Impact:** parallel sweeps were both crash-prone and scientifically invalid — the L0/L1/L2/L3 arms perturbed each other's character distributions.
- **Fixed:** 2026-07-14 — simulation mode disables account persistence before the container is built (`Program.cs:40`): the harness starts with an empty in-memory account store and never writes one. It only plays bot accounts, which it resets at start anyway, so nothing is lost; process startup also stops reading ~12k files (43s → 12s for a 100-game sim). Reading an account file now retries a transient I/O failure instead of dying on it, and still throws if the file is genuinely unreadable — silently skipping a real account would recreate it empty and then flush over the original (`UsersDataStorage.cs`). Verified: four simultaneous sims (`--ai-difficulty 0/1/2/3`, 307 games each) all exit 0 with 0 stuck and 0 errors.

### M53. unknown_bug did not stack Exploit payload when defeating its carrier in defense
- **Expected:** every real unknown_bug victory over the active Exploit carrier adds exactly one payload stack whether unknown_bug attacked or the carrier attacked; a direct attack closes the objective only after that resolved win is observed.
- **Actual before the fix:** `UnknownBug.RecordResolvedFight` returned unless the winner was the selected PointFunnel stream target, while `TryCommitExploit` added a stack only when unknown_bug was the attacker. A carrier attacking unknown_bug and losing therefore produced no payload. Naively broadening the observer would also have double-counted an attacking win because the resolved path calls the observer before commit (`UnknownBug.RecordResolvedFight`; `DoomsdayMachine` resolved-fight/commit order).
- **Impact:** the same carrier loss paid one stack when unknown_bug initiated it and zero when the carrier initiated it, undercounting the eventual Exploit commit.
- **Fixed:** 2026-07-14 — the authoritative resolved-fight observer now records both PointFunnel source wins over the carrier and unknown_bug's own carrier wins from either side. Commit consumes the already-observed attacking result without adding it again; a genuine Block/Skip no-fight exit still closes without a win stack. Eternal Tsukuyomi was subsequently made unable to clear Bug's action, so it no longer needs a synthetic cancellation commit (`UnknownBug.RecordResolvedFight` / `TryCommitExploit`; `DoomsdayMachine`; `Madara.PrepareEternalTsukuyomiRound`).

## Empire's Endgame browser-game triage (2026-07-14–2026-07-16)

### M54. Durak take/finish controls stay stale and can soft-lock an empty hand until reload
- **Expected:** after the defender finishes covering cards or declares a take, the current attacker immediately sees `Бито` / `Хватит подкидывать`; an attacker with no remaining card must always be able to resolve the bout and trigger the automatic refill.
- **Actual before the fix:** `playerCanTake` and `playerCanFinish` read only methods on a `shallowRef` engine. Engine commits replaced the separate reactive snapshot but did not invalidate those computed values, so both stayed at their initial `false`. A reload reconstructed the engine and made the missing button appear. The face-down deck was intentionally inert because refill happens only inside `resolveBout`, leaving the player with no alternative control (`pages/EmpiresEndgame.vue` `playerCanTake`/`playerCanFinish`; `components/empires-endgame/DurakTable.vue`).
- **Failure scenario:** play the last usable attack, let God cover it, and have no matching throw-in — the table says it is the emperor's turn but offers no button; refreshing reveals `Хватит подкидывать`.
- **Fix direction:** invalidate engine-backed computed state on every subscription emission, and automatically close a resolved bout when the player has no card left to play; cover the transition in a browser test without reloading.
- **Fixed:** 2026-07-14 — every engine subscription now invalidates the `shallowRef`-backed action computeds, and `runGodTurns` automatically calls `endAttack('player')` when an otherwise-resolvable player attack has an empty hand. The deck is non-control markup labelled `автодобор`. Named seeded fixtures plus Cypress verify both immediate `Хватит подкидывать` and empty-hand recovery without a reload (`EmpiresEndgame.vue` `initializeEngine`/`runGodTurns`; `qa.ts`; `cypress/e2e/empires-endgame.cy.ts`).

### M55. Opening the Empire's Endgame constructor throws `DataCloneError`
- **Expected:** `Конструктор` opens a detached editable definition from every phase.
- **Actual before the fix:** `cloneEmpiresConfig` called `structuredClone(config)`. After the first assignment, Vue wrapped the config in a reactive Proxy; browsers cannot structured-clone that Proxy, so `openEditor` threw `DataCloneError: #<Object> could not be cloned` (`features/empires-endgame/config.ts` `cloneEmpiresConfig`; `pages/EmpiresEndgame.vue` `openEditor`).
- **Fix direction:** clone the JSON-safe definition through serialization (or explicitly unwrap it), then browser-test opening, editing and saving the drawer.
- **Fixed:** 2026-07-14 — the central definition clone uses JSON serialization, which matches the validated JSON-only schema and safely detaches Vue Proxies; `BuilderDrawer` uses that same clone boundary. A reactive-config unit test and a browser error listener cover the original exception (`config.ts` `cloneEmpiresConfig`; `config.spec.ts`; `cypress/e2e/empires-endgame.cy.ts`).

### M56. Council-card CSS makes cards unreadable and their main surface is non-interactive
- **Expected:** held cards remain legible in `Совет карт`, can be selected, and clearly expose improvement/restoration availability.
- **Actual before the fix:** the broad `.council-grid button` rule also restyled the nested `EmpireCard` root buttons, replacing their light/inverted card surfaces with a nearly black translucent action-button background. Those cards were not marked `interactive`, so clicking the visually dominant surface emitted nothing; with zero points the only other buttons were disabled without an explanatory selected state (`pages/EmpiresEndgame.vue` council template/styles; `components/empires-endgame/EmpireCard.vue`).
- **Fix direction:** scope action-button CSS to the action row, make council cards selectable, and show why actions are unavailable; cover contrast/selection in browser tests.
- **Fixed:** 2026-07-14 — only direct `.council-actions` buttons receive the dark action style; every retained card is interactive/selectable and the zero-point state explains how points are earned. Non-interactive cards elsewhere render as articles instead of fake buttons. Cypress checks computed card luminance, selection and a real point-spending improvement (`EmpiresEndgame.vue` Council template/styles; `EmpireCard.vue`; `cypress/e2e/empires-endgame.cy.ts`).

### M57. Technology coordinates are interpreted as percentages, pushing nearly the whole tree off-canvas
- **Expected:** all configured doctrine/technology nodes are reachable on a scrollable tree; constructor mode permits dragging and relinking them.
- **Actual before the fix:** the default definition stores authored canvas coordinates such as x=180..900 and y=40..1,360, but `TechTree` appended `%` and clipped overflow in a fixed 600px canvas. Only the x=20/y=40 root remained visible; the other nodes and their edges were off-screen, and therefore could not be selected or dragged (`public/empires-endgame/game-config.json` technology `position`; `components/empires-endgame/TechTree.vue`).
- **Fix direction:** render authored coordinates on a sized, scrollable canvas and keep drag emissions in the same coordinate space; browser-test node reachability, selection and editor dragging.
- **Fixed:** 2026-07-14 — `TechTree` now derives a world-pixel canvas from all configured extents and exposes scrollbar, background-pan, touch and keyboard navigation. Nodes/edges share one origin; constructor drag accounts for scroll and grab offset before emitting integer world coordinates. The detail panel also exposes full costs and blocking dependencies. Cypress reaches all 62 configured nodes, selects the last one and verifies an editor drag changes its position (`TechTree.vue`; `EmpiresEndgame.vue` `technologyNodes`; `cypress/e2e/empires-endgame.cy.ts`).

### M58. Hand-card flag passives stack permanently after cards leave the hand
- **Expected:** only cards currently held during an empire phase contribute their normal/inverted passive flags.
- **Actual before the fix:** `startEmpirePhase` included `flag` in card phase effects, and `applyEffects` added those values directly to persistent `state.empire.flags`. No transition removed or rebuilt the contribution, so repeated cons accumulated ghost bonuses and penalties from cards no longer held (`features/empires-endgame/engine.ts` `startEmpirePhase`/`applyEffects`).
- **Fix direction:** serialize the current hand-card flag contribution separately, subtract it when the empire/event phase ends and before rebuilding the next one, and preserve permanent flags from gifts, events and research.
- **Fixed:** 2026-07-14 — serialized `cardFlagBonuses` records only the current hand's contribution; the engine subtracts it before rebuilding a card empire and when leaving empire/event flow, without touching permanent flags. Legacy empire/event snapshots reconstruct active contribution ownership from their held card faces; other phases default it empty. Focused tests prove temporary and permanent values separate correctly (`engine.ts` `heldCardFlagBonuses`/`recordCardFlagBonuses`/`clearCardFlagBonuses`/`validateAndCloneSnapshot`; `engine.spec.ts`).

### M59. The table exposes the next draw as the trump card
- **Expected:** the visible bottom card supplies the trump suit and remains the last card drawn.
- **Actual before the fix:** the engine draws with `deck.pop()` and derives trump from the bottom/index-0 side, but the page rendered `deck.at(-1)`. The UI therefore exposed the next draw and could show a suit unrelated to the real trump (`features/empires-endgame/engine.ts` `drawToHand`/`resolveTrumpSuit`; `pages/EmpiresEndgame.vue` `trumpCardView`).
- **Fix direction:** render `deck[0]` and assert the preview matches the engine's trump in deterministic tests.
- **Fixed:** 2026-07-14 — `trumpCardView` renders `deck[0]`; the deterministic deck inspector/test separately identifies index 0 as the trump source and the final element as the next `pop()` draw (`EmpiresEndgame.vue` `trumpCardView`; `qa.ts` `inspectEmpiresQaDeck`; `qa.spec.ts`).

### M60. Legacy empire saves cannot release card flags already folded into persistent state
- **Expected:** a schema-v1 save made before M58 still removes its active held-card flags when the restored empire/event phase ends.
- **Actual before the fix:** the migration filled a missing `cardFlagBonuses` field with `{}`. Those old saves already contained the current hand's flag values in `empire.flags`, so the later cleanup had no ownership record to subtract and made the bonuses permanent (`engine.ts` pre-fix `validateAndCloneSnapshot`).
- **Fixed:** 2026-07-14 — restoration reconstructs the contribution from each held card's current side and level during empire/event phases without applying it a second time. A regression restores both phase shapes and proves event resolution returns the flag to its permanent baseline (`engine.ts` `heldCardFlagBonuses`/`validateAndCloneSnapshot`; `engine.spec.ts`).

### M61. QA controls can overwrite or delete a real local campaign
- **Expected:** `?qa=1` is an isolated test stand; reseeding, importing, starting over and exercising the constructor must not touch the player's production campaign or custom definition.
- **Actual before the fix:** subscription autosave was guarded, but successful save import still wrote the production campaign, New Campaign cleared it, and constructor save/reset wrote or deleted production configuration/storage (`pages/EmpiresEndgame.vue` pre-fix `importSave`/`startNewCampaign`/`saveEditor`/`resetEditor`).
- **Fixed:** 2026-07-14 — all four paths are explicitly ephemeral in QA mode, use QA-specific confirmation/status text, and constructor reset loads the bundled definition directly without clearing local storage. Cypress plants byte-identical campaign/config sentinels, executes every mutation including a valid import, and verifies both survive (`config.ts` `loadBundledEmpiresConfig`; `EmpiresEndgame.vue`; `cypress/e2e/empires-endgame.cy.ts`).

### M62. Recruited soldiers remain in the civilian population and workforce
- **Expected:** every unit's population cost consumes ready military population and the corresponding people/classes from the city; food safety must reflect the smaller workforce and the new army.
- **Actual before the fix:** recruitment decremented only `militaryPopulation`. Total/class population, civilian food demand and available workers stayed unchanged while army upkeep was added, counting the same people on both sides of the economy (`engine.ts` pre-fix `recruitUnits`).
- **Fixed:** 2026-07-14 — recruitment proportionally consumes only configured `canRecruit` classes (with a classless fallback), total population and the ready pool. Validation projects the post-recruit population, operational-building shutdowns, production, civilian consumption and army upkeep before spending resources. Focused tests cover exact class deductions and a farm that becomes food-unsafe after its workers enlist (`engine.ts` `consumeRecruitmentPopulation`/`recruitUnits`; `engine.spec.ts`).

### M63. The browser harness can run Cypress against a stale server
- **Expected:** the self-starting QA command proves that the Vite process it launched owns the requested port before browser tests begin.
- **Actual before the fix:** readiness checked `curl` before child liveness. If another process already answered on the port, the request succeeded while strict-port Vite was still starting and about to exit, so Cypress targeted the unrelated service (`tools/test-empires-endgame.sh` pre-fix readiness loop).
- **Fixed:** 2026-07-14 — readiness now requires Vite's own `Local:` marker, a successful request and a live launched PID. An occupied-port reproduction exits 1 with Vite's strict-port error instead of starting Cypress (`tools/test-empires-endgame.sh`).

### M64. Position-less custom technologies collapse on the pixel tree
- **Expected:** `position` remains optional in a valid definition; omitted nodes receive reachable, non-overlapping world-pixel coordinates.
- **Actual before the fix:** fallback values retained the former percentage-scale spacing (roughly 10–100) after the tree moved to pixels, so custom nodes overlapped one another and authored nodes (`pages/EmpiresEndgame.vue` pre-fix `technologyNodes`; `types.ts` optional `position`).
- **Fixed:** 2026-07-14 — missing nodes use a 210×88-pixel tier grid beginning 250 pixels to the right of the greatest authored X coordinate. A mixed 62-node browser fixture removes half the positions and pairwise-checks every rendered node rectangle (`EmpiresEndgame.vue` `technologyNodes`; `cypress/e2e/empires-endgame.cy.ts`).

## Production runtime errors (2026-07-15)

### M65. A partial fight-log row crashes every Discord fight-page render
- **Expected:** a timed DM refresh tolerates any current log snapshot; only a complete fight row containing both `<:war:...>` and `⟶` is reordered, while an incomplete/malformed row is displayed unchanged.
- **Actual before the fix:** `HandleIsNewPlayerDescription` checked that the war marker produced two combatant parts, then unconditionally read `fightLineSplit[1]`. A row such as `attacker <:war:...> defender` therefore threw IndexOutOfRangeException; continuing the inner player loop after removing a completed row was a sibling out-of-range risk. Production Game #5, round 10 stopped updating its Discord page from the 30-second timer (`GameUpdateMess.cs:1075-1136`; stack at pre-fix line 1106).
- **Failure scenario:** the renderer observes or retains a fight header before its result suffix is appended → `FightPage` throws before the embed can be sent, and every later refresh seeing that row repeats the failure.
- **Fixed:** 2026-07-15 — fight-row normalization now requires both parsed separators and breaks immediately after consuming a complete row. Partial rows remain in their original position and the rest of `FightPage` renders (`GameUpdateMess.HandleIsNewPlayerDescription`).

### M66. Gordon Auto Move can stop after one rejected fallback target and receive an illegal Block
- **Expected:** because Halflife 3 permanently replaces Gordon's Block, bot/Auto Move may announce it once; afterward a Block decision must try legal attacks and Skip only when no enemy can be attacked.
- **Actual before the fix:** Gordon's `HandleAttack(..., -10)` adapter selected only the first living, non-team candidate and returned that attack's rejection. On round 10 a first candidate such as banned Тигр was absent from the fair AI target pool but still selected by this separate fallback. `HandleFairBotAttack` then returned as though the Block choice succeeded, leaving no attack, Block or Skip; the final CRIT guard logged `didn't do anything and auto move didn't as well` and assigned a real Block, contradicting Halflife 3 (`GameReactions.cs:746-782`; `BotsBehavior.cs:3590-3639`; `CheckIfReady.cs:1507-1520`).
- **Failure scenario:** round-10 Gordon in first place uses Auto Move while the first fallback candidate is Тигр with the public ban → fair AI elects defense, the sole fallback attack is rejected, and Gordon receives the generic emergency Block.
- **Fixed:** 2026-07-15 — the Gordon adapter now tries fallback candidates in leaderboard order until the authoritative attack handler accepts one. Only exhaustion clears the queue and commits Skip; no path from the missing Block action can leave Gordon actionless or blocked (`GameReactions.HandleAttack`).

## 99 Last Chances browser-game triage (2026-07-15)

### M67. `holdThenDoubleTap` required three presses and exposed no live timing feedback
- **Expected:** the GDD/Discord gesture «Задержка + двойное нажатие» is one hold/release followed by one timely repeat tap — two presses total — and the HUD shows whether the player is holding, inside the repeat window or completing the follow-up.
- **Actual before the fix:** after the qualifying hold, the recognizer treated the first repeat as an intermediate step and waited for a second short tap, requiring three presses total. The HUD showed only cooldowns and the eventual attack, so a missed window looked identical to an ignored input (`features/last-chances/gestures.ts` `LastChancesGestureRecognizer`; `components/last-chances/WeaponCooldowns.vue`).
- **Failure scenario:** hold and release either attack button, then tap once inside the configured window → no `holdThenDoubleTap` attack fires and there is no visible indication that another press is being awaited.
- **Fixed:** 2026-07-15 — one timely short press after the hold now resolves `holdThenDoubleTap`; expiry still falls back to the ordinary `hold`. Per-hand recognizer snapshots expose press, double-tap and hold-follow-up phases with remaining-time/progress feedback in the combat HUD, and focused tests cover completion, expiry and timing boundaries (`gestures.ts` `LastChancesGestureRecognizer`; `WeaponCooldowns.vue`; `gestures.spec.ts`).

### M68. 99LC was not operable from a Bluetooth DualSense
- **Expected:** a controller-only player can choose a route and play with the GDD bindings Square = left attack and Cross = right attack; standard and raw Bluetooth Sony reports are normalized and connection state is visible.
- **Actual before the fix:** the opening route map was pointer-only, the defaults were buttons 0/1 (Cross/Circle in the standard mapping), and the engine read only the first connected pad through fixed standard axes/buttons. Browsers exposing an unmapped Sony report use different face-button ordering and a different right-stick axis, while the UI exposed no connected pad, mapping profile or diagnostic state (`features/last-chances/engine.ts` pre-fix `pollGamepad`; `public/99lc/game-config.json`; `components/last-chances/RunMapOverlay.vue`).
- **Failure scenario:** connect a DualSense over Bluetooth and open `/99lc` without a mouse → no controller action enters the first room; after pointer entry, Square/Cross may trigger the wrong hand or no useful action.
- **Fixed:** 2026-07-15 — a testable gamepad adapter arbitrates the active pad, handles standard reports plus known raw Sony/DualSense face-button and right-stick layouts, and publishes connection/profile status. Defaults are Square/button 2 for the left hand and Cross/button 0 for the right; controller navigation selects and enters available route nodes, then combat feeds the normalized buttons into the same gesture recognizers as pointer/touch input (`features/last-chances/gamepad.ts` `createLastChancesGamepadAdapter`; `engine.ts` `pollGamepad`; `RunMapOverlay.vue`; `gamepad.spec.ts`).

### M69. 99LC's basic attack is a cooldown action, not the persistent tap combo in the mechanics GDD
- **Expected:** each hand's ordinary tap has no cooldown and continues its basic combo when the same button is tapped again inside the weapon's timing window. Using a special attack or briefly losing control must not erase a still-live chain.
- **Actual before the fix:** the recognizer emitted one generic `tap`, the engine tracked no combo step/window, and `performAttack` applied the same cooldown path used by every special. The shipped spear and knife taps had 280 ms and 210 ms cooldowns respectively (`features/last-chances/gestures.ts` pre-fix `LastChancesGestureRecognizer`; `engine.ts` pre-fix `performAttack`; `public/99lc/game-config.json`).
- **Failure scenario:** perform correctly spaced ordinary taps around another hand's skill → every accepted input repeats an untracked single strike, and a tap inside the authored cooldown is silently discarded instead of extending the basic chain.
- **Fixed:** 2026-07-15 — `tap` bypasses the special-attack cooldown path and advances an independent per-hand combo until the externally authored `input.tapComboWindowMs` expires. Other-hand specials and temporary pauses do not clear the chain; room/attempt resets do. The default spear and knife tap cooldowns are now zero and regression coverage proves continuation, expiry and special-use isolation (`engine.ts` `advanceTapCombo`/`performAttack`; `public/99lc/game-config.json`; `engine.spec.ts`).

### M70. 99LC configuration cannot represent the GDD weapon/equipment categories
- **Expected:** the data model can author two-handed weapons (ten attacks across both inputs), ordinary one-handed weapons usable in either slot including duplicate copies, primary-only and secondary-only weapons, and hybrid weapons that use both inputs only while the secondary slot is empty.
- **Actual before the fix:** a weapon carried one fixed `left|right` hand, validation required exactly two definitions with unique hands, and the engine converted that pair directly into a hand map. No valid JSON could express a catalog, loadout choice, duplicate, both-hand attack set, slot restriction or hybrid transition (`features/last-chances/types.ts` pre-fix `LastChancesWeaponDefinition`; `config.ts` pre-fix `validateWeapons`; `engine.ts` constructor).
- **Failure scenario:** add a second dagger, a two-handed bow or a hybrid spear in the Builder JSON → validation rejects the definition or the extra weapon is not an equippable runtime choice.
- **Fixed:** 2026-07-15 — definitions are now a weapon catalog with an explicit loadout and `twoHanded`, `eitherHand`, `primaryOnly`, `secondaryOnly` and `hybrid` equip modes. Two-handed/hybrid entries provide a second five-gesture attack set; either-hand entries can be selected in either slot or duplicated by ID; hybrid resolution switches automatically when a secondary is present. Valid legacy definitions still resolve by their old `hand` fields, and the previously shipped v1 definition is repaired before validation by M77. The engine and HUD consume the same resolved loadout (`types.ts` `LastChancesWeaponDefinition`/`LastChancesLoadoutDefinition`; `equipment.ts` `resolveLastChancesLoadout`; `config.ts` `validateWeapons`; `LastChances.vue`; `engine.ts`).

### M71. Shipped 99LC enemy spawns can be embedded in solid obstacles
- **Expected:** every validated spawn is inside its room's navigable floor and outside every obstacle, so a generated enemy can turn, notice, chase and attack.
- **Actual before the fix:** room validation checked only non-negative coordinates. The default `chest-gallery` points `(730,160)`, `(720,550)` and `(425,345)` intersect authored obstacles once enemy radius is included, as do `wrong-shadow-event` points `(570,595)` and `(420,345)`; `moveCircle` then rejects movement axes and line-of-sight may intersect the containing obstacle (`public/99lc/game-config.json`; `features/last-chances/config.ts` pre-fix `validateRooms`; `engine.ts` `moveCircle`/`enemyCanSeePlayer`).
- **Failure scenario:** a seeded node selects one of those spawn points → the enemy begins inside collision geometry and may remain trapped and unaware for the entire room.
- **Fixed:** 2026-07-15 — configuration validation now checks player and enemy circles against room bounds and obstacles, using the largest eligible enemy radius for each room, and rejects out-of-room obstacles. All five invalid default points and adjacent unsafe formation points were moved onto navigable floor; focused invalid-geometry fixtures cover obstacle and boundary failures (`config.ts` `validateSpawnGeometry`; `public/99lc/game-config.json`; `config.spec.ts`).

### M72. `graph.choicesPerNode` is exceeded while repairing route coverage
- **Expected:** every next-tier room has at least one incoming route without any current node exposing more than the configured maximum number of choices.
- **Actual before the fix:** `connectTiers` first sliced each shuffled list to the configured limit, then appended uncovered next nodes without checking remaining capacity. With the shipped limit of two, generation 1 includes nodes with three outgoing choices (`features/last-chances/plan.ts` pre-fix `connectTiers`).
- **Failure scenario:** designers set a two-choice route graph and test a generated run → some route overlays offer three rooms, so the external tuning value is not authoritative.
- **Fixed:** 2026-07-15 — generation assigns one incoming edge to every next-tier node before filling each parent only up to its authored cap. Validation rejects impossible tier/capacity combinations, and deterministic-plan tests assert both complete incoming coverage and the maximum outgoing degree (`plan.ts` `connectTiers`; `config.ts` `validateGraphCapacity`; `config.spec.ts`).

### M73. The 99LC Builder's two apply actions both reset play and one skips to generation 2
- **Expected:** Builder actions state their interruption semantics precisely; a persistence-only action leaves the active attempt untouched, while an immediate apply explicitly starts a new attempt from the edited definition. Gesture names must also describe the one post-hold repeat tap implemented by M67.
- **Actual before the fix:** both “Apply live” and “Apply & restart” destroyed/recreated the engine, clearing Chances, erosion and route progress. The restart flag then called `newGeneration()` on the fresh engine, advancing directly from generation 1 to generation 2. Builder, page and cooldown copy still labelled the two-press hold-follow-up as “Hold + double tap” (`pages/LastChances.vue` pre-fix `createEngine`/`applyBuilder`; `components/last-chances/BuilderDrawer.vue`; `WeaponCooldowns.vue`).
- **Failure scenario:** tune a visual or attack value mid-room and choose “Apply live” → the attempt is discarded despite the label; choose the other action → it is also discarded but begins from an unexpectedly advanced seed.
- **Fixed:** 2026-07-15 — “Save browser override” persists the validated definition without touching the active engine, room, Chances or erosion. “Apply & start fresh generation” explicitly replaces the attempt and starts the edited definition at generation 1. English/Russian gesture copy now says one post-hold tap, and the browser regression verifies both persistence and interruption semantics (`BuilderDrawer.vue`; `LastChances.vue` `createEngine`/`applyBuilder`; `WeaponCooldowns.vue`; `cypress/e2e/last-chances.cy.ts`).

### M74. 99LC has a flat spawn-point pool instead of the prototype brief's seeded layout templates
- **Expected:** every room authors several coherent enemy-spawn layouts; generation chooses one layout randomly but stores that seeded choice in the immutable plan, and death retries reproduce it exactly.
- **Actual before the fix:** a room exposed one flat `enemySpawns` array. `makeEnemyPlan` shuffled individual points for every node and, when enemy count exceeded the pool, jittered/clamped duplicate points; there was no layout identity or way to author/select a whole arrangement (`features/last-chances/types.ts` pre-fix `LastChancesRoomTemplate`; `plan.ts` pre-fix `makeEnemyPlan`/`makeNode`; `public/99lc/game-config.json`).
- **Failure scenario:** author two intentional formations for a room and regenerate the run → the schema cannot represent either formation boundary, the plan mixes individual points instead of choosing one, and over-cap duplicates may be pushed into walls even when every authored point validates.
- **Fixed:** 2026-07-15 — schema-v2 rooms now require at least two named `spawnLayouts`; each planned node selects one layout from its seed, records `spawnLayoutId`, and places enemies only on that layout's exact authored points. Validation rejects a layout that cannot hold the maximum eligible tier count, an enemy circle outside the room or inside an obstacle, and pairwise overlap at the room's largest eligible enemy radius. Every shipped room has two safe formations, while schema-v1 `enemySpawns` remains a validated legacy fallback (`types.ts` `LastChancesSpawnLayoutDefinition`/`LastChancesPlanNode`; `plan.ts` `roomSpawnLayouts`/`makeNode`; `config.ts` `validateSpawnGeometry`; `public/99lc/game-config.json`; `config.spec.ts`).

### M75. 99LC's live melee/burst hitbox radius and duration controls do not affect collision
- **Expected:** the prototype's externally editable attack hitbox size and attack duration change the real collision window, so tuning them in the Builder produces a testable combat difference.
- **Actual before the fix:** melee and burst executors damaged every target immediately when the gesture resolved. Melee used only range/arc and burst used only range; both ignored `attack.radius`, while `durationMs` controlled only the drawn effect even though the client documentation described it as active time (`features/last-chances/engine.ts` pre-fix `performMelee`/`performBurst`/`addEffect`; `components/last-chances/BuilderDrawer.vue`; `docs/WEB-CLIENT.md` §12A).
- **Failure scenario:** increase a melee attack's radius and active duration in Quick Tune, then let an enemy enter the visible swing before it fades → collision is identical to the old values and the later overlap never damages it.
- **Fixed:** 2026-07-15 — melee and burst gestures create a cast-origin active area for the authored `durationMs`, test overlap throughout that window, include the attack and enemy radii in reach, and damage each target at most once. Melee also preserves its authored facing arc; burst knockback remains radial. Active areas are cleared on room completion, death, retry, generation replacement and engine destruction, and focused tests cover late entry, expiry, one-hit behavior and radius padding (`engine.ts` `startActiveArea`/`updateActiveAreas`/`applyActiveAreaHits`; `engine.spec.ts`).

### M76. Meaningful enemy movement tuning remains compiled into the 99LC engine
- **Expected:** values designers are likely to tune during prototype playtests live in the external definition and Builder, including how quickly an idle enemy scans and how close it tries to stand before attacking.
- **Actual before the fix:** idle facing advanced by the literal `0.28` radians/second and chase stopped at the literal `0.72 × attackRange`; neither value existed in the enemy schema or editor despite the prototype brief's explicit no-recompile tuning contract (`features/last-chances/engine.ts` pre-fix `updateEnemies`; `types.ts` `LastChancesEnemyDefinition`; `components/last-chances/BuilderDrawer.vue`).
- **Failure scenario:** a playtest shows an enemy circling too slowly or crowding too close → changing every exposed enemy value cannot reproduce the desired behavior; a TypeScript edit and SPA rebuild are required.
- **Fixed:** 2026-07-15 — every schema-v2 enemy now authors `idleTurnRadiansPerSecond` and `preferredAttackRangeRatio` in the runtime JSON; validation constrains both, Quick Tune exposes both, and `updateEnemies` consumes them directly. Schema-v1 definitions retain the old `0.28`/`0.72` defaults for compatibility (`types.ts` `LastChancesEnemyDefinition`; `config.ts` `validateEnemies`; `engine.ts` `updateEnemies`; `BuilderDrawer.vue`; `public/99lc/game-config.json`; `config.spec.ts`).

### M77. The schema-v2 rollout strands real schema-v1 browser overrides
- **Expected:** a browser override saved from the previously deployed 99LC prototype is migrated into a safe current-runtime definition and remains editable; compatibility coverage uses the actual old definition rather than a synthetic v1-shaped copy of new data.
- **Actual before the fix:** validation accepted schema version 1 in name but applied the new zero-tap-cooldown and spawn-geometry invariants before any migration. The previously shipped 280/210 ms tap cooldowns and five obstacle-overlapping spawn points therefore made a real saved override unloadable, while the compatibility test derived its fixture from already-safe schema-v2 data (`features/last-chances/config.ts` interim `validateAttackSet`/`validateSpawnGeometry`/`loadLastChancesConfig`; `config.spec.ts` interim schema-v1 test; previous `public/99lc/game-config.json`).
- **Failure scenario:** open `/99lc` after saving a browser override on the prior deployment → loading stops at the invalid-definition screen until the player discards all local tuning.
- **Fixed:** 2026-07-15 — `migrateLastChancesConfig` deep-clones schema-v1 input, zeros every basic-tap cooldown, and relocates the exact five unsafe points from the previously shipped rooms before strict validation; schema v2 is never rewritten. Browser load/save, direct engine construction and Builder JSON validation/import all use that migration path. The regression fixture reconstructs the prior hand-slot definition, 280/210 ms taps and exact flat spawn pools, then proves the migrated result validates without fetching or discarding the override (`config.ts` `LEGACY_SPAWN_RELOCATIONS`/`migrateLastChancesConfig`/`assertValidConfig`; `engine.ts` constructor; `BuilderDrawer.vue` `parseRaw`; `config.spec.ts` `previousShippedSchemaV1Config`).

### M78. Sector-shaped 99LC burst attacks damage targets behind the player
- **Expected:** any attack that authors an arc below 360 degrees respects that facing sector in collision, including the spear's 150-degree `Широкий толчок древком`.
- **Actual before the fix:** active-area angular filtering ran only when `kind === 'melee'`; every burst was treated as a full circle even though the schema and shipped secondary spear move authored narrower arcs (`features/last-chances/engine.ts` interim `applyActiveAreaHits`; `public/99lc/game-config.json` `Широкий толчок древком`).
- **Failure scenario:** use the forward staff shove with an enemy directly behind the player but inside its radius → the unseen rear target takes damage and knockback.
- **Fixed:** 2026-07-15 — every active area with `arcDegrees < 360` now applies the same facing-dot-product gate used by melee. This matches the existing sector renderer and a regression places equal targets in front of and behind a 150-degree burst, proving that only the forward target is damaged (`engine.ts` `applyActiveAreaHits`/`renderEffect`; `engine.spec.ts`).

### M79. 99LC spawn validation allows an enemy to start on top of the player
- **Expected:** every authored formation keeps enemy circles separated from the room's player spawn by at least the sum of their validated radii.
- **Actual before the fix:** player and enemy points were each checked against bounds/obstacles and enemy points against each other, but the two collections were never compared (`features/last-chances/config.ts` interim `validateSpawnGeometry`).
- **Failure scenario:** set a layout point equal to `playerSpawn` in Builder JSON → validation succeeds and combat begins with overlapping actors.
- **Fixed:** 2026-07-15 — every legacy or named layout point is now compared with `playerSpawn` using `player.radius + largest eligible enemy radius`; overlap is rejected before a plan can be built. The focused fixture puts an enemy exactly on the player and asserts the combined-radius validation error (`config.ts` `validateSpawnGeometry`; `config.spec.ts`).

### M80. 99LC burst collision reaches full size before its visible wave
- **Expected:** a duration-based expanding burst damages a distant target when the rendered wave reaches it, so visual timing and live collision describe the same attack.
- **Actual before the fix:** `startActiveArea` tested the full `range + radius` at cast time and every later frame, while `renderEffects` drew burst radius from zero to full size across `durationMs` (`features/last-chances/engine.ts` interim `startActiveArea`/`applyActiveAreaHits`/`renderEffects`).
- **Failure scenario:** cast a long-duration burst at an enemy near its outer edge → damage lands immediately while the visible wave is still at the player's feet.
- **Fixed:** 2026-07-15 — active bursts retain their total and remaining duration, and collision reach expands by the same normalized progress as the rendered wave; a zero-duration burst remains an intentional instant full-reach attack. The regression proves an outer target is untouched at cast and mid-progress, then hit when the wave completes (`engine.ts` `startActiveArea`/`updateActiveAreas`/`applyActiveAreaHits`/`renderEffect`; `engine.spec.ts`).

### M81. 99LC ignores authored pierce limits outside projectile attacks
- **Expected:** the shared `pierce` field means the number of additional targets an attack may damage, so `0` hits one target, `1` hits two, and the deliberately high values on area/dash moves permit their authored crowds.
- **Actual before the fix:** projectiles tracked `pierce + 1` remaining hits, but active melee/burst areas and dashes damaged every overlapping enemy regardless of the same field. Quick Tune exposed `pierce` for every executor and shipped non-projectile attacks authored values from 0 through 8 (`features/last-chances/engine.ts` interim `RuntimeActiveArea`/`RuntimeDash`/`performProjectile`; `public/99lc/game-config.json`; `components/last-chances/BuilderDrawer.vue`).
- **Failure scenario:** line up several enemies in the spear's `pierce: 0` basic slash or a low-pierce dash → all of them take damage, so editing the external value has no effect.
- **Fixed:** 2026-07-15 — dashes and active melee/burst areas now initialize `remainingHits = pierce + 1`, decrement only on a new damaged target, and stop applying damage when exhausted; exhausted areas are removed while a dash still completes its authored travel. Focused three-target tests prove a `pierce: 1` area hits two and a `pierce: 0` dash hits one without stopping movement (`engine.ts` `RuntimeDash`/`RuntimeActiveArea`/`updatePlayer`/`startActiveArea`/`applyActiveAreaHits`; `engine.spec.ts`).

### M82. Schema-v1 migration can move a safe customized spawn into a customized obstacle
- **Expected:** the narrow repair for previously shipped unsafe coordinates inspects the override's current circle geometry; it moves a point only when the original is unsafe and the known replacement is safe.
- **Actual before the fix:** `migrateLastChancesConfig` matched only room ID plus exact point coordinates. A user who had moved the room's obstacles could make an old point safe or its replacement unsafe, but migration still rewrote it before validation (`features/last-chances/config.ts` interim `LEGACY_SPAWN_RELOCATIONS`/`migrateLastChancesConfig`).
- **Failure scenario:** in the old `chest-gallery`, move obstacle 1 to `{x:775,y:130,width:20,height:60}` → `(730,160)` becomes safe, yet migration changes it to `(780,160)` inside the customized obstacle and rejects the override.
- **Fixed:** 2026-07-15 — migration now derives the largest enemy radius eligible for each legacy room and tests the original and proposed replacement circles against the override's current bounds, obstacles and player spawn. A known point is moved only when the original is unsafe and the replacement is safe; schema v2 remains untouched. The exact customized-obstacle reproduction keeps `(730,160)`, rejects `(780,160)`, still repairs the other unsafe shipped points, and validates (`config.ts` `eligibleEnemySpawnRadii`/`spawnFitsRoomGeometry`/`migrateLastChancesConfig`; `config.spec.ts`).

### M83. Empire's Endgame consumes city-targeted gifts without asking for a target
- **Expected:** accepting a gift whose authored effect names a selected city pauses resolution, presents only legal accessible cities, persists that pending choice in the save, and applies the resources to the chosen city's economy before the empire phase begins.
- **Actual before the fix:** `chooseGift` immediately appended the gift and called `startEmpirePhase`; `EmpiresEffect.resource` could mutate only the empire-global ledger. `gift-resource-grant` therefore gave wood and iron everywhere, left its food in a gameplay-dead global stock, and offered no city choice (`features/empires-endgame/engine.ts` `chooseGift`/`applyEffects`; `types.ts` `EmpiresEffect`/`EmpiresCityState`; `pages/EmpiresEndgame.vue` `chooseGift`).
- **Failure scenario:** accept «Ресурсы в выбранный город» while two cities have different construction needs → the phase advances immediately, both cities can spend the same global grant, and no target was ever selected.
- **Fix direction:** add a serialized pending-resolution contract, city stockpiles that participate in local construction/recruitment and starvation, an accessible target picker, deterministic save restoration, and QA/browser coverage.
- **Fixed:** 2026-07-16 — `EmpiresPendingGiftResolution` keeps the accepted gift in `divineGift` until `resolvePendingTarget` receives a currently accessible city; restoration rebuilds legal targets from trusted config rather than the saved list. Resource effects enter that city's ledger, which local construction/recruitment spend before the empire reserve; city then shared empire food covers settlement deficits. The target dialog, named QA fixture and Cypress flow prove the choice persists and other city ledgers remain unchanged (`types.ts` `EmpiresPendingGiftResolution`; `engine.ts` `chooseGift`/`resolvePendingTarget`/`resourcePaymentPlan`/`finishEmpireInternal`; `TargetResolutionDialog.vue`; `qa.ts`; `cypress/e2e/empires-endgame.cy.ts`).

### M84. Empire's Endgame region-sacrifice gifts leave the destroyed region fully playable
- **Expected:** «Предать Запад огню» and the equivalent North/South/East rewards permanently remove access to that region: its cities cannot produce, construct, recruit, receive targets, or provide regional services.
- **Actual before the fix:** each reward only incremented an unconsumed `destroyWest`/`destroyNorth`/`destroySouth`/`destroyEast` flag. The map, city selector, production, population totals and every empire action ignored those flags (`public/empires-endgame/game-config.json`; `engine.ts` `productionForCity`/`checkEmpireAction`/`totalPopulation`; `pages/EmpiresEndgame.vue` `mapRegionViews`/`cityViews`).
- **Failure scenario:** accept «Предать Запад огню», then open a western city and upgrade its farm → the supposedly annihilated region still produces and accepts orders.
- **Fix direction:** persist authoritative region status, enforce it in every city/production/target boundary, and render the loss explicitly. Preserve the city records for save/history because the export does not specify inventory conversion.
- **Fixed:** 2026-07-16 — region-sacrifice gifts write `destroyedRegionIds`; `isRegionAccessible`/`isCityAccessible` gate targeting, production, construction, recruitment, boosts, population totals and settlement without deleting historical city records. The map keeps a lost region inspectable, marks its cities unavailable and disables them in the city selector; schema-v1 saves reconstruct the state from old sacrifice flags or claimed gifts (`engine.ts` `applyFixedGiftResolution`/`isRegionAccessibleInState`/`migrateDestroyedRegions`; `EmpireMap.vue`; `EmpiresEndgame.vue`; `engine.spec.ts`; `cypress/e2e/empires-endgame.cy.ts`).

### M85. The farm/lumber relic records flags but changes neither current nor maximum building level
- **Expected:** «Реликвия плодородия и леса» raises every placed farm and lumbermill by one effective current level and raises their effective maximum by one, including production and workforce demand.
- **Actual before the fix:** the gift only added `farmLevelBonus` and `lumberLevelBonus`; building lookup, production, worker shutdown, upgrade availability and UI maximums read the unmodified configured/raw levels (`game-config.json`; `engine.ts` `upgradeBuilding`/`productionForCity`/`workerDemand`; `pages/EmpiresEndgame.vue` `cityViews`).
- **Failure scenario:** accept the relic with a level-3 farm and level-2 lumbermill → both still display and produce at 3/3 and 2/3.
- **Fix direction:** make the level bonus an executable domain effect, use exact configured next levels when present and deterministic extrapolation above the authored cap, and test both effective production and UI levels.
- **Fixed:** 2026-07-16 — the relic writes typed `buildingLevelBonuses` for farm/lumber slots. `effectiveBuildingLevel` and `effectiveBuildingMaxLevel` feed production, worker demand, dependencies and the city display; `buildingLevelDefinitionAt` uses an authored level when present and deterministically extrapolates the last per-level delta beyond the catalog. Raw purchased levels remain intact for save/editor ownership, and migration reconstructs the bonus from old flags or gift history (`types.ts` `EmpiresGiftResolution`; `engine.ts` `applyFixedGiftResolution`/`effectiveBuildingLevel`/`buildingLevelDefinitionAt`/`migrateBuildingLevelBonuses`; `CityView.vue`; `engine.spec.ts`).

### M86. The meteor reward has neither target selection nor a meteor impact
- **Expected:** accepting the meteor pauses for a legal city/territory target; resolving it consumes the reward once, applies a deterministic configurable impact, grants self-target meteorite iron/radiation consequences, and then continues the campaign.
- **Actual before the fix:** `gift-meteor` only incremented the unconsumed `meteorCharges` flag. There was no target state, action, charge consumption, building/city damage, insurance hook or radiation consequence (`game-config.json`; `engine.ts` `chooseGift`/`applyEffects`; `pages/EmpiresEndgame.vue`).
- **Failure scenario:** accept «Метеорит» → the empire phase starts with `meteorCharges: 1`, but no control ever lets the player choose a city or use it.
- **Fix direction:** implement a persisted city-target resolution. The authoritative export does not specify meteor damage, so keep the damage amount configurable; the bundled default may use the older non-conflicting note's one-building-level impact rather than hard-code an undocumented universal rule.
- **Fixed:** 2026-07-16 — the meteor uses the same persisted accessible-city target contract, then `damageCityWithMeteor` deterministically selects the highest raw-level building (ID tie-break), removes the configured `damageLevels`, and locks that building for the current con. Its iron enters only the selected city. The bundled value is the configurable one-level impact from the older non-conflicting note, not a new universal rule; the separate meteorite-iron/radiation outcome stays explicitly deferred because the export does not define an executable radiation lifecycle (`types.ts` `EmpiresGiftResolution`; `game-config.json` `gift-meteor`/`gift-meteor-iron`; `engine.ts` `applyTargetedGiftResolution`/`damageCityWithMeteor`; `TargetResolutionDialog.vue`; `engine.spec.ts`; `cypress/e2e/empires-endgame.cy.ts`).

### M87. Reversed military recruiters omit the authored unit loss, downgrade and one-turn arson lock
- **Expected:** when reversed «Зазывалы в войска» is held, every accessible city loses one deterministic random unit, its military building loses one level, that building cannot be interacted with or repaired for the phase, and recruitment is disabled.
- **Actual before the fix:** only `recruitmentDisabled` had a consumer. `militaryArson` was stored as a temporary flag but never removed a unit, damaged a barracks or blocked building interaction (`game-config.json`; `engine.ts` `startEmpirePhase`/`recruitUnits`/`upgradeBuilding`).
- **Failure scenario:** enter an empire phase holding the reversed card in a city with recruited units and a level-3 barracks → recruitment is blocked, but all units remain and the barracks stays level 3 and upgradeable.
- **Fix direction:** resolve the city effects once at empire start with the seeded RNG, persist the downgrade and phase lock, and migrate old saves with no locks.
- **Fixed:** 2026-07-16 — the executor now applies `militaryArson` once to every accessible city: the serialized RNG chooses one present unit kind and removes one unit, the highest barracks loses one raw level, and `buildingInteractionLocks` blocks placement, repair/upgrade and production-boost interaction for that con while `recruitmentDisabled` blocks orders globally. Snapshot migration initializes locks and reconstructs the once-only transition for affected saves. Because the bundled army, Barracks and recruitment payoff are themselves outside the playable campaign, both Hearts-7 faces are explicitly deferred there instead of advertising an effect on disabled content; constructor scenarios that make those systems live use the completed executor (`engine.ts` `startEmpirePhase`/`applyMilitaryArsonToState`/`isBuildingInteractionLocked`/`validateAndCloneSnapshot`; `game-config.json` `card-hearts-7`; `engine.spec.ts`).

### M88. Empire's Endgame progression unlocks and per-branch research limits are not enforced
- **Expected:** Fair, Temple, Smithy and Bank construction require their authored unlock technologies; a city may use the relic exemptions for Smithy iron and Stable livestock; each empire phase permits at most one technology/steel node per branch and one reform/doctrine per branch.
- **Actual before the fix:** the first levels of those buildings omitted their unlock prerequisites, the exemption flags had no consumer, and `research` allowed any number of nodes from the same `groupId` in one phase as long as time/resources remained (`game-config.json`; `engine.ts` `placeBuilding`/`checkEmpireAction`/`research`).
- **Failure scenario:** place a Fair before «Зарождение торговли», build a Smithy without «Изделия из железа», or research two Science technologies in the same empire phase → every action succeeds.
- **Fix direction:** repair the bundled dependencies, consume the two construction-exemption flags narrowly, serialize per-phase branch usage, and expose the limit in availability/error text.
- **Fixed:** 2026-07-16 — the bundled Fair, Temple, Smithy and Bank level-one definitions carry their technology dependencies. The engine consumes Smithy iron and Stable livestock exemptions only in their matching construction checks/costs; the bundled exemption relic is deferred together with those two deferred facilities, so it cannot be drafted as a current-campaign no-op. `researchUsage` serializes one technology/steel selection per `groupId` and one reform/doctrine selection per group for the current empire phase, resets at the next phase, and is mirrored in Development availability text. `giftIsUnlocked` keeps relic-kind rewards out of the draft until `relicsUnlocked`, and validation requires three executable non-relic gifts for the pre-unlock pool (`game-config.json`; `engine.ts` `buildingDependencies`/`buildingResourceCosts`/`firstMissingDependency`/`researchUsageKey`/`research`/`giftIsUnlocked`; `EmpiresEndgame.vue` `researchUsageKey`; `config.spec.ts`; `engine.spec.ts`).

### M89. Empire's Endgame advertises purchasable mechanics that only store unread flags
- **Expected:** every card face, gift, event choice, building, technology and unit offered by the current campaign either has an executable effect or clearly states that its required system is outside the playable ruleset and cannot consume the player's resources/time.
- **Actual before the fix:** the bundled definition produces more than one hundred distinct flags, while only a handful have engine readers. Numerous technologies and buildings can spend days, food and resources for no outcome; several event choices do nothing at all; many authored card faces promise loyalty, trade, epidemic, combat, fleet or expedition effects that the runtime cannot represent. The same problem affects recruitable units: they consume population and food but have no combat or expedition payoff (`public/empires-endgame/game-config.json`; `engine.ts` `applyEffects`; `pages/EmpiresEndgame.vue`).
- **Failure scenario:** research «Компас», construct a Bank, choose «Запереть город» during the epidemic, or recruit a military unit → the cost is paid and a flag/count changes, but no playable system ever observes the result.
- **Fix direction:** audit every active definition. Implement effects whose authoritative numbers and current-loop substrate exist; add a validated deferral contract for content that requires absent combat/fleet/epidemic/diplomacy/expedition systems or unresolved design numbers, and block it in both engine and UI instead of leaving a silent no-op.
- **Fixed:** 2026-07-16 — cards/faces, gifts, resources, buildings, units, technologies and events/choices now share an explicit `deferredReason` boundary. `validateDeferredReasons` rejects empty markers and `validateLiveEffects` rejects unsupported flag effects on non-deferred content unless the engine consumes the flag or a live prerequisite references it. Drafts and engine actions cannot spend on deferred content; cards remain playable in Durak but their future face is badged and skipped in the empire phase. The bundled combat/fleet/epidemic/diplomacy/expedition systems and export-undefined faces are labelled unavailable instead of simulated by unread flags (`types.ts`; `config.ts` `validateDeferredReasons`/`validateLiveEffects`; `engine.ts` `beginDivineGift`/`placeBuilding`/`upgradeBuilding`/`recruitUnits`/`research`/`chooseEvent`/`startEmpirePhase`; `EmpireCard.vue`; `GiftDraft.vue`; `TechTree.vue`; `CityView.vue`; `config.spec.ts`; `cypress/e2e/empires-endgame.cy.ts`).

### M90. Defined current-loop progression effects are still inert
- **Expected:** the current empire phase enforces the authored barracks recruitment cap and its Hearts-7 bypass, Conservation counters the inverted famine card, Granary levels protect/discount provisions, Trade Levy converts idle buildings and surplus food into gold, Small Temples share city construction resources with the authored transfer loss, and Treasury pays its end-of-turn saved-gold return.
- **Actual before the fix:** `equippedRecruitCapacity`, `unlimitedTavernRecruitment`, `famineYearCounter`, `famineProtectionTurns`, `provisionEfficiencyPercent`, `idleBuildingGoldBase`, `surplusFoodPerGold`, `templarTransferLossPercent` and `treasuryGoldPerSavedMillion` were stored but never read, despite all required city, food, recruitment, building, research and resource ledgers existing (`game-config.json`; `engine.ts`).
- **Failure scenario:** recruit beyond the barracks cap, hold reversed «Голодный год» after researching Conservation, or finish a phase with a Trade Levy and food surplus → the result is identical to not owning the relevant card/building/technology.
- **Fix direction:** implement these effects as derived current-loop rules with deterministic accounting and focused tests; treat absolute cap/level flags as authored values rather than additively stacking every building level.
- **Fixed:** 2026-07-16 — operational building effects now drive the per-city barracks cap and Hearts-7 bypass, Conservation and Granary famine counters, Granary provision efficiency, Small-Temple cross-city payment with deterministic transfer loss, and Trade-Levy idle-building/surplus-food gold; Treasury pays once from whole saved millions at settlement. Values are read from the applicable operational/effective level instead of globally adding every level definition. Bundled combat units remain deferred because their absent combat payoff is not fabricated, but the recruitment executor enforces these rules for any non-deferred unit catalog (`engine.ts` `recruitUnits`/`operationalBuildingFlagValue`/`famineFoodMultiplier`/`foodConsumptionForCity`/`resourcePaymentPlan`/`tradeLevyGoldForCity`/`applyTreasuryIncome`; `engine.spec.ts`).

### M91. Random-event eligibility and effect timing make authored choices permanent, late, or repeatable
- **Expected:** event prerequisites include prior event outcomes, one-crisis modifiers affect the crisis they describe, and “stop this recurring problem” choices prevent that event from being selected again.
- **Actual before the fix:** `eventIsEligible` checks only static definition prerequisites. «Найти и казнить предводителя» sets `horseTheftDisabled` but horse theft remains eligible forever. «Строгие пайки» is offered only after starvation has already been resolved, then its `starvationLossMultiplierPercent` modifier persists into every later con instead of the current crisis. Several other events offer only future-system flag choices (`engine.ts` `finishEmpireInternal`/`eventIsEligible`/`chooseEvent`; `game-config.json`).
- **Failure scenario:** end a famine phase, choose strict rationing, then observe no benefit to that famine but a permanent 20% loss reduction thereafter; or eliminate the horse thieves and immediately roll the same theft again.
- **Fix direction:** enforce outcome-based event suppression, give temporary crisis effects an explicit lifecycle or defer the event until its settlement order is implemented, and exclude events whose choices depend entirely on absent systems.
- **Fixed:** 2026-07-16 — `finishEmpireInternal` calculates `uncoveredFoodDeficit` and lets the famine event win the event roll before `settleEmpireEconomy`; `EmpiresEventState.empireSettlementPending` persists that ordering across autosave. `chooseEvent` applies the ration/purchase choice, settles the same crisis, then restores/removes the temporary starvation multiplier before the next con. `eventIsEligible` excludes deferred events/choices and suppresses horse theft after `horseTheftDisabled`; schema-v1 event saves without the marker retain their historical post-settlement meaning (`types.ts` `EmpiresEventState`; `engine.ts` `finishEmpireInternal`/`uncoveredFoodDeficit`/`settleEmpireEconomy`/`chooseEvent`/`eventIsEligible`; `engine.spec.ts`).

## 99 Last Chances authored-weapon completion (2026-07-16)

### M92. The 99LC attack schema cannot express the authored seven-weapon mechanics
- **Expected:** attacks may retain exact charge duration, select release bands, channel while held, recover/cancel, use per-skill invulnerability, repeat or follow movement/aim, and expose disabled gesture slots where the brief defines no action.
- **Actual before the fix:** every gesture resolves one fixed melee/projectile/dash/burst immediately; input emits only the gesture name, `doubleTapHold` fires at the global hold threshold, all dashes grant invulnerability, and the schema has no charge bands, channels, recovery or conditional mechanics (`features/last-chances/types.ts`; `gestures.ts`; `engine.ts` `performAttack`).
- **Failure scenario:** hold the spear for an early, medium or late release → every duration produces the same fixed projectile and the runtime cannot retain which release was authored.
- **Fix direction:** introduce a charge-aware gesture lifecycle, schema-v3 attack behavior/collider/effect metadata and reusable runtime action state.
- **Fixed:** 2026-07-16 — schema v3 now carries behavior, exact collider, ordered charge bands, recovery/root/invulnerability, repeated-hit, resource, status and live-tuning metadata. Gesture resolutions retain both press durations; the engine rejects releases below the first armed band, maintains held channels, and exposes the same action state to Canvas, touch and the accessible HUD (`types.ts` `LastChancesAttackDefinition`/`LastChancesChargeDefinition`; `gestures.ts` `LastChancesGestureRecognizer`; `weapon-runtime.ts`; `engine.ts` `performAttack`/`updateHeldWeaponMechanics`/`createSnapshot`; focused config/gesture/runtime tests).

### M93. The shipped spear is not the authored two-handed spear
- **Expected:** spear attacks have an inner dead zone and edge critical zone, charge-scaled ram, three hold-release outcomes, charged spin follow-up, real parry/push/armor stance, turning contact stance and invulnerable pole vault.
- **Actual before the fix:** `primary-spear` is a generic hybrid composed only from fixed arc/projectile/dash/burst executors; it has no distance bands, wall pin/carry, charge branches or continuous stance (`public/99lc/game-config.json`; `engine.ts`).
- **Failure scenario:** hit an enemy point-blank or at the spear tip, or release the hold in different sectors → damage and behavior remain identical.
- **Fix direction:** replace it with the authored two-handed kit and make every distance/charge/control rule externally tunable.
- **Fixed:** 2026-07-16 — `twohand-spear` supplies ten authored inputs: strict body-overlap/tip-critical basics, charge-scaled ram, early wide release, medium targeted stun throw, late piercing carry-and-wall-pin throw, charged overhead spin, short-release provisional parry, range-setting shove/charge-scaled kick, aim-following contact/slice stance and traversal-only invulnerable pole vault. A successful provisional parry consumes a queued compound gesture; an unused one is canceled by that gesture. The late kick reaches the primary critical band, while carried enemies pin only at an obstacle or perimeter and are backtracked to safe geometry. Projectile/dash offsets and strict inner colliders preserve the close dead zone, and all charge/collider/impact values remain in runtime JSON (`public/99lc/game-config.json` `twohand-spear`; `colliders.ts` strict inner exclusion; `engine.ts` `beginProvisionalTapParry`/`performSpearRelease`/`pinProjectileTargets`/`dashStepColliders`/`updateHeldWeaponMechanics`; engine/collider tests).

### M94. Chain is absent from the playable 99LC catalog
- **Expected:** Chain captures a non-bleed DOT, stores it without expiry, spreads it on the next chain attack, supports parry/disarm combo, rotation-assisted repeated spin, group throw, charged hook/drag and a seven-second bind that removes the chain until recovery.
- **Actual before the fix:** no chain definition or runtime state exists; the engine has no DOT carrier, tether, bound target, attack-rate slow or temporarily unavailable weapon (`public/99lc/game-config.json`; `types.ts`; `engine.ts`).
- **Failure scenario:** equip the intended secondary weapon after applying poison → there is no chain to capture or redistribute the effect.
- **Fix direction:** add the secondary-only Chain plus persistent per-weapon DOT/tether/loss state.
- **Fixed:** 2026-07-16 — `secondary-chain` captures the strongest poison/burn/chemical DOT without a timer and consumes it on the next chain action; its parrying/disarming combo, movement-assisted second spin, group directional throw, charge-scaled hook/drag and seven-second bind are implemented. Bind is not a hidden stun: separate authored slow and attack-slow effects leave the target mobile but attacking four times less often. Binding empties the chain resource until that target dies or a boss actually loses the configured max-HP share from one hit; that recovery also applies its authored boss stun. Resource, stored-DOT and bind/status indicators remain visible in the HUD/world (`public/99lc/game-config.json` `secondary-chain`; `statuses.ts` `captureLastChancesDot`/`spreadLastChancesDot`; `engine.ts` chain branches/resource lifecycle; focused status/engine tests).

### M95. Claws are absent from the playable 99LC catalog
- **Expected:** Claws can occupy either or both hands, have no ordinary combo, bleed on every even successful slash, reward alternating dual-hand rhythm, block healing, disarm, charge a dash and convert the follow-up window into a deep critical strike.
- **Actual before the fix:** no claw definition exists and tap sequencing is only a cyclic authored combo; the runtime has no successful-hit parity, heal block, disarm or dual-hand rhythm state (`public/99lc/game-config.json`; `engine.ts` `advanceTapCombo`).
- **Failure scenario:** duplicate the intended weapon into both slots and alternate taps → no bleed parity or rhythm microstagger can occur.
- **Fix direction:** add either-hand Claws with shared successful-hit state and generic status hooks.
- **Fixed:** 2026-07-16 — `either-claws` can occupy either or both slots, keeps ordinary slashes on one non-cyclic action, bleeds every second damaging claw hit and microstaggers properly timed alternating hands. Its rend blocks bleed healing, weapon catch disarms, charged dash scales a damage-free traversal and creates the damaging scratch only at its endpoint, and timed cancellation becomes a deep critical; Fire, Chemical and Poison Symbols are behavior-scoped to the authored rend, trail and bleeding-target poison interactions. The healing-block timer freezes the bleed wound timer without suppressing its damage ticks (`public/99lc/game-config.json` `either-claws`; `equipment.ts`; `statuses.ts` `updateLastChancesStatuses`; `engine.ts` `damageEnemy`/dash landing/trail branches; status/augment/engine tests).

### M96. Knife-spider cannot be captured or used as its authored living weapon
- **Expected:** after dodging its leap, a player behind the recovering Knife-spider can interact to replace the secondary weapon; the living knife loses health through use/damage, accelerates thrusts, bleeds, can be twisted deeply or destroyed by its charged throw.
- **Actual before the fix:** Knife-spider exists only as an enemy. There is no combat interaction prompt/input, capture window, weapon durability or self-damage lifecycle (`engine.ts`; `TouchControls.vue`; `public/99lc/game-config.json`).
- **Failure scenario:** dodge the leap and stand behind the spider before it turns → no interaction is offered and the secondary slot cannot become the spider knife.
- **Fix direction:** add a deterministic rear-capture interaction, ephemeral secondary weapon state and durability feedback.
- **Fixed:** 2026-07-16 — a missed locked Knife-spider leap preserves its locked facing and creates a true rear capture window with keyboard, touch and gamepad interaction feedback. Capture replaces the secondary slot with `secondary-spider-knife`; use and successful hits consume visible durability, held thrusts accelerate, twist refreshes/amplifies bleed, and the charged throw consumes all remaining durability and removes the living weapon. Enemy Knife-spiders also pay self-damage when they attack (`engine.ts` `updateEnemies`/`capturableKnifeSpider`/`captureKnifeSpider`/enemy attack paths/spider durability branches; `TouchControls.vue`; `public/99lc/game-config.json`; focused engine/page tests).

### M97. The generic hammer does not implement the authored two-handed Axe
- **Expected:** every Axe hit hooks inward; skills cause recovery that an empowered tap cancels; the kit includes fourth-hit bleed, choke/armor tear or aimed throw, long parry, movement-assisted reflecting spin and an invulnerable momentum leap with landing impact.
- **Actual before the fix:** `twohand-hammer` only uses broad fixed damage/knockback actions and has no hook pull, recovery cancel, grabbed target, projectile reflection or spin inertia (`public/99lc/game-config.json`; `engine.ts`).
- **Failure scenario:** use a special then immediately tap, or rotate movement during the held spin → neither the recovery nor number of rotations changes.
- **Fix direction:** replace the hammer with the authored Axe and reusable recovery/grab/channel rules.
- **Fixed:** 2026-07-16 — `twohand-axe` replaces the hammer with hook pull on every hit, fourth-hit bleed, post-action recovery canceled by the next empowered combo tap, single-target choke/late armor tear, held target spin-and-aimed throw, long parry and held reflecting spin. Matching movement/aim rotation doubles its sampled revolutions and preserves the release tangent for the invulnerable leap; flight is non-damaging except for Chemical trail, while the small landing circle alone deals the massive damage/stun. Charged invulnerability covers the entire resolved flight rather than the unscaled base duration (`public/99lc/game-config.json` `twohand-axe`; `engine.ts` `latchAxeTarget`/`updateActiveAreas`/`stopHeldChannel`/delayed recovery/landing branch; engine tests).

### M98. Katana is absent from the playable 99LC catalog
- **Expected:** Katana is two-handed, refunds other cooldowns on skill hits, has execute/self-root finisher, interrupting overhead, aggressive multi-slash leap, bleed cashout/refresh, parry, invulnerable side steps, disarm and a true-damage lightning dash that resets on a kill.
- **Actual before the fix:** no Katana definition exists and the runtime lacks bleed lifecycle, true damage, cooldown-on-hit refunds, self-root, cast interruption and kill-reset hooks (`public/99lc/game-config.json`; `engine.ts`).
- **Failure scenario:** land a Katana skill while another skill is cooling down → no such weapon or cooldown flow exists.
- **Fix direction:** add the complete two-hand kit on generic status, cooldown and action-lock primitives.
- **Fixed:** 2026-07-16 — `twohand-katana` implements both five-input sets and refunds only other Katana cooldowns on skill hits. The kit includes low-health execute/self-root, unconditional cast-interrupt overhead, dodge-aware multi-hit invulnerable flight, Symbol-gated bleed cashout/doubling, timer refresh, reactable short-release parry, directional invulnerable hops, disarming iaido and armor-breaking true-damage flash that resets only its own cooldown on a kill. Dance/flight invulnerability covers the whole action and every authored critical outcome emits the common `CRIT` cue (`public/99lc/game-config.json` `twohand-katana`; `engine.ts` Katana/parry/critical/cooldown branches; status/engine tests).

### M99. The generic hybrid sword does not implement the authored rhythm/opening sword
- **Expected:** correctly timed Zornhaw taps step forward and continue stagger pressure, matching aim motion raises their damage, fast taps cause Sword-only fatigue, elite/boss stagger pressure eventually grants visible Unstoppable, and Oberhaw may morph the current strike before an optional held Unterhaw follow-up.
- **Actual before the fix:** `hybrid-sword` is a generic fixed combo with no timing bands, fatigue, enemy opening state or contextual double-tap result. The schema also forces invented actions into unspecified gesture slots (`public/99lc/game-config.json`; `types.ts`; `engine.ts`).
- **Failure scenario:** tap too quickly, at the intended rhythm, or after an enemy opening appears → all inputs resolve the same cyclic attack.
- **Fix direction:** add explicit rhythm/opening state and allow disabled gesture slots instead of inventing undocumented moves.
- **Fixed:** 2026-07-16; reworked 2026-07-18 — `hybrid-sword` keeps all moves on the left runtime hand and explicitly disables unspecified slots. Zornhaw now bypasses legacy tap classification on press, alternates real side-to-side colliders, advances on every accepted strike, drives the configurable center rhythm/fatigue feedback and scales damage/impact from matching horizontal aim motion. Configurable stagger may be disabled as a subsystem; otherwise elites/bosses gain five seconds of visible Unstoppable after six default half-second staggers. Oberhaw immediately morphs an active Zornhaw, uses reduced base damage or the larger opening execution, and marks an executed victim so any later death resets its cooldown. Holding the second input through the configurable one-second gate queues the target-aware Unterhaw and lights its formerly dim HUD row (`public/99lc/game-config.json` `hybrid-sword`; `engine.ts` `press`/`applySwordRhythm`/`morphSwordAttack`/`applySwordStagger`/`executePendingUnterhau`; Builder, status and engine tests).

### M100. 99LC lacks in-world charge/readiness feedback and collider-faithful traces
- **Expected:** the player has color-coded gesture readiness, segmented charge sectors for the actual release outcomes, becomes transparent during invulnerability, and every attack leaves a fading trace of its real collider.
- **Actual before the fix:** charge feedback is a sidebar bar clamped at the global hold threshold, invulnerability only changes the outline to white, and melee/dash/projectile effects are approximate lines that omit dead zones/radius/swept geometry (`WeaponCooldowns.vue`; `engine.ts` `renderPlayer`/`renderEffect`).
- **Failure scenario:** charge the spear through its release sectors or pole-vault through danger → the arena gives no exact release boundary and the body remains opaque.
- **Fix direction:** derive Canvas cues and fading traces from the same geometry/state used by collision, while mirroring them in the accessible HUD.
- **Fixed:** 2026-07-16 — five stable gesture colors drive both HUD rows and world-space readiness pips; charge actions render threshold-accurate segmented rings, armed sectors and recovery blocking. The player is translucent through the complete resolved invulnerability action, and authored sweet spots/critical skills share a world-space `CRIT` cue. Sectors, strict annuli, capsules, circles and rotating sweeps share one collision/outline resolver; projectiles, dashes and low-frame-rate rotations sample their swept volume and feed those exact samples into fading traces. Empty slots and disabled/recovering gestures no longer advertise usable inputs, and the Builder exposes strict dead-zone editing alongside the other collider controls (`colliders.ts`; `engine.ts` `dashStepColliders`/`updateActiveAreas`/`damageEnemy`/`renderColliderTrace`/`renderActionCues`/`renderPlayer`; `BuilderDrawer.vue`; `WeaponCooldowns.vue`; `TouchControls.vue`; collider/engine/page tests).

### M101. Existing schema-v2 browser overrides would strand testers on the obsolete nine weapons
- **Expected:** deploying the seven-weapon catalog upgrades the exact previous browser override while preserving unrelated player, enemy, room and balance tuning.
- **Actual before the fix:** `loadLastChancesConfig` returns localStorage before fetching `/99lc/game-config.json`; a saved schema-v2 catalog therefore permanently wins over the new server definition (`features/last-chances/config.ts` `loadLastChancesConfig`).
- **Failure scenario:** a tester with any saved Builder override opens the new build → the old spear, knife, bow, hammer, shield, orb and swords remain active.
- **Fix direction:** fetch the current schema-v3 definition, merge compatible non-weapon override tuning, adopt the new weapon/loadout/content references and persist the migrated result.
- **Fixed:** 2026-07-16 — loading a schema-v2 override now fetches the current server definition, preserves compatible player/input/enemy/progression/room tuning, adopts schema-v3 weapons/loadout/content references, validates the merged result and persists the one-time upgrade. Schema-v1's narrow geometry/tap repair remains separate, and schema-v3 overrides still load without a network merge (`config.ts` `mergeLegacyOverrideWithCurrent`/`loadLastChancesConfig`; exact migration/error tests).

### M102. Post-roll character replacement bypasses mutually exclusive roster pairs
- **Expected:** LeCrisp/Толя and HardKitty/Эрен Йегер can never coexist through a normal roster assignment, including guaranteed rewards, draft confirmation and the admin test picker (`CHARACTERS.md`; `StartGameLogic.AreMutuallyExclusiveCharacters`).
- **Actual before the fix:** natural rolling removed both counterparts, but the queued-loot replacement claimed only an exact-name bot or the first bot, while the admin test picker swapped only an exact duplicate (`WebGameService.cs:298-316,1194-1213`). Draft confirmation also rejected only exact duplicates. These post-roll paths could therefore introduce either forbidden pair after a valid bot roster had already been built.
- **Failure scenario:** create an admin test game as HardKitty after the preliminary bot roll assigns Эрен Йегер → Eren remains in the game; queued HardKitty or either member of the LeCrisp/Толя pair has the same failure mode.
- **Fixed:** 2026-07-16 — `StartGameLogic.AreMutuallyExclusiveCharacters` is now the shared roster rule. Natural/draft pools and competing guaranteed reservations apply it; a queued reward or admin test choice replaces/swaps the incompatible bot seat; and both web and Discord draft confirmation revalidate the pair before replacing a bridge. An incompatible reservation that loses the shuffled reservation race remains queued for a later game rather than being consumed.

## 99 Last Chances authored-weapon regression review (2026-07-16)

### M103. Tap-parries resolve after the enemy's reactable window has already ended
- **Expected:** releasing a short parry input during the visible 95–190 ms final windup can stop the strike, while a double/held gesture cannot receive a free overlapping parry.
- **Actual before the fix:** the gesture recognizer waited the full 260 ms double-tap window before emitting `tap`; every shipped parry window was shorter, so a reactive parry always arrived late. Arming on raw press had previously made every compound gesture parry for free (`features/last-chances/gestures.ts`; interim `engine.ts` `press`/`performAttack`; `public/99lc/game-config.json` enemy `parryWindowMs`).
- **Failure scenario:** press and release Spear guard when the enemy marker opens → the enemy attack resolves before `tap`; start a double tap instead → a raw-press implementation would overlap guard and shove.
- **Fixed:** 2026-07-16 — a short first release creates a provisional directional parry immediately. If it catches an attack/projectile, the queued compound gesture commits only the parry; otherwise the provisional collider is canceled when the double/held gesture resolves. Pause/blur commits a consumed provisional state and cancels an unused one (`engine.ts` `beginProvisionalTapParry`/`commitOrCancelProvisionalParry`/`consumeActiveParry`; focused engine tests).

### M104. Charged invulnerability can end before the charged action
- **Expected:** every action marked invulnerable keeps the player translucent and immune for its complete resolved movement.
- **Actual before the fix:** invulnerability used the unscaled `invulnerabilityMs`, while charge bands could lengthen `durationMs`. The late Axe leap lasted 984 ms but became vulnerable after 900 ms; Katana dance remained active for 760 ms but lost immunity after 720 ms (`engine.ts` interim `performAttack`; `public/99lc/game-config.json` Axe/Katana attacks).
- **Failure scenario:** take a hit during the final 84 ms of a late Axe flight → damage lands while the leap animation/collider is still active.
- **Fixed:** 2026-07-16 — any authored invulnerability is raised to at least the fully resolved attack duration, and the same authoritative timer drives damage immunity, snapshot state and body transparency (`engine.ts` `performAttack`/`damagePlayer`/`renderPlayer`; charged Axe/Katana regression).

### M105. Spear inner radii still hit enemies standing body-to-body
- **Expected:** Spear damage starts beyond the protected close zone even for large enemy bodies, not merely enemy centers.
- **Actual before the fix:** annular collision accepted any target circle touching the damaging band. At real contact distance `player radius + enemy radius`, a 16–44-unit enemy still overlapped the 52/56/62-unit inner edge (`colliders.ts` interim `annulusHitsCircle`/`sectorHitsCircle`; shipped Spear colliders).
- **Failure scenario:** stand touching a large enemy and use the basic sweep → its center is inside the dead zone, but its outer circle receives full damage.
- **Fixed:** 2026-07-16 — collider definitions may opt into `strictInnerRange`, which rejects a target whose body overlaps the protected circle. Every relevant Spear basic/thrust/ram/release/stance collider enables it, the dash collider carries the same exclusion, and Quick Tune exposes the flag (`types.ts`; `colliders.ts` `resolveAttackCollider`/`colliderHitsCircle`; `engine.ts` `dashStepColliders`; `BuilderDrawer.vue`; contact-radius tests).

### M106. The charged Spear kick does not place enemies in Spear critical range
- **Expected:** the late “Критовая дистанция” kick pushes the target into the critical band and gains distance from its stronger charge.
- **Actual before the fix:** both kick bands teleported the target to the same 80-unit fallback; the resolved knockback multiplier affected neither final position nor the next Spear sweet spot, which begins around 140 units (`engine.ts` interim Spear shove/kick branch; `public/99lc/game-config.json` `spearKick`).
- **Failure scenario:** release the late charged kick and immediately use the basic Spear sweep → the target remains well inside the critical band.
- **Fixed:** 2026-07-16 — `spearKick` derives final range from the larger of its base target and resolved charge-scaled knockback. The shipped late band lands at 167.4 units; the knob remains external as `targetDistancePerKnockback` (`engine.ts` Spear kick branch; `public/99lc/game-config.json`; focused engine test).

### M107. Late Spear throws neither pin at the room perimeter nor keep carried enemies outside walls
- **Expected:** a late throw pins carried enemies only when the projectile meets an obstacle or room boundary and leaves their circle on navigable floor.
- **Actual before the fix:** only obstacles counted as walls; range/time expiry silently cleared carried IDs. On obstacle collision, the enemy had already been copied to the projectile's post-step point, potentially inside or beyond the wall (`engine.ts` interim `updateProjectiles`/`pinProjectileTargets`).
- **Failure scenario:** throw through an obstacle-free line toward the arena edge → the target travels outside the room and receives no wall pin; a large enemy hitting an obstacle can be stunned inside it.
- **Fixed:** 2026-07-16 — swept projectile checks now include arena bounds. A collision clamps the projectile, backtracks each carried enemy opposite travel until its full radius is outside obstacles/bounds, then applies the authored pin; natural range/time expiry still releases without an air pin (`engine.ts` `updateProjectiles`/`pinProjectileTargets`; obstacle-free perimeter regression).

### M108. Knife-spider turns toward the player during its locked leap and hides its rear
- **Expected:** once the leap vector locks, both movement and facing stay on that vector through a miss so following the spider exposes its back.
- **Actual before the fix:** `updateEnemies` unconditionally faced every attacker toward the player's current position before advancing a locked leap. A dodged Knife-spider therefore looked backward at its pursuer when the capture window opened (`engine.ts` interim `updateEnemies`/`updateEnemyAttack`).
- **Failure scenario:** dodge, run behind the spider along its path and press interact → rear-dot validation treats the player as standing in front.
- **Fixed:** 2026-07-16 — an attacker with a locked leap direction retains its facing until the leap finishes; the missed-spider stun/capture window therefore uses the actual rear hemisphere (`engine.ts` `updateEnemies`/`finishEnemyAttack`/`capturableKnifeSpider`; simulated miss regression).

### M109. Charged Claw dash scratches every enemy along its traversal
- **Expected:** charge controls travel range; the damaging scratch happens once at the dash endpoint, while Chemical may independently leave a trail.
- **Actual before the fix:** `clawDash` was a normal damaging dash, so every swept traversal collider dealt the full scratch and there was no endpoint attack (`engine.ts` interim `performDash`/`updatePlayer`; `public/99lc/game-config.json` `clawDash`).
- **Failure scenario:** align one enemy halfway through the dash and one at the destination → both receive the same full scratch.
- **Fixed:** 2026-07-16 — Claw travel is traversal-only and keeps its exact fading path/optional Chemical trail; completion spawns one externally tuned 82-unit, 110-degree endpoint scratch collider. A two-target regression proves only the destination target is damaged (`engine.ts` `performDash`/landing branch; `public/99lc/game-config.json` `clawDash.tuning`).

### M110. Authored critical attacks do not use the available world-space CRIT cue
- **Expected:** Spear tip criticals, deep Claw strike, Knife-spider impale, Katana overhead/execute and Sword opening visibly distinguish their critical result.
- **Actual before the fix:** `criticalHitMs` was set only by the Spear-spin head zone; the remaining critical multipliers changed health silently despite the renderer already supporting `CRIT` (`engine.ts` interim `damageEnemy`/`renderEnemy`).
- **Failure scenario:** consume a Sword opening or land the Claw deep strike → damage increases, but the target shows no critical feedback.
- **Fixed:** 2026-07-16 — `damageEnemy` marks every authored sweet spot/critical behavior with the shared 650 ms cue (externally overridable), while the existing Spear-spin headshot retains its own tuning. A table regression covers all named critical families (`engine.ts` `damageEnemy`/`renderEnemy`; focused engine tests).

## Sim sweep triage (2026-07-17)

> **Scope:** triage of the errors in sweep `DataBase/Simulations/sweep-20260716-230144` (100k requested games per AI level): four Int32-overflow game crashes (ai-0 #3120; ai-2 #2382/#2605/#2974), one unhandled NRE that killed the entire ai-1 arm after batch 3, and three non-fatal `CRIT … didn't do anything` bot stalls in the ai-1 log.

### M111. TickAsync's catch re-indexed the live GamesList and killed the process (M51's open race, now fatal)
- **Expected:** a per-game exception in the readiness loop is logged/sinked and the loop continues; concurrent `GamesList` mutation must never crash the host.
- **Actual:** `Global.GamesList` is a plain `List<GameClass>`; the sim runner adds thousands of games (`BotGameFactory`) and the watchdog removes stuck ones (`SimulationRunner`) while the timer thread iterates the same list in `CheckIfReady.TickAsync`. A racing `Add`/`Remove` exposes a transient null slot: `games[i]` returned null (NRE inside the loop body, caught) — and the catch block itself then re-indexed `games[i]`, throwing a second NRE that escaped the catch, propagated through the `async void` timer handler and terminated the process. The ai-1 sweep arm lost batches 4–19. This is exactly the "Still open" residual recorded under M51; the same race exists in production (web/Discord threads add games while the timer ticks).
- **Failure scenario:** bulk sim creates games / watchdog removes a stuck one while the timer thread is mid-iteration → null slot read → catch re-indexes the mutated list → unhandled NRE → process dies, taking every live game with it.
- **Fixed:** 2026-07-17 — every `GamesList` mutation now locks the list instance (adds: `WebGameService`, `BotGameFactory`, `General`, `AdminPanel`; removes: `CheckIfReady.HandleLastRound`, all four `SimulationRunner` sites); `TickAsync` iterates a snapshot taken under the same lock, null-guards each entry, and its catch uses the captured `game` reference instead of re-indexing. `HandlePredic2`'s `GamesList.Find` + `game!` (the earlier member of this race family named in M51) got a null-guard. Unsynchronized *reads* on request threads remain (e.g. `Global.GetGame`) — an exception there stays inside the request and cannot kill the process.

### M112. Battleship's live rules diverged from the approved designer review
- **Expected:** the 2026-07-17 choices in `BATTLESHIP-DESIGNER-REVIEW.md` are one authoritative runtime contract: one global/idempotent Boarding transition, dead-deck-only reset rules, source/deck-aware weapons, compulsory CAPTURE, non-predictive defeat, deterministic first-player tie resolution, physical-board movement effects, legal bot knowledge/actions, reviewed UI visibility and exact GDD assets. Explicit rejects I02/I16/D14 and the own-board-only Greek Fire exception remain exceptions.
- **Actual before the fix:** Boarding could retrigger and resolve only one side; repeated dead-deck shots reset turns; weapon selection lost source/module identity; CAPTURE could toggle or softlock and was not represented end-to-end; poison/reveal/summon movement crossed incorrect boundaries; defeat predicted ammo sufficiency; setup and bot paths bypassed player rules; legal weapon/status/armor UI state was incomplete; Discus and dead model scaffolding appeared live despite no mechanic.
- **Failure scenario:** destroy the first Mid, fire into an already dead deck, capture a Close ship, or destroy one of several identical weapon modules → bonuses/conversions repeat, the shot series continues illegally, the owner cannot complete the forced own-board action, or the wrong module remains selectable; clients and bots then act on state that the server should have hidden or rejected.
- **Fixed:** 2026-07-17 — implemented D01–D18, I01/I03–I15/I17–I20, Part D and the approved Part E defaults in the Battleship engine/service/hub/Vue flow. I02/I16/D14 remain rejected as recorded. The exact nine GDD bitmap assets ship under `Web/VueClient/public/battleship/icons/`; `BattleshipGDD.md`, `WEB-BACKEND.md`, `WEB-CLIENT.md` and the review implementation record now describe the live contract. Core anchors: `BattleshipGameEngine.GetUsableWeapons`/`TriggerBoarding`/`MoveSummons`/`CheckWinCondition`; `BattleshipService.SelectWeapon`/`PassBoardingTurn`/`MapPlayer`; `CombatPhase.vue`, `WeaponBar.vue`, `CellComponent.vue` and `store/battleship.ts`.

### M113. Battleship follow-up state projection loses summon/reveal identity and serializes the bot's whole reset chain
- **Expected:** the 2026-07-17 follow-up specification is authoritative for Battleship presentation and edge cases: dead summons disappear from active status; historical trail cells retain every visiting summon type; Scout and post-destruction reveals distinguish empty water from intact ships; a shot into any already-dead deck is a miss; Maneuvering Double reconciles its stale hit after sinking; collision damage is identical in both views; only a regular Ram may turn back; CAPTURE/Devastated and duplicate ship identities stay unambiguous; bot reset shots have the same two-second interaction window as a human; projectile/impact/sinking VFX never change resolution.
- **Actual before the fix:** `Cell.SummonTrail`/`CellDto.SummonTrail` are one boolean and the Vue fallback converts every unknown trail to `Ram`; the local trail cache can recover a type only while that summon remains in the current DTO. Reveal helpers set only `IsRevealed`, so empty Scout/blast cells have no miss mark, and `RevealShip` explicitly skips occupied neighbours. Maneuvered historical hit flags are never reconciled on final death. Ram scratches do not persist the same hit identity in fog. Every summon can enter `WaitingForTurnBack`. `ProcessBotTurn` resolves up to 100 reset shots synchronously while the game lock is held. `CellComponent` gives destroyed red priority over Devastated and uses purple for intact revealed occupancy; projectile VFX always start at the target board's bottom centre as one cannonball (`BattleshipModels.Cell`; `BattleshipGameEngine.RevealShip`/`RevealArea`/`MoveSummons`/`MarkRamDamageOnBoard`; `BattleshipService.ProcessBotTurn`; `CombatPhase.vue` trail fallback; `CellComponent.vue`; `useVfx.ts`).
- **Failure scenario:** send a Scout through a Ram trail and let Drakkar freeze it, then sink a moved Maneuvering Double beside another ship while the bot has a reset chain → both trail legends say Ram, empty revealed cells have no permanent miss glyph, the adjacent intact ship stays dark, the old destroyed-deck coordinate remains red, the two views may disagree on the rammed Triple deck, and the bot completes every follow-up shot before the human can deploy a summon.
- **Fix direction:** make trails a persistent set of summon types per physical cell; centralize reveal-result marking and reveal ownership; reconcile Maneuver history on sink; reject dead-deck damage before hit flags/logs; restrict re-entry to regular Ram; serialize one bot action per delayed hub pump; project typed shot source/projectile data for privacy-safe VFX; update the Battleship GDD/backend/client contracts and add focused regression coverage.
- **Fixed:** 2026-07-17 — `Cell.SummonTrails` is now a persistent typed set projected to both boards; active DTOs contain only living summons. `RevealCell` owns empty-miss/anonymous-occupancy projection and credits only its actor, while `RevealShip` exposes occupied Space and reconciles Maneuvering Double's vacated destroyed deck on final death. Dead-deck shots return miss before hit/Mast/reset logic; Ram collision marks the exact deck in both views; only regular Ram can re-enter and spawn/re-entry runs collision hazards immediately. CAPTURE, black Devastated cells and duplicate fleet names remain unambiguous. The bot advances through a two-second one-action hub pump with the lock released. Typed shot source/target/projectile data drives privacy-safe arrow/stone/buckshot/fire flight plus splash, splinters and sinking VFX. (`BattleshipModels.Cell`/`ShotResult`; `BattleshipGameEngine.RevealCell`/`RevealShip`/`MoveSummons`; `BattleshipService.ProcessBotStep`; `GameHub.RunBattleshipBotPump`; `ProjectileLayer.vue`; `CellComponent.vue`.)

### M114. Джон Сноу does not start in Черный Замок, so opening loyalty pays when it should be blocked
- **Expected:** whenever Джон Сноу is in the roster, initial placement puts him at exact place 4, immediately starts the three-action-round Черный Замок hold and leaves him on neither loyalty side for rounds 1–3 unless an explicit position effect moves him.
- **Actual:** `HandleEventsBeforeFirstRound` calls `JonSnow.FinalizeInitialPositions` before the common callers assign `Status.PlaceAtLeaderBoard`. `FinalizeInitialPositions` neither moves the list nor recognizes the default place value as 4, so the initial hold never starts and Jon keeps his random seat (`CharacterPassives.cs:487`; `JonSnow.cs:426-449`; callers assign places only afterward in `General.cs:293-296`, `WebGameService.cs:400-405`, `BotGameFactory.cs:80-83` and draft completion).
- **Failure scenario:** Jon rolls into place 1–3 or 5–6 → round-1 allies win fights → Черный Замок awards bonus points even though the intended opening three turns should place Jon at neutral place 4.
- **Fix direction:** make the final initialization step move Jon to list index 3 and arm the initial hold before the shared caller assigns numeric places.
- **Fixed:** 2026-07-17 — `JonSnow.FinalizeInitialPositions` now receives the mutable authoritative list, moves Jon to index 3 after every other opening placement effect, and arms the hold through action rounds 1–3 before the common callers assign place numbers. Place 4 remains neutral in `AwardBlackCastleLoyalty`, so those opening rounds cannot award side-victory points unless a later explicit mover removes Jon and `FinalizePositionEffects` cancels the hold.

### m49. Score-derived achievement progress overflowed Int32 and crashed HandleLastRound
- **Expected:** `TrackGameEnd` measures achievement progress for any legal final score.
- **Actual:** Гордон Фримен's Halflife 3 super multiplier `P × P^P` legitimately produces round-10 settlements above `int.MaxValue` (P=9 → ≈3.5 billion; score is decimal by design). `(int)Math.Floor(...)` on `RoundTenRegularPoints` (`g_quad_damage`) threw `OverflowException`, aborting `HandleLastRound` — the game errored at round 11 and was additionally reported stuck. The same unguarded cast sat on Итачи's `TotalStolenPoints` (`c_itachi_tax` — Цукуеми inherits the complete transformed Halflife settlement) and Наруто's `ShadowPointsTransferred` (`c_naruto_rasengan`). Four games crashed in the sweep; every lineup contained Гордон Фримен.
- **Failure scenario:** Gordon attempts the Halflife 3 release on round 10 with ≥9 pending regular points → settlement > 2³¹ → `TrackGameEnd` throws at game end → no winner/rewards, game stuck at round 11.
- **Fixed:** 2026-07-17 — added `ToProgress(decimal)` (floor + clamp to [0, int.MaxValue]) in `AchievementClass` and routed all three casts through it. Progress semantics unchanged for every legitimate sub-int value.

### m50. L1 "must attack" bots could end their turn with no action after one rejected blind retry
- **Expected:** a bot whose policy forbids Block (Наруто clones, Спарта/Aggress carriers, blades/Мишень/event plans) always ends readiness with an attack, or a legal Block/Skip when nothing is attackable.
- **Actual:** in the legacy (L1) attack path, when the preference-roll attack was rejected, the `isBlock == noBlock` recovery branch picked **one** random target and ignored `AttackPlayer`'s return value. A second rejection (sealed Мадара is still present in the legacy target pool at rounds 9–10; pet-pair/Вампур re-attack rules likewise reject) left the bot with no attack, no Block, no Skip — tripping the readiness CRIT guard `didn't do anything and auto move didn't as well`, which force-assigned Block/Skip. Three occurrences in the ai-1 sweep log (all Наруто, rounds 9/10). Same family as M66 (Gordon, fair path).
- **Failure scenario:** L1 Наруто clone at round 9 rolls sealed Мадара → rejected; the blind retry randomly picks Мадара again → silently rejected → no action → the CRIT fallback forces Skip, wasting the clone's turn (clones are designed to always attack).
- **Fixed:** 2026-07-17 — the recovery branch now retries the whole remaining pool (mirroring `HandleBotAttackRandom`'s loop) and, if every candidate is rejected, falls through to the authoritative `HandleAttack(-10)` fallback — Наруто clones route through `AttackOrSkipInsteadOfBlock` (attack anyone legal, else a real Skip), everyone else gets a legal Block (`BotsBehavior.cs` legacy `isBlock == noBlock` branch; `GameReactions.cs` `AttackOrSkipInsteadOfBlock`).

### m51. Король Сервера appears at the bottom instead of replacing Тупой бастард in the passive panel
- **Expected:** reaching 228 Skill replaces the visible Тупой бастард card in place with Король Сервера, preserving that slot in Discord/web passive presentation.
- **Actual:** `JonSnow.TryBecomeKing` removes Тупой бастард from the passive list and merely reveals the already-existing Король Сервера entry near the bottom of `characters.json`; order-preserving clients therefore render the replacement at that lower index (`JonSnow.cs:79-87`; `SkillsPanel.vue:140-163`).
- **Failure scenario:** expand Тупой бастард, reach 228 Skill → its card disappears and the newly visible Король Сервера description appears several rows lower rather than in the vacated slot.
- **Fix direction:** move the existing hidden Король Сервера `Passive` object into the exact former Тупой бастард index while revealing it.
- **Fixed:** 2026-07-17 — `JonSnow.TryBecomeKing` now removes the existing hidden King object from its old position, reveals it and assigns that same object into Тупой бастард's exact list index. Both Discord's ordered fields and Vue's index-keyed expanded card therefore retain the replacement in place.

### m52. Черный Замок has no public marker on place 4
- **Expected:** while Джон Сноу's Черный Замок mechanic exists, the fixed fourth leaderboard cell is visibly marked as the castle in Discord, web and replay projections.
- **Actual:** `GameUpdateMess.CustomLeaderBoardBeforeNumber` adds public Джон `🐺` marks but never annotates the fixed place-4 cell; web/replay use the same projection (`GameUpdateMess.cs:245-334`; `WebGameService.cs:196-222`, `PopulateCustomLeaderboard`).
- **Failure scenario:** open any Джон match → the leaderboard gives no visual indication which cell is Черный Замок or where the upper/lower-side boundary lies.
- **Fix direction:** add a public `🏰` prefix to exact place 4 through the shared custom-leaderboard projection whenever the roster contains the Черный Замок holder.
- **Fixed:** 2026-07-17 — `GameUpdateMess.CustomLeaderBoardBeforeNumber` now emits `🏰` for exact place 4 whenever Джон Сноу's roster entry still carries Черный Замок. Discord uses it directly; web and replay receive the same prefix through `WebGameService.PopulateCustomLeaderboard`.

### M115. 99LC «Включить функции DualSense» could never open the WebHID chooser — no production transport was wired
- **Expected:** in a secure Chromium context the explicit `Enable DualSense features` action opens the browser HID chooser for a DualSense/DualSense Edge pad and, once granted, raises feedback to Tier 2 (adaptive triggers + rumble); denial or unsupported contexts fall back without affecting input.
- **Actual:** the documented-as-intended verified-hardware boundary (WEB-CLIENT.md §Tier 2): both `DualSenseFeedbackController` constructions omitted the `outputs` argument (`engine.ts` constructor and `applyControlDefinition`), so `enableEnhancedFeatures()` returned false in `feedback.ts` before any WebHID code ran; the only `requestDevice` caller (`DualSenseWebHidDriver`) was never instantiated in production and no packet serializer existed in the repo. Designer verdict 2026-07-18: cross the boundary — ship the real transport, USB + Bluetooth, with physical verification performed by the designer.
- **Failure scenario:** connect a DualSense in Chrome on the hosted HTTPS page, select the DualSense scheme, click «Включить функции DualSense» → no permission prompt ever appears, the toast always reports enhanced feedback as not enabled and adaptive triggers can never activate.
- **Fixed:** 2026-07-18 — new `dualsense-serializers.ts` ships the production transport: USB output report `0x02` and Bluetooth report `0x31` (sequence nibble, `0x10` tag, CRC-32 over `0xA2` + report id + payload) built from the shared 47-byte common payload (rumble motors plus per-hand adaptive-trigger blocks from each effect's `adaptiveProfile`; neutral writes full-off mode `0x05`). `createLastChancesDualSenseEnhancedOutput` binds `navigator.hid` behind the Sony allowlist and is injected as `outputs.enhanced` at both engine construction sites; unsupported/insecure contexts keep Tier 0/1 with a dedicated unsupported toast. Trigger-byte feel (mode/scaling) may still be tuned after the designer's physical Tier-2 pass; the `/99lc/dualsense-harness.html` page supports that iteration. The first pass also read calibration feature report `0x05` over Bluetooth — that regression is catalogued and fixed as M116.

### M116. M115's Bluetooth prepare step froze controller input — the pad was flipped into extended report mode
- **Expected:** enabling Tier 2 over Bluetooth leaves controller input untouched: the Gamepad API keeps reading the pad while WebHID sends output reports alongside it.
- **Actual:** the first M115 implementation read calibration feature report `0x05` in a Bluetooth `prepare` hook before the neutral packet. That read flips a BT DualSense from simple mode (standard-typed `0x01` input reports) into extended mode (vendor-typed `0x31` input reports), which Chromium's Gamepad API cannot map — controller input froze while HID output kept working, and the flip persists until the pad power-cycles. The flip is unnecessary for output: the pad accepts CRC-framed `0x31` output reports while staying in simple mode (verified on the designer's physical pad via a standalone trigger-lab page that never reads `0x05` and works).
- **Failure scenario:** BT DualSense → «Включить функции DualSense» → grant → mouse-driven game actions rumble and set trigger resistance, but the pad's buttons/sticks/triggers stop driving the game entirely until the controller is turned off and back on.
- **Fixed:** 2026-07-18 (same change-set) — removed the `prepare` hook end-to-end (serializer interface member, driver call, `receiveFeatureReport` on the device shape, related tests); `BluetoothDualSenseSerializer` now carries a comment documenting why the report mode must never be touched.
- **Re-verified:** 2026-07-18 (later the same day) — the freeze was re-reported from a build that still contained the `0x05` read; a triage pass initially concluded (from SDL's PS5 driver, which claims any effects output packet flips the pad) that Bluetooth Tier 2 was impossible and briefly shipped a USB-only restriction, then reverted it based on a lab check believed to show input surviving Bluetooth output. **Correction (same day, M118):** that lab check was wrong — the designer's controlled retest showed the trigger-lab page loses Gamepad-API input the moment its first Bluetooth output packet is sent, i.e. SDL's claim holds for this pad/firmware. Any BT effects output flips the pad; the durable fix is the M118 WebHID input reader, not any output byte recipe.

### M117. Phase 11's canonical soldier migration leaves city rendering on deleted veteran/recovery collections
- **Expected:** opening a city derives wounded-veteran treatment and cohort recovery status from the canonical per-soldier army state; an army with no wounded or recovering soldiers renders normally.
- **Actual before the fix:** Phase 11 migrates legacy `army.veterans` and `army.recoveries` into `army.unitInstances`, then deletes both compatibility collections. `EmpiresEndgame.vue` still called `Object.entries(state.army.veterans)` whenever a Medical Academy was operational and `.filter` on `state.army.recoveries` for every visible cohort. The production epidemic QA fixture builds an Academy, so selecting its infected city threw `TypeError: Cannot convert undefined or null to object` and blanked the city projection; a city with a recruited cohort could hit the same stale-read family while building its recovery badge (`engine.ts` `normalizeArmyUnitInstances`; `pages/EmpiresEndgame.vue` pre-fix `cityViews`; `cypress/e2e/empires-endgame.cy.ts` epidemic scenario).
- **Failure scenario:** restore a schema-v14 campaign containing an operational Medical Academy or a recruited cohort, then open the city → the page can crash before epidemic, cohort or treatment details render, including when the corresponding canonical list is empty.
- **Fixed:** 2026-07-18 — cohort recovery counts/ready cons now come from the cohort's canonical member instances, and the Medical Academy projection filters/sorts the same canonical dictionary by stable ID, exposes only `veteran && wounds > 0`, and passes that instance ID to `treatVeteran`. The full epidemic Cypress scenario exercises the Academy city with an empty veteran list, while the expedition browser flow carries a canonical recruited cohort; both render without an exception (`EmpiresEndgame.vue` `cityViews`; `engine.ts` `treatVeteran`; `cypress/e2e/empires-endgame.cy.ts`; `cypress/e2e/empires-endgame-expedition.cy.ts`).

### M118. Any Bluetooth effects output flips the DualSense into extended report mode — Tier 2 enable froze controller input
- **Expected:** enabling Tier 2 over Bluetooth keeps the pad fully playable: controller input keeps driving the game while WebHID delivers rumble and adaptive triggers.
- **Actual:** on the designer's pad/firmware, receiving *any* CRC-framed `0x31` effects output over Bluetooth — including the neutral packet written at enable — flips the pad from simple mode (standard `0x01` input reports) into extended mode (vendor-typed `0x31` input reports), exactly as SDL's PS5 driver warns. Chromium's Gamepad API cannot map the vendor-typed reports, so `navigator.getGamepads()` freezes everywhere (any tab) until the pad power-cycles. The M116 fix was correct but insufficient: it removed the `0x05` feature-report read, yet the first output packet triggers the same flip. Designer evidence 2026-07-18: after the in-game freeze, input was dead in every page until power-cycle, and the root `dualsense-trigger-lab.html` reproduces the freeze as soon as it connects and sends its first output — disproving the earlier lab-based re-verification recorded under M116 (`dualsense-serializers.ts` `BluetoothDualSenseSerializer`; `dualsense-hid.ts` `enableEnhancedFeatures`).
- **Failure scenario:** BT DualSense → «Включить функции DualSense» → grant the chooser → rumble/adaptive triggers work for mouse-driven actions, but the pad's sticks/buttons/triggers stop driving the game (and every other page) until the controller is turned off and back on.
- **Fixed:** 2026-07-18 — the flip is accepted as pad firmware behavior and compensated on the input side: new `dualsense-input.ts` parses the extended `0x31` input reports (sticks, analog triggers, hat, all buttons) into canonical standard-gamepad order, and `DualSenseHidInputReader` exposes the latest reading as a synthetic standard-mapping pad with a 500 ms staleness cutoff. `DualSenseWebHidDriver` starts the reader on Bluetooth enable and surfaces it via `hidInputSnapshot()`; `DualSenseFeedbackController.hidGamepadSnapshot()` forwards it, and `engine.ts` `pollGamepad` prefers a fresh WebHID reading over the frozen Gamepad-API list so the frozen pad cannot win arbitration. Because the pad stays flipped until power-cycle, disabling enhanced output (and output write failures) over Bluetooth keeps the device open input-only with an honest `controls-only`/`error` capability message, and re-enable reuses the retained device without a second chooser prompt; page teardown closes fully. USB is untouched (Gamepad API keeps working there).

### M119. Tier-2 DualSense effects never stopped — one HID write left the motors energized until the next effect
- **Expected:** a Tier-2 rumble effect ends after its `durationMs`, like Tier 1's `playEffect({ duration })`; between cues the pad is silent and only the resting adaptive-trigger state remains.
- **Actual:** `DualSenseWebHidDriver.play()` wrote one output report per effect and returned; nothing ever wrote a motor-off packet afterward, so the motors stayed energized until the *next* effect or a full `neutralize()`. Combined with the long `ramp` (600 ms) / `tension` (520 ms) profiles emitted on every combo-node advance, this produced the reported "rumbles all the time" — over WebHID the rumble was literally continuous (`dualsense-hid.ts` `play`; `feedback.ts`).
- **Failure scenario:** enable DualSense features (USB or BT) → pull a trigger through one gate → the pad keeps buzzing indefinitely until another cue or disable.
- **Fixed:** 2026-07-18, as part of the per-weapon haptics rework — every effect played on the enhanced output now schedules a stop at its `durationMs` that re-writes the **baseline** (motors off, each hand's resting/detent adaptive-trigger block kept): `buildDualSenseBaselinePayload` + `baseline()` on both serializers, driver `setBaseline`/`writeBaseline` with deep-equal dedup and restore-on-enable, `DualSenseFeedbackController.scheduleEnhancedStop` on an injectable scheduler (superseded by a newer effect, cancelled by neutralize/dispose/off). The same rework replaced continuous cue rumble with discrete gate ticks, pulse-count charge-band patterns, per-weapon commit signatures, per-node adaptive-trigger ladders and the spider-knife escape wriggle (`feedback.ts`; `dualsense-serializers.ts`; `dualsense-hid.ts`; `engine.ts`; `public/99lc/game-config.json`; `docs/WEB-CLIENT.md` §12A).

## Unfinished work backlog (2026-07-12)

### Still-open findings
- **m12** — Сайтама's round-1 "serious targets" are effectively arbitrary (skill is 0 at game start).
- **m19** — Итачи's Crows/Izanagi charges have no web-UI representation (only Tsukuyomi state is mapped).
- **m24** — ARAM pick phase has no web UI (hub/REST/serialization exist, no Vue component).
- **m26** — `HandleBotAttack` scoring flags never reset per target (deliberately deferred; needs its own sim-compared change).

### Bot-strength follow-up
M45 replaced the privileged L2/L3 scorer with a fair information boundary and completed the persistent Darksci/Глеб plus character-plan work. Future balance sweeps must compare the new legal L2/L3 policies by character and `AiPlaystyle`; the editable decision catalogue and suggested review questions are in [BOT-AI-DESIGNER-REVIEW.md](BOT-AI-DESIGNER-REVIEW.md).

### Unbuilt GameDesign.txt characters
Торик, Rey, Lewdweak, Второй Крисп, Leshkinson a.k.a. Удир, Кнусклес, Таинственный Клоун, Бог ЛоЛа, and the Хранитель МСД / Машина Судного Дня event boss. (Named in rarity tables only, no kits: тоширка, Hitler.)

### Unbuilt systems & intents (GameDesign.txt)
Full team-mode ruleset (2х2х2/3х3 team-score win, forced ally predictions — only the TeamModeOnly pair is seeded); new-player onboarding/tier gating (no bot predictions vs newbies, tier unlocks every 10 games); player statistics command (most points/wins/performance/elo with defined formula); donation multiplier shop item; pre-release cleanup list (rename internal log strings «евреи»/«тебя усыпили», «Ты»→«Вы» sweep, hide round 11 from view, split `HandleNextRoundAfterSorting` round-11 handling into a post-loop pass). Designer margin note that Толя «Подсчет» was only half-implemented («ты это на половину сделал»).

### Code TODOs & dormant blocks
- `CheckIfReady.cs:670` — `//todo: need to redo this system` (disabled `_finishedGameLog.CreateNewLog`).
- `GeneralCommands/HelpModule.cs:116` — `//TODO Move it as service`.
- Commented-out «Оборотень» stat-swap blocks (`CP:494-502`, `CP:1241-1242`) — dead code awaiting a decision.
- Root `BattleShip_update` ТЗ: all 23 items were re-verified against backend, bot and Vue paths on 2026-07-12; M41/m46 document and close the cross-action/feedback gaps found behind the old ✅ annotations.

## Summary count

**1 Critical** (C1) · **119 Major** (M1–M119) · **52 Minor** (m1–m52) · **14 Design questions** (D1–D14). Phase-6 designer review was fully implemented 2026-07-13: M37–M40 and m37–m44 fixed, D12 confirmed intended, D13–D14 fixed; m47 was discovered and fixed in the same score-floor change. M44 was discovered during the Близнец block audit; M45 was discovered and fixed during the bot-knowledge review; M46–M47 were discovered and fixed in their follow-up audits. m48 was reproduced from replay `2fb5f271` and fixed by restoring Darksci's same-transition public log. M48–M50 were triaged from production runtime errors and fixed 2026-07-13; M51–M52 closed the simulator performance/concurrency failures; M53 fixed unknown_bug's missing defensive Exploit stack. M54–M64 catalogue and close the interactive Empire's Endgame browser-game and QA hardening pass. M65–M66 close the production Discord renderer and Gordon Auto Move failures reported 2026-07-15. M67–M82 catalogue and close the two-pass 99 Last Chances GDD/live compliance audit plus regression review. M83–M91 catalogue and close the Empire's Endgame mechanics-completion and no-op-content audit. M92–M101 catalogue and close the authored 99LC weapon/runtime/visual/migration layer. M102 closes the post-roll mutual-exclusion bypass; M103–M110 close the final authored-weapon regression review. M111 and m49–m50 close the 2026-07-17 sweep triage; M112 closes the approved Battleship designer-review implementation. M113 closes the Battleship follow-up specification. M114 and m51–m52 close the Джон Сноу opening-placement/passive-order/board-marker defects. M115 ships the 99LC DualSense WebHID transport (adaptive triggers) per the 2026-07-18 designer verdict; M116 closes its Bluetooth extended-mode regression that froze pad input. M117 closes the Phase 11 canonical-soldier Medical Academy projection crash. M118 accepts the pad's flip-on-any-BT-output firmware behavior and restores Bluetooth Tier-2 controller input through a WebHID `0x31` input reader. M119 closes the never-ending Tier-2 rumble as part of the 2026-07-18 per-weapon DualSense haptics rework. Still open: **m12, m19, m24, m26**.

## Verification addendum (second pass, 2026-07-01)

A full re-verification was run over these docs: every file:line anchor (~230) was mechanically dumped and compared against the cited code — **all anchors correct**; the eight highest-risk claim clusters (agent-sourced or never personally read in the first pass) were re-read at the source; and a completeness diff was run (every `PassiveName` in characters.json and every `case` label in CharacterPassives.cs vs the docs).

**Corrected during verification** (all fixes applied in place):
1. `SecureRandom` was described as a crypto RNG in ARCHITECTURE §9 — it is a plain `System.Random` wrapper (→ new finding m21).
2. Tier semantics: Sakura and unknown_bug (Tier −1) were described as non-rolling specials — they are **secret rollable** characters (baseline range 40, humans only); unknown_bug is now additionally removed from every prediction/catalog surface. Молодой Глеб's exclusion comes from the `CharactersPull` Tier ≥ −1 filter, not a range-0 roll (m6, D3, CHARACTERS.md, GAME-DESIGN §11).
3. D5 ("2kxaoc") described the masking backwards — the passive is *exempt from* enemy-passive-name masking, not hidden by it.
4. CHARACTERS.md under-documented three verified mechanics, now added: Спартанец's "Это привилегия - умереть от моей руки" per-win rider (+1 extra Justice to the victim, −1 Int to himself, `CP:2859-2867`); Вампур's Гематофагия Psyche-priority rule (`CP:2904-2919`); Молодой Глеб's "Следит за игрой" marks up to **3** targets (`CP:4740-4780`).
5. Two flavor-only hidden passives were missing entirely: "God Of War" (Кратос) and "Искусство" (mylorik/Спартанец) — added.
6. Exact passive-name hygiene: "Стримснайпят и банят и банят и банят" and "Это привилегия - умереть от моей руки" appeared with typographic substitutions (…/—) in places; fixed to exact strings (they are load-bearing identifiers).

**Confirmed unchanged** (spot-listed because they were the highest hallucination risks): all `Luck()` probability figures (Luck(a,b) ≈ a-in-b, `SecureRandom.cs:35-45`); m19 (Itachi crows/Izanagi truly absent from the web layer); M2 (Jew widget state source); the bot moral thresholds 20/13/8/5/3 and round>10 auto-block; the AdminPlayerType runtime injection (`GameUpdateMess.cs:253`); the ARCHITECTURE §3 handler line table (all rows). No finding was retracted; C1–M12 all stand.

## Doc sweep — designer verdicts on ambiguous findings (2026-07-03)

The designer reviewed every ambiguous finding in `docs/DESIGNER-REVIEW.md`. The **БАГ** ones were fixed (each carries a `**Fixed:**` note above). The following were confirmed **ОК / intended** — no code change; the ⚠ divergence marks in `docs/CHARACTERS.md` and `docs/INTERACTION-MATRIX.md` were converted to "intended".

| Finding | Verdict (ОК) as confirmed by the designer |
|---|---|
| **M3** | AWDKA silently forced to last place before every fight calculation — intended hidden mechanic. |
| **M5** | "Тигр топ" opening window at game start (rounds 1–3) — intended, in addition to the later random window. |
| **M6** | "Лучше с двумя" counts Тигр himself — intended: Тигр is a member of his own clan. |
| **M12** | Монстр's "Пейзаж конца света" **does** kill goblin pawns — intended (the one kill-source goblins aren't immune to). |
| **m8** | Толя "Подсчет" cd 4–5 — no bug; the description counts from the **end of the effect**. |
| **m9** | Итачи Цукуеми recharge 4 rounds — no bug; changed nothing. |
| **m13** | HardKitty's opening −30 **score** logged as "−30 Морали" — intentional text joke. |
| **m14** | Butcher sup marks only from round 2 — intended. |
| **D1** | ~~Darksci can dodge Дизмораль by hoarding the round-9 level-up — intended tech.~~ **Reversed 2026-07-03**: web-only banking artifact; closed by M15 (web level-up gate). Darksci now eats the −5 on both platforms. |
| **D4** | "Булинг" / "Го играть" / "lvl-мяк" / "2kxaoc" keyed on character Name — intended (name-specific jokes). |
| **D5** | "2kxaoc" is a visible-only meme (exempt from enemy-passive masking) — intended. |
| **D6** | Вампуризм **copies** the victim's Justice (doesn't drain) — intended. |
| **D8** | "Пейзаж конца света" +7 **regular** (×4 = +28) — intended; changed nothing. |
| **D9** | Premade **copies** the Carry's fight-moral (Carry keeps theirs) — intended. |
| **D10** | Ziggurat copies any `Standalone` passive incl. Булькает / dead copies — left as-is (only «Еврей» excluded, D2). |

(**D7** was *not* ОК — the designer reclassified it as БАГ in chat; fixed in the goblin batch. See D7's `**Fixed:**` note.)
