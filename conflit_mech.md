# Конфликтующие механики core gameplay — дизайнерские вердикты

> Аудит 2026-07-26; дизайнерские вердикты и реализация сверены 2026-07-27. Метод: кандидаты собраны по `docs/GAME-DESIGN.md` / `docs/INTERACTION-MATRIX.md`, каждый пункт проверен чтением кода. Охват: базовые системы (математика боя, Justice, Мораль, Score, Вред/Дроп, пайплайн раунда, классы) + стыки «пассивка × базовое правило».
>
> Пары «персонаж × персонаж» каталогизированы в `docs/INTERACTION-MATRIX.md`; найденные дефекты и оставшиеся открытые вопросы — в `docs/AUDIT-FINDINGS.md`.
>
> Маркировка после вердиктов: ✅ исправлено · ⚪ подтверждено, код не меняется · 🟠 открыто и требует отдельного правила агрегации/приоритета.
>
> Сокращения: `CR` = `Game/GameLogic/CalculateRounds.cs`, `DM` = `Game/GameLogic/DoomsdayMachine.cs`, `CC` = `Game/Classes/CharacterClass.cs`, `CIR` = `Game/GameLogic/CheckIfReady.cs`, `IGS` = `Game/Classes/InGameStatusClass.cs`, `GPB` = `Game/Classes/GamePlayerBridgeClass.cs`.

---

## 1. Justice

### 1.1 ⚪ «Любая победа обнуляет Justice» × блок и форс-бои
- **Механика A:** блок — главный источник Justice: каждый остановленный атакующий даёт +1 в буфер следующего раунда (`DM:817`), как и проигрыш боя (`DM:1259-1260`, `DM:1470-1474`).
- **Механика B:** победа в **любом** бою раунда ставит `IsWonThisRound`, и в конце раунда Justice обнуляется до посадки буфера (`CC:1897-1901`, посадка `CC:1943-1947`).
- **Конфликт:** блокирующего можно заставить драться против его воли (IsArmorBreak, Штормяк, Монстр, Рельса — INTERACTION-MATRIX §1), и если он **выигрывает** такой навязанный бой — вся накопленная за прошлые раунды Justice сгорает. Оборонительная механика наказывается за успешную оборону, которую игрок не выбирал.
- **Доказательство, что напряжение реально:** у Толи есть точечный вентиль — «Раммус мейн» снимает `IsWonThisRound` при победе с блока (`DM:1466-1468`). Ядро такой оговорки не имеет ни для кого другого.
- **Вердикт:** сейчас работает как надо. Победа в навязанном бою остаётся настоящей победой и обнуляет Justice по общему правилу; точечная оговорка Раммуса не расширяется.

### 1.2 ⚪ TooGOOD/TooSTONK живут только там, где их «не должно быть»
- **Механика A:** TooGOOD (|weighing| ≥ 13) и TooSTONK (≥ 30) меняют **только** `randomForPoint` (`CR:229-252`, `CR:304-347`) — порог броска Шага 3.
- **Механика B:** Шаг 3 выполняется только при нулевой сумме Шагов 1+2 (`DM:1031`). Но |weighing| ≥ 13 гарантирует ±1 на Шаге 1 — значит, до рандома дело доходит **только** если Шаг 2 (Justice-тайбрейк, `CR:418-439`) его погасил.
- **Конфликт:** механика «ты слишком хорош» срабатывает исключительно в ситуации «слабый противник пере-справедливил тебя», и там работает **против** собственной механики Justice: Justice слабого расширяет окно броска (`maxRandom`, `CR:449-455`), TooGOOD сильного двигает порог (70/30). Две ручки крутят один бросок в противоположные стороны. Как «страховка сильного от Justice-апсета» — связно, но нигде так не описано.
- **Вердикт:** сейчас работает как надо; без изменений.

### 1.3 ⚪ Нелимитированный скилл-модификатор Шага 3 гасит Justice-окно
- **Механика A:** `randomForPoint += (mySkill − targetSkill)/60` без ограничения (`CR:372-377`).
- **Механика B:** Justice сжимает/расширяет `maxRandom` (`CR:449-455`), защита от нуля — только `Math.Max(1, …)` на кубике (`CR:467`).
- **Конфликт:** при большом разрыве скилла `randomForPoint` уходит ≥ `maxRandom` (гарантированная победа) или < 1 (гарантированное поражение) — и тогда Justice-механика Шага 3 математически отключена: никакое значение Justice обеих сторон не влияет на исход. Порог и окно не согласованы между собой.
- **Вердикт:** сейчас работает как надо; модификатор остаётся нелимитированным.

### 1.4 ⚪ Немезис асимметричен: в атаке сильнее, чем в обороне
- **Факты:** `nemesisMultiplier = 1.5` выставляется только атакующему (`CR:78-85`); защитник с контр-классом получает лишь +1 к множителю скилла (`CR:87-92`). В Шаге 3 Justice усиливается немезисом опять только у атакующего: `myJustice × nemesisMultiplier` (`CR:452-454`).
- **Конфликт:** «контр» как правило игры подаётся симметрично (класс контрит класс), но фактически контр-класс в обороне заметно слабее контр-класса в атаке — и это нигде не сообщается игроку.
- **Вердикт:** сейчас работает как надо; асимметрия намеренная.

---

## 2. Мораль и Score

### 2.1 ✅ Штрафы пробитых пулов больше не инвертируют вклад
- **Механика A:** пробитие Int-пула даёт −10% скилла навсегда (`CC:345`), пробитие Психики — −20% морали (`CC:352`). Оба хранятся счётчиком без ограничений.
- **Исправленное правило:** множители остаются линейными, но `GetIntelligenceQualitySkillBonus` и `GetPsycheQualityMoralBonus` имеют нижний предел **0**.
- **Конфликт:**
  - 10+ пробитий Int → множитель 0: Skill перестаёт давать вклад, но никогда не становится боевым долгом.
  - 5+ пробитий Психики → множитель 0: положительная Мораль может исчезнуть из эффективного значения, но не инвертируется в отрицательное.
- **Достижимость:** обычной игрой — вряд ли; через СуперМудень — да: рекурсия «каждое сработавшее пробитие ставит ещё один Вред в очередь» ограничена 50 **дропами** за ход, но не количеством Int/Psyche-пробитий (`DM:1301-1341`, `CC:236-359`), плюс доп. пробития Спартанца (`CC:293-332`).
- **Статус:** исправлено как M167.

### 2.2 ⚪ `MoralBonus` — одно поле на две роли
- **Роль 1:** психика ≥ 10 надувает мораль на +20%/тир — `SetMoralBonus` пересчитывает поле **целиком** после каждого раунда (`CC:1200-1203`, `CIR:1090-1091`), и этот бонус реально тратится (тиры читают `GetMoral() = Moral + MoralBonus`, `CC:1210-1214`).
- **Роль 2:** то же поле — копилка долга: не-боевая трата/потеря, уводящая мораль ниже нуля, паркует минус в `MoralBonus` (`CC:1317-1321`).
- **Конфликт:** долг живёт ровно до ближайшего `SetMoralBonus`/`ResetMoralBonus` (`CIR:1735` перед боями, `CIR:1090-1091` после раунда) — то есть прощается в пределах того же тика. Сейчас жизненные циклы случайно расставлены так, что внутри хода долг работает (нетто трат против бонуса), а между раундами испаряется. Любая перестановка порядка вызовов молча поменяет экономику морали. Хрупкая связка, не задокументированная нигде.
- **Вердикт:** пока не трогаем.

### 2.3 ⚪ Тройной тайминг «мораль → очки» и валюта 10-го раунда
- **Факты:** конверсия стейджится в `BonusPointsFromMoral` (`GameReactions.cs:104-137`) → флашится в начале **следующего** расчёта (`DM:269-282`; у мёртвых сгорает, `DM:272-275`) → на 10-м раунде вся мораль ≥ 5 принудительно конвертируется **до** боёв (`CIR:1723-1726`).
- **Конфликты:**
  - мораль, заработанная **в боях 10-го раунда**, не конвертируется никогда — канал наград последнего раунда (обыграть верхнего) в части морали выдаёт пустышку; единственное исключение — ручной возврат Сайтамы (см. каталог «На мели» в `docs/CHARACTERS.md`; предупреждение о ручном флаше — CLAUDE.md);
  - принудительный дамп идёт по **надутому** `GetMoral()` (см. 2.2: `ResetMoralBonus` выполняется позже, `CIR:1735`), т.е. +20% психики-10 монетизируются именно в этот момент — тонкая, нигде не описанная зависимость от порядка строк в одном методе.
- **Вердикт:** сейчас работает как надо; без изменений.

### 2.4 ⚪ Bonus и regular намеренно имеют разный курс
- **Факты:** regular буферится и умножается ×1/×2/×4 (`IGS:362-426`), bonus начисляется сразу и не умножается (`IGS:254-280`). Отрицательные regular тоже **умножаются** (−1 на 10-м раунде = −4, `IGS:397-425`), после чего пол 0 (вердикт m40=А).
- **Вердикт:** это фундаментальное правило игры: bonus начисляются сразу и **никогда не умножаются раундом**, regular буферятся и умножаются ×1/×2/×4. Проблемы нет; при добавлении награды автор обязан осознанно выбрать канал.

### 2.5 ⚪ Пол очков делает штраф за атаку в блок бесплатным при 0
- **Механика A:** атака в блок наказывается −1 bonus (`DM:786`).
- **Механика B:** bonus-мутации флорятся на 0 (`IGS:277-279`).
- **Конфликт:** игрок с 0 очков атакует блоки бесплатно — сдерживающий штраф не работает ровно против тех, кому нечего терять (типичный обитатель дна, спамящий атаки). Блокирующий при этом всё равно получает свою +1 Justice (`DM:817`) — асимметрия не полная, но штрафная половина петли выключена.
- **Вердикт:** сейчас работает как надо; это следствие подтверждённого пола очков.

---

## 3. Вред, Дроп, позиции

### 3.1 ✅ Бои используют строгий одновременный combat snapshot
- **Исправленное правило:** после всех разрешённых общераундовых подготовок движок замораживает неизменяемый `RoundFightCharacter` каждого игрока. Перед **каждым** фактическим боем оба участника получают свежие клоны этого шаблона; результат предыдущего параллельного боя не может изменить статы, класс или Justice следующего.
- **Justice:** `Status` остаётся общим служебным состоянием, но `Justice` глубоко копируется. Kimiko и **Убийственная Парочка** меняют Justice свежего fight-клона для текущей математики, а долговременную кражу отдельно проводят через `GameCharacter`; эта персистентная выплата не видна другим боям той же пачки.
- **Контракт для AI/maintainers:** рядом с созданием шаблона/клона в `DoomsdayMachine` оставлено прямое напоминание: новый боевой источник обязан проверяться на независимость от порядка параллельных боёв. Если механике нужен результат всей пачки, она сначала собирает события, затем агрегирует их после цикла.
- **Статус:** основной combat snapshot исправлен как M168. Найденные эффекты, которым ещё требуется дизайнерское правило агрегации результатов, перечислены в M171 и в итоговом списке ниже.

### 3.2 ✅ Текущий класс боя имеет одну точку истины
- **Исправленное правило:** Мишень, немезис, классовые перки, записываемый класс и вся математика читают текущий `FightCharacter` свежего боя.
- **Легитимные изменения:** ForOneFight-эффекты, например смена класса Геральта, применяются к этому fight-клону и потому являются истинным классом именно данного боя. Они не меняют замороженный шаблон и не протекают в параллельный бой.
- **Статус:** рассинхрон live `GameCharacter` и fight-класса закрыт в M168.

### 3.3 ✅ Мёртвые исключены из Drop-движения
- **Факты:** пред-боевой сброс `ResetMoralBonus`/`ResetStrengthQualityDropTimes` пропускает мёртвых (`CIR:1732-1738`), а толкание Дропа в конце раунда мёртвых **не фильтрует** (`DM:2183-2206`).
- **Конфликт:** умерший в раунде своего Дропа игрок сохраняет `StrengthQualityDropTimes` навсегда и толкается вниз на эту величину каждый последующий раунд. Практически незаметно (мёртвые и так на дне после сброса очков), но это ровно паттерн, который дизайнер объявил багом в вердикте M40: «мертвые вообще не должны участвовать, любое вмешательство, даже пассивное — баг».
- **Исправление:** `StrengthQualityDropTimes` сбрасывается и у мёртвых, а end-of-round Drop mover дополнительно фильтрует мёртвые места. Мёртвый игрок больше не сохраняет и не применяет stale Drop (m61).

### 3.4 ✅ Все источники кайта складываются
- **Исправленное правило:** `GetSpeedQualityKiteBonus` складывает пассивный бонус, Speed-10 `+1` и **Импакт `+2`**. Поэтому Импакт со Speed 10 даёт 3, а будущие/скопированные источники добавляются поверх. Плоский Speed-resist 6 у Импакта — отдельное подтверждённое правило и не менялся (m62).

---

## 4. Классы и статы

### 4.1 ✅ Versatility разрешает 1–1 через RPS выигранных статов
- **Исправленное правило:** обычное большинство сравнений Int/Str/Speed по-прежнему даёт ±5. Если один игрок выиграл ровно один стат, второй выиграл ровно один, а третий равен, сравниваются **категории выигранных статов** по фундаментальному циклу Int > Speed > Str > Int. `+5` получает игрок, чей выигранный стат контрит выигранный стат соперника; второй получает зеркальные `−5`.
- **Пример:** игрок A выиграл Int, игрок B выиграл Speed, Str равен → Int контрит Speed, значит A получает +5. Сам отдельный шаг немезиса ±2 и его skill-множители не менялись.
- **Статус:** отсутствующая ветка добавлена как M169.

### 4.2 ⚪ Тайбрейк класса Int > Str > Speed
- **Факты:** при равных максимальных статах класс выбирается фиксированным порядком (`CC:822-831`): 8/8 Сила/Скорость — всегда Сильный, N/N Int/что-угодно — всегда Умный.
- **Конфликт:** класс — живой и пересчитывается на каждый чих; равностатные билды систематически смещены к ранним веткам порядка, что меняет и немезис-геометрию, и попадание чужих Мишеней (`CC:814-843`), — при этом ни в одном игровом тексте порядок не назван.
- **Вердикт:** известная и намеренная визуальная механика «что выше — то и главнее»; без изменений.

### 4.3 ⚪ Психика посчитана дважды и может выходить за шкалу
- **Факты:** психика входит в scale (`CR:131`) и отдельным членом ±1/±2/±4 (`CR:201-221`). Безумие (DeepList) снимает пол 0 (`CC:1484-1485`, `CC:1542-1543`).
- **Конфликт:** все пороги (±4 при diff ≥ 6, размер пулов от стата) задуманы под шкалу 0–10; отрицательная психика пробивает диапазон разности (например, −5 против 7 = diff 12) — противник получает и scale-разницу, и максимальный психический член от значений, невозможных для остальных 45 персонажей. Один персонаж живёт вне системы координат ядра.
- **Вердикт:** намеренный дизайн DeepList. Фактическая разница считается без искусственного потолка: разница 12 означает 12; без изменений.

### 4.4 ⚪ Классовые перки несимметричны по ролям
- **Факты:** Быстрый +2 скилла в каждом бою обеих ролей (`DM:941-945`); Сильный +4 за победу в обеих ролях (`DM:1179`, `DM:1398`); Умный +6 — **только атакуя** цель с 0 Justice (`DM:935-938`).
- **Конфликт:** в связке с 1.1 (любая победа обнуляет Justice) Умный фармится исключительно агрессией по недавним победителям; пассивный Умный не получает от класса ничего. Ролевая асимметрия перков нигде не заявлена игроку (флейвор «нападает на тех, кто без Справедливости» — единственный намёк).
- **Вердикт:** работает как надо. Перк можно использовать или не использовать через выбор действия; баланс не меняется.

---

## 5. Пайплайн раунда и принудительные исходы

### 5.1 ⚪ Равные форсы аннулируются; unknown_bug имеет приоритет 666
- **Факты:** флаг реализован симметричными ±50 к сумме шагов (`DM:967-976`).
- **Вердикт:** два одинаковых обычных форса ±50 взаимно гасятся, после чего бой честно решается обычной математикой. Это намеренный контракт, а не ошибка.
- **Исключение:** финальный `AutoWin` unknown_bug имеет явный приоритет **666** и применяется после прочих заменителей результата; поэтому редкий персонаж выигрывает любой состоявшийся бой с любой стороны.

### 5.2 ✅ Штормяк видимо трогает Doomsday Machine
- **Факты:** трюк Кошачьей засады прибавляет ±5 к сумме **между** Шагом 1 и Шагом 2 (`DM:984-1013`).
- **Конфликт:** ±5 на аккумуляторе из ±1 делает Шаг 2 (±1) арифметически бессильным, а Шаг 3 (только при нуле) — недостижимым: в бою со Штормяком Justice-подсистема не участвует вовсе. В доках эффект описан как «nudge», по факту — терминальный решатель (плюс перенаправление победного очка).
- **Вердикт:** хаос-механика сохранена. В Fight Animation на результате Step 1 появляется подписанный удар лапой по **DOOMSDAY MACHINE**, искры и короткий толчок арены; затем остаётся чек с перспективно-правильным ±5, цветом «помог/помешал» и штампом переворота. Reduced-motion/skip показывают статический вариант, а «Все бои» — `🐾`/`🐾↻`. Три поля результата сохраняются в replay; точный delta/flip видят только участники боя (m63).

### 5.3 ⚪ Форс-бои × блок/скип/баны — обязательный дизайнерский вопрос
Семейство конфликтов «источник форс-боя пробивает состояние не-боя» каталогизировано и в основном закрыто точечными вентилями: M10, M11 (починены), M40 (мёртвый Штормяк), Гарем/Расенган-правила, изоляции Цукуеми/Кратоса. Единая точка правды — `docs/INTERACTION-MATRIX.md` §1; при добавлении нового источника форс-боя или нового состояния не-боя строка в таблице обязательна (там уже дважды ловили дыры).

В `DoomsdayMachine` добавлен AI/maintainer-комментарий: до реализации нового источника спросить геймдизайнера о приоритете и предложить как минимум три явных варианта — (A) обычная очередь уважает Block/Skip, (B) источник пробивает только названные состояния, (C) состояние полностью подавляет источник. Отдельно должны быть решены Dead, общий round-10 бан, Цукуеми/Кратос, sealed Мадара, unknown_bug и расход заряда при коллизии.

### 5.4 ✅ `MinusPsyche`: иммунитеты обязательны, стандартный лог настраивается отдельно
- **Было:** общая воронка одновременно проверяла 5 иммунитетов и безусловно писала «игрок психанул». Эффекту со своим текстом приходилось вызывать сырой `AddPsyche(−N)` и вместе с дефолтным логом случайно обходить Спокойствие/M.M.
- **Исправлено:** `MinusPsyche(game, −N, source)` всегда использует `GameCharacter`, проверяет все 5 иммунитетов и по умолчанию пишет глобальный «игрок психанул». Единственная настройка `suppressDefaultGlobalLog: true` убирает только эту строку, но не иммунитеты; Дизмораль переведена на такой вызов.
- **Граница:** сырой отрицательный `AddPsyche`/`SetPsyche` оставлен только там, где это не событие «психанул», а документированная цена самому себе, откат временного стата либо структурная трансформация (например снятие бонуса Гребанных ассасинов и Морок). Контракт закреплён в `AGENTS.md`/`CLAUDE.md` и M172.

---

## 6. Стыки «пассивка × базовое правило» (сводно)

| Стык | Суть | Якорь | Связь |
|---|---|---|---|
| Раммус мейн × обнуление Justice | единственный персонаж с вентилем от 1.1 | `DM:1466-1468` | →1.1 |
| Kimiko / Убийственная Парочка × Justice | fight-клон меняется только для текущего боя; персистентная кража проводится отдельно и не протекает в параллельные бои | `TheBoys.ApplyKillingCoupleJustice`; `CharacterPassives` Kimiko defense | →3.1 |
| Близнец × Block Justice | копирует persistent Justice остановленных атакующих как отдельное подтверждённое block-settlement правило | `DoomsdayMachine` block path; INTERACTION-MATRIX §4 | →1.1 |
| СуперМудень × рекурсия Вреда | реалистичный путь довести quality-множитель до 0; нижний пол не даёт инверсии | `CharacterClass.GetIntelligenceQualitySkillBonus` / `GetPsycheQualityMoralBonus` | →2.1 |
| Мадара × TooGOOD/TooSTONK | пороги доминации гейтятся числом атакующих — персонаж перенастраивает ядро под себя | `CR:231`, `CR:306-324` (`Madara.CanUseTooGood/TooStronk`) | →1.2 |
| Безумие × психический член весов | отрицательная психика пробивает диапазон ±4-порога | `CC:1484-1485`, `CR:201-221` | →4.3 |
| Импакт × кайт | `+2` складывается со Speed-10 и другими источниками | `CharacterClass.GetSpeedQualityKiteBonus` | →3.4 |
| Болевой порог × посадка Justice-буфера | монетка конвертирует каждый входящий пункт Justice в очко — легитимный, но единственный потребитель буфера до посадки | `CC:1919-1938` | →1.1 |
| Никому не нужен / HardKittyMinus × пол очков | пролом пола — отдельным мутатором без floor-проверки, а не флагом в общем | `IGS:232-245`, `IGS:277-279` | →2.5 |
| Штормяк × Justice-ступени | хаотичный ±5 сохранён и теперь явно показан в Fight Animation/replay | `DoomsdayMachine` Storm result; `FightArena*.vue` | →5.2 |

---

## Аудит похожих последовательных эффектов

Проверка **3-0 обоссан** подтвердила и закрепила требуемый кейс: при серии 2–0 и одновременных проигрыше+победе одному врагу парный проигрыш не стирает серию, победа переводит её в 3–0 и пассивка срабатывает. Исправлен остаточный cleanup, который раньше мог уже после прока обнулить сохранённую серию (M170).

Core-входы боя теперь изолированы шаблоном, но следующий аудит нашёл эффекты, чьи **результаты** всё ещё агрегируются последовательным порядком. Они не исправлены вслепую: для каждого нужен дизайнерский контракт «сумма / максимум / последнее событие / приоритет / один расход на пачку» (M171):

- счётчики/серии: mylorik **Спарта** `LostTimes`, HardKitty **Доебаться** (включая defense-clear), Weed/`WinStreak`, achievement `ConsecutiveWins`;
- накопительные метки: Homelander Rage→laser, Eren hatred/**Овца в загоне**, TheBoys virus, Toxic Mate **Get cancer**/`BlockMoralGain`, Итачи Crows/Цукуеми, Goblin population;
- live-экономика и последствия: unknown_bug Exploit commit, Darksci **Повезло** (live score copy), Omni-man **Подземный Поезд** (live score/Moral), Harm/resist/Drop attribution;
- изменение оставшейся пачки: Rick Portal переписывает очереди; смерти Кратоса отменяют ещё не исполненные заявки;
- ограниченные ресурсы, для которых особенно нужен явный приоритет: Изанаги uses, BFG charge/causal waves, каждый третий бой **Монтировки**, следующий Portal charge, next-fight capsule Salldorum.

## Итог

Исправлены **2.1, 3.1–3.4, 4.1, 5.2, 5.4**, включая Джона Сноу: превращение в **Короля Сервера** откладывается до завершения всей fight-пачки, поэтому Skill из одного боя не удваивает bonus другого одновременного боя. **5.1** формализован: равные обычные ±50 гасятся, unknown_bug имеет финальный 666. **1.1–1.4, 2.2–2.5, 4.2–4.4** подтверждены без изменений; **5.3** закреплён maintainer-контрактом. Оставшийся отдельный дизайн-аудит — M171.
